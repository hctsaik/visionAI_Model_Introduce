首頁
/
時間序列、運動與影片理解
/
V-JEPA
▷ 時間序列、運動與影片理解
先理解，再看工程圖
V-JEPA

你有許多未標動作的工業影片，想先學可以交給不同工作任務使用的表示，而不是要求模型逐像素重畫每段影片。

你會拿到：影片表示與下游任務候選。本課不把特徵差直接命名為缺陷機率，也不宣稱輸出未來影片、精密位置或動作指令。
先停下來：先確認關鍵動作進入片段，再用標註資料比較任務頭與取樣設定；回原片判讀不確定情況。
完成本主題
☆ 收藏
從案例理解 V-JEPA
從必要原理、具體反例到工作取捨

你有許多未標動作的工業影片，想先學可以交給不同工作任務使用的表示，而不是要求模型逐像素重畫每段影片。 下列圖像、特徵及回答為教學示意，不是模型實測。

第 01 張 · 看案例，再做判斷

V-JEPA：預測被遮住部分的特徵

你有許多未標動作的工業影片，想先學可以交給不同工作任務使用的表示，而不是要求模型逐像素重畫每段影片。

V-JEPA：預測被遮住部分的特徵
放大完整圖
為什麼需要這個設計？

本課講2024原版V-JEPA：上下文編碼器看未被遮住的影片部分，預測器估計被遮區的特徵，對齊另一個目標編碼器從完整影片提供的對應特徵。訓練比較發生在表示空間，不是把缺少的像素或未來影片重畫出來。

讀完要能說清楚的三個要點
目標也是編碼特徵

目標編碼器的權重隨上下文編碼器緩慢更新；目標不是人工動作標籤，也不是預測器提前拿到完整影格答案。

表示與判斷分工

學到的編碼器可交出影片特徵；本例用有標註資料訓練任務頭，才產生夾取階段候選。原版不等於機器人控制模型。

交付與接手

影片表示與下游任務候選。本課不把特徵差直接命名為缺陷機率，也不宣稱輸出未來影片、精密位置或動作指令。

想深入，再看這張圖的完整工程說明
V-JEPA：預測被遮住部分的特徵

原始問題：你有許多未標動作的工業影片，想先學可以交給不同工作任務使用的表示，而不是要求模型逐像素重畫每段影片。

本課講2024原版V-JEPA：上下文編碼器看未被遮住的影片部分，預測器估計被遮區的特徵，對齊另一個目標編碼器從完整影片提供的對應特徵。訓練比較發生在表示空間，不是把缺少的像素或未來影片重畫出來。

目標也是編碼特徵

目標編碼器的權重隨上下文編碼器緩慢更新；目標不是人工動作標籤，也不是預測器提前拿到完整影格答案。

表示與判斷分工

學到的編碼器可交出影片特徵；本例用有標註資料訓練任務頭，才產生夾取階段候選。原版不等於機器人控制模型。

交付與接手

影片表示與下游任務候選。本課不把特徵差直接命名為缺陷機率，也不宣稱輸出未來影片、精密位置或動作指令。

第 02 張 · 看案例，再做判斷

關鍵動作沒拍進去，影片特徵也不夠

觀察哪個條件改變，讓原本的結果不夠可靠？

關鍵動作沒拍進去，影片特徵也不夠
放大完整圖
看哪裡

若輸入時間窗漏掉短暫滑落，預測特徵也可能保留不了工作需要的事件；預訓練可遷移性不能取代域內驗證。

接著怎麼做

先確認關鍵動作進入片段，再用標註資料比較任務頭與取樣設定；回原片判讀不確定情況。

想深入，再看這張圖的完整工程說明
關鍵動作沒拍進去，影片特徵也不夠

原始問題：觀察哪個條件改變，讓原本的結果不夠可靠？

若輸入時間窗漏掉短暫滑落，預測特徵也可能保留不了工作需要的事件；預訓練可遷移性不能取代域內驗證。

看哪裡

若輸入時間窗漏掉短暫滑落，預測特徵也可能保留不了工作需要的事件；預訓練可遷移性不能取代域內驗證。

接著怎麼做

先確認關鍵動作進入片段，再用標註資料比較任務頭與取樣設定；回原片判讀不確定情況。

第 03 張 · 看案例，再做判斷

記住序列、補像素、預測特徵：學習方式不同

相同工作需求下，資料、輸出與成本如何取捨？

記住序列、補像素、預測特徵：學習方式不同
放大完整圖
怎麼選

VideoMAE在預訓練補像素，V-JEPA預測特徵；兩者是否有利要看同一工作任務與成本。若少量時間狀態已足夠，也可先比較簡單規則或ConvLSTM。

資料與完整成本

可用2024原版相容預訓練權重起步，仍需下游標註與測試；換場景、視角或動作後重驗特徵與任務頭。比較凍結或微調方案時記錄調整預算，不混入V-JEPA2／2.1能力。

想深入，再看這張圖的完整工程說明
記住序列、補像素、預測特徵：學習方式不同

原始問題：相同工作需求下，資料、輸出與成本如何取捨？

VideoMAE在預訓練補像素，V-JEPA預測特徵；兩者是否有利要看同一工作任務與成本。若少量時間狀態已足夠，也可先比較簡單規則或ConvLSTM。

怎麼選

VideoMAE在預訓練補像素，V-JEPA預測特徵；兩者是否有利要看同一工作任務與成本。若少量時間狀態已足夠，也可先比較簡單規則或ConvLSTM。

資料與完整成本

可用2024原版相容預訓練權重起步，仍需下游標註與測試；換場景、視角或動作後重驗特徵與任務頭。比較凍結或微調方案時記錄調整預算，不混入V-JEPA2／2.1能力。

換情境試著判斷

換成要預測未來影片的需求

同事看到V-JEPA的預測二字，想直接輸出下一秒的影像給操作員。

本課原版能直接接成這個功能嗎？

看解釋與可接受的取捨

不能；這裡預測的是被遮區的特徵，不是像素影片。若要未來影像須另選生成／預測方案並驗證；若要判目前動作，則可用原版特徵搭配任務頭。

需要時查看基本名詞與模型背景
自監督影片特徵預測
目標也是編碼特徵：目標編碼器的權重隨上下文編碼器緩慢更新；目標不是人工動作標籤，也不是預測器提前拿到完整影格答案。
表示與判斷分工：學到的編碼器可交出影片特徵；本例用有標註資料訓練任務頭，才產生夾取階段候選。原版不等於機器人控制模型。
自監督影片特徵預測

本課講2024原版V-JEPA：上下文編碼器看未被遮住的影片部分，預測器估計被遮區的特徵，對齊另一個目標編碼器從完整影片提供的對應特徵。訓練比較發生在表示空間，不是把缺少的像素或未來影片重畫出來。

若輸入時間窗漏掉短暫滑落，預測特徵也可能保留不了工作需要的事件；預訓練可遷移性不能取代域內驗證。

工程參考：展開文字因果鏈與設定核對點不重複主圖；只核對每步要鎖定的設定。

模型專屬因果鏈

實作時逐步核對

主圖已在上方第一遍。這裡只核對每一步要鎖定的設定與為什麼，不再重貼同一張圖。

1
可見影片內容

目標編碼器的權重隨上下文編碼器緩慢更新；目標不是人工動作標籤，也不是預測器提前拿到完整影格答案。

因為：由本課核心機制連接工作用途

設定核對：WI028-1

2
預測特徵對齊目標

學到的編碼器可交出影片特徵；本例用有標註資料訓練任務頭，才產生夾取階段候選。原版不等於機器人控制模型。

因為：由本課核心機制連接工作用途

設定核對：WI028-2

3
特徵接工作任務

影片表示與下游任務候選。本課不把特徵差直接命名為缺陷機率，也不宣稱輸出未來影片、精密位置或動作指令。

因為：由本課核心機制連接工作用途

設定核對：WI028-3

4
工作核對與接手

在相同影片／標註預算下比較任務錯誤、短事件漏報、片段等待、完整推論成本及換產品後維護量；不以特徵圖漂亮排名。

因為：由本課核心機制連接工作用途

設定核對：WI028-4

想深入：展開失效案例、模型比較與小規模試做（POC）
初學者可先跳過；準備實作或驗收時再逐項核對。

失效案例、選型與小規模試做

什麼情況不能相信它？

這一區保留完整工程條件，供準備實作或驗收的人逐項核對。

本課具體反例

可能看到：若輸入時間窗漏掉短暫滑落，預測特徵也可能保留不了工作需要的事件；預訓練可遷移性不能取代域內驗證。

正確下一步：先確認關鍵動作進入片段，再用標註資料比較任務頭與取樣設定；回原片判讀不確定情況。

更換產品、模型或輸入設定

可能看到：可用2024原版相容預訓練權重起步，仍需下游標註與測試；換場景、視角或動作後重驗特徵與任務頭。比較凍結或微調方案時記錄調整預算，不混入V-JEPA2／2.1能力。

正確下一步：先定義工作要判斷的動作與時間範圍，準備完整作業影片、少量下游標註與獨立測試，再固定原版權重與取樣。

把示意當成實際推論

可能看到：本課特徵、排序、回答及圖像僅用於解釋，不支持現場準確率或耗時結論。

正確下一步：在相同影片／標註預算下比較任務錯誤、短事件漏報、片段等待、完整推論成本及換產品後維護量；不以特徵圖漂亮排名。

相鄰方法比較

記住序列、補像素、預測特徵：學習方式不同
方法	所需資料與方法差異	輸出與維護責任
ConvLSTM應用	連續片段、任務標註；訓練空間記憶與任務頭	依任務定義的序列輸出；管理狀態／重置
VideoMAE原版	像素重建預訓練；可用現有權重，需下游標註	影片表示＋任務頭；重建不是現場判定
V-JEPA 2024原版	特徵預測預訓練；可用現有權重，需下游標註	影片表示＋任務頭；不是未來影片生成

VideoMAE在預訓練補像素，V-JEPA預測特徵；兩者是否有利要看同一工作任務與成本。若少量時間狀態已足夠，也可先比較簡單規則或ConvLSTM。 可用2024原版相容預訓練權重起步，仍需下游標註與測試；換場景、視角或動作後重驗特徵與任務頭。比較凍結或微調方案時記錄調整預算，不混入V-JEPA2／2.1能力。

工程第一步

以真實工作資料做小規模比較
先定義工作要判斷的動作與時間範圍，準備完整作業影片、少量下游標註與獨立測試，再固定原版權重與取樣。
可用2024原版相容預訓練權重起步，仍需下游標註與測試；換場景、視角或動作後重驗特徵與任務頭。比較凍結或微調方案時記錄調整預算，不混入V-JEPA2／2.1能力。
在相同影片／標註預算下比較任務錯誤、短事件漏報、片段等待、完整推論成本及換產品後維護量；不以特徵圖漂亮排名。
目前做法

既有流程或最簡單可行方法

要量什麼

在相同影片／標註預算下比較任務錯誤、短事件漏報、片段等待、完整推論成本及換產品後維護量；不以特徵圖漂亮排名。

反例測試

若輸入時間窗漏掉短暫滑落，預測特徵也可能保留不了工作需要的事件；預訓練可遷移性不能取代域內驗證。

誰確認

實作與影像核對負責人

先停下來的訊號：先確認關鍵動作進入片段，再用標註資料比較任務頭與取樣設定；回原片判讀不確定情況。

讀完請自己講回來

它改變什麼？何時選另一種做法？
看本頁練習的核對說明

本課講2024原版V-JEPA：上下文編碼器看未被遮住的影片部分，預測器估計被遮區的特徵，對齊另一個目標編碼器從完整影片提供的對應特徵。訓練比較發生在表示空間，不是把缺少的像素或未來影片重畫出來。 VideoMAE在預訓練補像素，V-JEPA預測特徵；兩者是否有利要看同一工作任務與成本。若少量時間狀態已足夠，也可先比較簡單規則或ConvLSTM。

換一個現場再推理

換成要預測未來影片的需求

情境：同事看到V-JEPA的預測二字，想直接輸出下一秒的影像給操作員。

請先回答：本課原版能直接接成這個功能嗎？

核對這個遷移題

不能；這裡預測的是被遮區的特徵，不是像素影片。若要未來影像須另選生成／預測方案並驗證；若要判目前動作，則可用原版特徵搭配任務頭。

內容依據：展開這頁主張的 model.md 可追溯範圍

roadmap-model-selection/video-temporal/v-jepa/model.md ↗

WI-033：工程層四圖與手機比較修正；首讀有效內容保留。原生圖已審，整頁與使用者審閱另記。

beginner_path.visuals[conveyor-context-workpiece]：模型定位 / architecture_path
beginner_path.visuals[context-target-mask]：architecture_path
beginner_path.visuals[dual-encoder-latent]：representation_or_score
beginner_path.visuals[latent-not-pixel]：failure_boundary
beginner_path.visuals[downstream-event-gate]：selection_gate / evidence_bundle
promise：模型定位 / representation_or_score
mechanism_steps：architecture_path / representation_or_score
boundary_cases：failure_boundary / selection_gate
comparison + selection：selection_gate
poc + acceptance：cost_and_operating_point / evidence_bundle
transfer_check：failure_boundary / selection_gate
beginner_path.visuals[0] + scenario + mental_model：roadmap-model-selection/video-temporal/v-jepa/model.md
micro_example + transfer_check + selection：roadmap-model-selection/video-temporal/v-jepa/model.md
beginner_path + core + selection + transfer：https://arxiv.org/abs/2404.08471
工程參考：展開四張工程圖與完整節點
從輸入、機制、交付到限制，核對實作與驗證方式。

本課講2024原版V-JEPA：上下文編碼器看未被遮住的影片部分，預測器估計被遮區的特徵，對齊另一個目標編碼器從完整影片提供的對應特徵。訓練比較發生在表示空間，不是把缺少的像素或未來影片重畫出來。

01
先看它交出什麼
02
再看它怎麼做
03
上線前先固定條件
04
選它前先看風險
01

先看它交出什麼

V-JEPA：特徵預測之後再學任務

自監督訊號不會自動變成事件標籤。

正式教學插圖 1/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

自監督訊號不會自動變成事件標籤。
先看哪裡

先看未標註影片學特徵預測，再核對標註夾取影片教任務頭，最後看新片特徵交任務判斷。

框與框的關係

原版2024 V-JEPA預測表徵而非像素；下游可固定骨幹學注意力探針，或另選微調策略須記清。目標編碼器和預測器不是部署任務頭。

本圖只要帶走

自監督訊號不會自動變成事件標籤。

示意數值：作者給定局部算例，非模型實測或建議參數。
同位置：比較保持相同影像座標與時間身份，不能任意換工件。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
原版2024 V-JEPA預測表徵而非像素；下游可固定骨幹學注意力探針，或另選微調策略須記清。目標編碼器和預測器不是部署任務頭。
先看未標註影片學特徵預測，再核對標註夾取影片教任務頭，最後看新片特徵交任務判斷。
技術上不可誤讀
若輸入時間窗漏掉短暫滑落，預測特徵也可能保留不了工作需要的事件；預訓練可遷移性不能取代域內驗證。

完整節點

原始工程節點
1
未標註影片學特徵預測

未標註影片學特徵預測；請對照圖內輸入、變化與結果。

2
標註夾取影片教任務頭

標註夾取影片教任務頭；請對照圖內輸入、變化與結果。

3
新片特徵交任務判斷

新片特徵交任務判斷；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看未標註影片學特徵預測，再核對標註夾取影片教任務頭，最後看新片特徵交任務判斷。

怎麼理解

原版2024 V-JEPA預測表徵而非像素；下游可固定骨幹學注意力探針，或另選微調策略須記清。目標編碼器和預測器不是部署任務頭。

不要誤用

先確認關鍵動作進入片段，再用標註資料比較任務頭與取樣設定；回原片判讀不確定情況。

繼續看第 2 張：再看它怎麼做 ↓

02

再看它怎麼做

V-JEPA：預測同位置的目標特徵

目標特徵提供訓練訊號，部署另學任務。

正式教學插圖 2/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

目標特徵提供訓練訊號，部署另學任務。
先看哪裡

先看可見區編碼並指定遮蔽位置，再核對完整影片產生停止梯度目標，最後看同遮蔽位置比較特徵差。

框與框的關係

2024原版V-JEPA；上下文編碼器讀可見tokens，預測器加遮蔽位置資訊預測目標tokens。完整影片只進目標編碼器，目標stop-gradient，其權重由上下文EMA更新。L1比較同位置特徵。作者兩維算例預測[.2,.8]目標[.4,.7]平均絕對差.15，不是異常分數。 r01實看修正：目標格與EMA文字分離。

本圖只要帶走

目標特徵提供訓練訊號，部署另學任務。

示意數值：作者給定局部算例，非模型實測或建議參數。
同位置：比較保持相同影像座標與時間身份，不能任意換工件。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
2024原版V-JEPA；上下文編碼器讀可見tokens，預測器加遮蔽位置資訊預測目標tokens。完整影片只進目標編碼器，目標stop-gradient，其權重由上下文EMA更新。L1比較同位置特徵。作者兩維算例預測[.2,.8]目標[.4,.7]平均絕對差.15，不是異常分數。 r01實看修正：目標格與EMA文字分離。
先看可見區編碼並指定遮蔽位置，再核對完整影片產生停止梯度目標，最後看同遮蔽位置比較特徵差。
技術上不可誤讀
若輸入時間窗漏掉短暫滑落，預測特徵也可能保留不了工作需要的事件；預訓練可遷移性不能取代域內驗證。

完整節點

原始工程節點
1
可見區編碼並指定遮蔽位置

可見區編碼並指定遮蔽位置；請對照圖內輸入、變化與結果。

2
完整影片產生停止梯度目標

完整影片產生停止梯度目標；請對照圖內輸入、變化與結果。

3
同遮蔽位置比較特徵差

同遮蔽位置比較特徵差；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看可見區編碼並指定遮蔽位置，再核對完整影片產生停止梯度目標，最後看同遮蔽位置比較特徵差。

怎麼理解

2024原版V-JEPA；上下文編碼器讀可見tokens，預測器加遮蔽位置資訊預測目標tokens。完整影片只進目標編碼器，目標stop-gradient，其權重由上下文EMA更新。L1比較同位置特徵。作者兩維算例預測[.2,.8]目標[.4,.7]平均絕對差.15，不是異常分數。 r01實看修正：目標格與EMA文字分離。

不要誤用

先確認關鍵動作進入片段，再用標註資料比較任務頭與取樣設定；回原片判讀不確定情況。

繼續看第 3 張：上線前先固定條件 ↓

03

上線前先固定條件

V-JEPA：保存取樣與下游模式

骨幹、讀出頭與取樣設定是一組。

正式教學插圖 3/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

骨幹、讀出頭與取樣設定是一組。
先看哪裡

先看固定時空取樣，再核對固定骨幹或微調需寫明，最後看片長、事件和延遲一起驗。

框與框的關係

保存2024骨幹checkpoint、影格取樣、裁切與下游讀出頭。EMA是預訓練目標權重更新，不是現場每片自動更新模型；原版不宣稱未來影像生成。 r01實看修正：时间刻度及標籤各自定位。

本圖只要帶走

骨幹、讀出頭與取樣設定是一組。

示意數值：作者給定局部算例，非模型實測或建議參數。
同位置：比較保持相同影像座標與時間身份，不能任意換工件。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
保存2024骨幹checkpoint、影格取樣、裁切與下游讀出頭。EMA是預訓練目標權重更新，不是現場每片自動更新模型；原版不宣稱未來影像生成。 r01實看修正：时间刻度及標籤各自定位。
先看固定時空取樣，再核對固定骨幹或微調需寫明，最後看片長、事件和延遲一起驗。
技術上不可誤讀
若輸入時間窗漏掉短暫滑落，預測特徵也可能保留不了工作需要的事件；預訓練可遷移性不能取代域內驗證。

完整節點

原始工程節點
1
固定時空取樣

固定時空取樣；請對照圖內輸入、變化與結果。

2
固定骨幹或微調需寫明

固定骨幹或微調需寫明；請對照圖內輸入、變化與結果。

3
片長、事件和延遲一起驗

片長、事件和延遲一起驗；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看固定時空取樣，再核對固定骨幹或微調需寫明，最後看片長、事件和延遲一起驗。

怎麼理解

保存2024骨幹checkpoint、影格取樣、裁切與下游讀出頭。EMA是預訓練目標權重更新，不是現場每片自動更新模型；原版不宣稱未來影像生成。 r01實看修正：时间刻度及標籤各自定位。

不要誤用

先確認關鍵動作進入片段，再用標註資料比較任務頭與取樣設定；回原片判讀不確定情況。

繼續看第 4 張：選它前先看風險 ↓

04

選它前先看風險

V-JEPA與VideoMAE：監督目標不同

同影片比較任務成果，不能比兩種損失值。

正式教學插圖 4/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

同影片比較任務成果，不能比兩種損失值。
先看哪裡

先看同一夾取影片與同一標籤，再核對VideoMAE對像素平方差，最後看V-JEPA對目標特徵L1差。

框與框的關係

兩者預訓練目標及尺度不同，L1=.15不能對MSE=1排名。固定資料、讀出方式、硬體與事件指標比較；沒有本輪共同實測。

本圖只要帶走

同影片比較任務成果，不能比兩種損失值。

示意數值：作者給定局部算例，非模型實測或建議參數。
同位置：比較保持相同影像座標與時間身份，不能任意換工件。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
兩者預訓練目標及尺度不同，L1=.15不能對MSE=1排名。固定資料、讀出方式、硬體與事件指標比較；沒有本輪共同實測。
先看同一夾取影片與同一標籤，再核對VideoMAE對像素平方差，最後看V-JEPA對目標特徵L1差。
技術上不可誤讀
若輸入時間窗漏掉短暫滑落，預測特徵也可能保留不了工作需要的事件；預訓練可遷移性不能取代域內驗證。

完整節點

原始工程節點
1
同一夾取影片與同一標籤

同一夾取影片與同一標籤；請對照圖內輸入、變化與結果。

2
VideoMAE對像素平方差

VideoMAE對像素平方差；請對照圖內輸入、變化與結果。

3
V-JEPA對目標特徵L1差

V-JEPA對目標特徵L1差；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看同一夾取影片與同一標籤，再核對VideoMAE對像素平方差，最後看V-JEPA對目標特徵L1差。

怎麼理解

兩者預訓練目標及尺度不同，L1=.15不能對MSE=1排名。固定資料、讀出方式、硬體與事件指標比較；沒有本輪共同實測。

不要誤用

先確認關鍵動作進入片段，再用標註資料比較任務頭與取樣設定；回原片判讀不確定情況。

工程師操作卡

把 V-JEPA 放進小規模試做（POC）前，先回答這五個問題。

先讀白話版就能開始溝通；需要實作、驗證或交接時，再展開下方技術契約。

1
它會交出什麼？

影片表示與下游任務候選。本課不把特徵差直接命名為缺陷機率，也不宣稱輸出未來影片、精密位置或動作指令。

2
開始先做什麼？

先定義工作要判斷的動作與時間範圍，準備完整作業影片、少量下游標註與獨立測試，再固定原版權重與取樣。

3
開始前要寫下來的四件事

固定畫面範圍、資料來源、模型版本和驗收方式；詳細項目可在下方展開。

4
怎樣才算真的有用？

在相同影片／標註預算下比較任務錯誤、短事件漏報、片段等待、完整推論成本及換產品後維護量；不以特徵圖漂亮排名。

5
碰到什麼先停下來？

先確認關鍵動作進入片段，再用標註資料比較任務頭與取樣設定；回原片判讀不確定情況。

工程師需要時，再展開原始技術契約

原始建置項目

這些設定要一併保存

輸入與版本：先定義工作要判斷的動作與時間範圍，準備完整作業影片、少量下游標註與獨立測試，再固定原版權重與取樣。；關鍵機制：本課講2024原版V-JEPA：上下文編碼器看未被遮住的影片部分，預測器估計被遮區的特徵，對齊另一個目標編碼器從完整影片提供的對應特徵。訓練比較發生在表示空間，不是把缺少的像素或未來影片重畫出來。；獨立驗證：在相同影片／標註預算下比較任務錯誤、短事件漏報、片段等待、完整推論成本及換產品後維護量；不以特徵圖漂亮排名。；失敗處理：先確認關鍵動作進入片段，再用標註資料比較任務頭與取樣設定；回原片判讀不確定情況。

原始選型條件：VideoMAE在預訓練補像素，V-JEPA預測特徵；兩者是否有利要看同一工作任務與成本。若少量時間狀態已足夠，也可先比較簡單規則或ConvLSTM。 可用2024原版相容預訓練權重起步，仍需下游標註與測試；換場景、視角或動作後重驗特徵與任務頭。比較凍結或微調方案時記錄調整預算，不混入V-JEPA2／2.1能力。

原始失效邊界

下列情況不可忽略

若輸入時間窗漏掉短暫滑落，預測特徵也可能保留不了工作需要的事件；預訓練可遷移性不能取代域內驗證。

工程結論
影片表示與下游任務候選。本課不把特徵差直接命名為缺陷機率，也不宣稱輸出未來影片、精密位置或動作指令。
把這張操作卡帶入 POC 畫布 →
查看 model.md ↗
查看圖檔 manifest ↗

相鄰比較

下一個可比較的模型

比較前，先把「看哪段畫面、用哪些資料、交出什麼、多久完成、誰覆核」固定；再到家族頁看共同條件與差異。

Frame Difference →
Background Subtraction →
Lucas–Kanade →
RAFT →
查看家族比較 →
← VideoMAE
DINOv2 →