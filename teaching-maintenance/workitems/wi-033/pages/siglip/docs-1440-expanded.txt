首頁
/
Foundation Vision、語意檢索與 VLM
/
SigLIP
✦ Foundation Vision、語意檢索與 VLM
先理解，再看工程圖
SigLIP

你也需要用文字搜尋工件，但想了解 SigLIP 和 CLIP 的差異究竟發生在哪一步，而不是只比較模型名稱。

你會拿到：交出圖文表示及配合分數／排序，供語意檢索或分類；本課不把 sigmoid 分數當作缺陷機率或位置。
先停下來：先補齊需求相關的候選與未知測試樣本，再用這組權重重新驗證分數與拒答方式；不要直接搬用 CLIP 的門檻。
完成本主題
☆ 收藏
從案例理解 SigLIP
從必要原理、具體反例到工作取捨

你也需要用文字搜尋工件，但想了解 SigLIP 和 CLIP 的差異究竟發生在哪一步，而不是只比較模型名稱。 下列圖像、特徵及回答為教學示意，不是模型實測。

第 01 張 · 看案例，再做判斷

SigLIP：逐對學會圖文是否配合

你也需要用文字搜尋工件，但想了解 SigLIP 和 CLIP 的差異究竟發生在哪一步，而不是只比較模型名稱。

SigLIP：逐對學會圖文是否配合
放大完整圖
為什麼需要這個設計？

原始 SigLIP 同樣學影像和文字的共同表示，但在預訓練時，對每一個配對或不配對的圖文組合分別計算 sigmoid 損失，無需用全批次相似度做 softmax 正規化。這些學習訊號仍共同更新編碼器；部署時再比較新影像與候選文字的配合程度。

讀完要能說清楚的三個要點
逐對計算，不是逐對一個模型

同一張支架影像和「金屬支架」是配對樣本，和「齒輪」是不配對樣本；兩者的訓練訊號一起更新共享編碼器。原始方法仍使用負配對，並非只學正例。

分數不能直接借用別人的門檻

不必讓每組候選分數加總為一，不代表分數就是經校準的現場正確率。權重、文字與前處理改變，分數尺度和錯誤也可能改變。

交付與接手

交出圖文表示及配合分數／排序，供語意檢索或分類；本課不把 sigmoid 分數當作缺陷機率或位置。

想深入，再看這張圖的完整工程說明
SigLIP：逐對學會圖文是否配合

原始問題：你也需要用文字搜尋工件，但想了解 SigLIP 和 CLIP 的差異究竟發生在哪一步，而不是只比較模型名稱。

原始 SigLIP 同樣學影像和文字的共同表示，但在預訓練時，對每一個配對或不配對的圖文組合分別計算 sigmoid 損失，無需用全批次相似度做 softmax 正規化。這些學習訊號仍共同更新編碼器；部署時再比較新影像與候選文字的配合程度。

逐對計算，不是逐對一個模型

同一張支架影像和「金屬支架」是配對樣本，和「齒輪」是不配對樣本；兩者的訓練訊號一起更新共享編碼器。原始方法仍使用負配對，並非只學正例。

分數不能直接借用別人的門檻

不必讓每組候選分數加總為一，不代表分數就是經校準的現場正確率。權重、文字與前處理改變，分數尺度和錯誤也可能改變。

交付與接手

交出圖文表示及配合分數／排序，供語意檢索或分類；本課不把 sigmoid 分數當作缺陷機率或位置。

第 02 張 · 看案例，再做判斷

候選沒有正確答案，第一名也可能錯

觀察哪個條件改變，讓原本的結果不夠可靠？

候選沒有正確答案，第一名也可能錯
放大完整圖
看哪裡

SigLIP 也會受到候選內容限制：齒輪影像面對只有支架、軸承的集合，仍可能選錯。逐對訓練目標不會替你補齊遺漏詞彙，也不保證知道何時拒答。

接著怎麼做

先補齊需求相關的候選與未知測試樣本，再用這組權重重新驗證分數與拒答方式；不要直接搬用 CLIP 的門檻。

想深入，再看這張圖的完整工程說明
候選沒有正確答案，第一名也可能錯

原始問題：觀察哪個條件改變，讓原本的結果不夠可靠？

SigLIP 也會受到候選內容限制：齒輪影像面對只有支架、軸承的集合，仍可能選錯。逐對訓練目標不會替你補齊遺漏詞彙，也不保證知道何時拒答。

看哪裡

SigLIP 也會受到候選內容限制：齒輪影像面對只有支架、軸承的集合，仍可能選錯。逐對訓練目標不會替你補齊遺漏詞彙，也不保證知道何時拒答。

接著怎麼做

先補齊需求相關的候選與未知測試樣本，再用這組權重重新驗證分數與拒答方式；不要直接搬用 CLIP 的門檻。

第 03 張 · 看案例，再做判斷

CLIP 與 SigLIP：訓練不同，選型要同題

相同工作需求下，資料、輸出與成本如何取捨？

CLIP 與 SigLIP：訓練不同，選型要同題
放大完整圖
怎麼選

與 CLIP 比較時，先認清批次 softmax 和逐對 sigmoid 是訓練差異；再固定相同工件、候選文字與測試條件，比實際排序品質與成本。本課介紹原始 SigLIP，不把 SigLIP 2 的新增能力混在一起。

資料與完整成本

可先用預訓練權重和文字探索，仍要準備相關性／類別驗證資料；換產品需補詞彙、難例及未知圖。各模型使用正確的 tokenizer／前處理，記錄快取、輸入解析度、完整耗時及人工覆核量，不能只比一個絕對分數。

想深入，再看這張圖的完整工程說明
CLIP 與 SigLIP：訓練不同，選型要同題

原始問題：相同工作需求下，資料、輸出與成本如何取捨？

與 CLIP 比較時，先認清批次 softmax 和逐對 sigmoid 是訓練差異；再固定相同工件、候選文字與測試條件，比實際排序品質與成本。本課介紹原始 SigLIP，不把 SigLIP 2 的新增能力混在一起。

怎麼選

與 CLIP 比較時，先認清批次 softmax 和逐對 sigmoid 是訓練差異；再固定相同工件、候選文字與測試條件，比實際排序品質與成本。本課介紹原始 SigLIP，不把 SigLIP 2 的新增能力混在一起。

資料與完整成本

可先用預訓練權重和文字探索，仍要準備相關性／類別驗證資料；換產品需補詞彙、難例及未知圖。各模型使用正確的 tokenizer／前處理，記錄快取、輸入解析度、完整耗時及人工覆核量，不能只比一個絕對分數。

換情境試著判斷

分數較高能否直接勝出

同一批圖上 SigLIP 的分數普遍高於 CLIP，但兩者最高候選大致相同。

可以沿用 CLIP 門檻並宣稱 SigLIP 更準嗎？

看解釋與可接受的取捨

不行。兩者的分數尺度與轉換不同；應在固定留出集各自驗證拒答／門檻，再比實際排序錯誤及成本。如果結果相當，維護和延遲也可以決定選擇。

需要時查看基本名詞與模型背景
圖文雙編碼器
逐對計算，不是逐對一個模型：同一張支架影像和「金屬支架」是配對樣本，和「齒輪」是不配對樣本；兩者的訓練訊號一起更新共享編碼器。原始方法仍使用負配對，並非只學正例。
分數不能直接借用別人的門檻：不必讓每組候選分數加總為一，不代表分數就是經校準的現場正確率。權重、文字與前處理改變，分數尺度和錯誤也可能改變。
圖文雙編碼器

原始 SigLIP 同樣學影像和文字的共同表示，但在預訓練時，對每一個配對或不配對的圖文組合分別計算 sigmoid 損失，無需用全批次相似度做 softmax 正規化。這些學習訊號仍共同更新編碼器；部署時再比較新影像與候選文字的配合程度。

SigLIP 也會受到候選內容限制：齒輪影像面對只有支架、軸承的集合，仍可能選錯。逐對訓練目標不會替你補齊遺漏詞彙，也不保證知道何時拒答。

工程參考：展開文字因果鏈與設定核對點不重複主圖；只核對每步要鎖定的設定。

模型專屬因果鏈

實作時逐步核對

主圖已在上方第一遍。這裡只核對每一步要鎖定的設定與為什麼，不再重貼同一張圖。

1
準備圖文配對

你也需要用文字搜尋工件，但想了解 SigLIP 和 CLIP 的差異究竟發生在哪一步，而不是只比較模型名稱。

因為：由本課核心機制連接工作用途

設定核對：WI027-1

2
每對分別計算目標

同一張支架影像和「金屬支架」是配對樣本，和「齒輪」是不配對樣本；兩者的訓練訊號一起更新共享編碼器。原始方法仍使用負配對，並非只學正例。

因為：由本課核心機制連接工作用途

設定核對：WI027-2

3
共同更新兩個編碼器

不必讓每組候選分數加總為一，不代表分數就是經校準的現場正確率。權重、文字與前處理改變，分數尺度和錯誤也可能改變。

因為：由本課核心機制連接工作用途

設定核對：WI027-3

4
部署時再比圖文

交出圖文表示及配合分數／排序，供語意檢索或分類；本課不把 sigmoid 分數當作缺陷機率或位置。

因為：由本課核心機制連接工作用途

設定核對：WI027-4

想深入：展開失效案例、模型比較與小規模試做（POC）
初學者可先跳過；準備實作或驗收時再逐項核對。

失效案例、選型與小規模試做

什麼情況不能相信它？

這一區保留完整工程條件，供準備實作或驗收的人逐項核對。

本課具體反例

可能看到：SigLIP 也會受到候選內容限制：齒輪影像面對只有支架、軸承的集合，仍可能選錯。逐對訓練目標不會替你補齊遺漏詞彙，也不保證知道何時拒答。

正確下一步：先補齊需求相關的候選與未知測試樣本，再用這組權重重新驗證分數與拒答方式；不要直接搬用 CLIP 的門檻。

更換產品、模型或輸入設定

可能看到：可先用預訓練權重和文字探索，仍要準備相關性／類別驗證資料；換產品需補詞彙、難例及未知圖。各模型使用正確的 tokenizer／前處理，記錄快取、輸入解析度、完整耗時及人工覆核量，不能只比一個絕對分數。

正確下一步：選定原始 SigLIP 權重及配套前處理，沿用一組固定的工件影像與候選文字，同時保留 CLIP 基準和未知樣本。

把示意當成實際推論

可能看到：本課特徵、排序、回答及圖像僅用於解釋，不支持現場準確率或耗時結論。

正確下一步：同條件看候選命中、錯誤拒答與未知誤選，另量影像處理和文字快取後的完整時間；分數高低本身不是選型結果。

相鄰方法比較

CLIP 與 SigLIP：訓練不同，選型要同題
方法	所需資料與方法差異	輸出與維護責任
CLIP	批次對比預訓練；需候選文字及域內測試	圖文相似度與候選排序
原始 SigLIP	逐對 sigmoid 預訓練；含負配對，需獨立驗證門檻	圖文配合分數與候選排序
專用分類器基準	需要目標類別標註與獨立測試；換類別通常需重建或調整	固定類別輸出；若工作不需開放詞彙，可比較錯誤及成本

與 CLIP 比較時，先認清批次 softmax 和逐對 sigmoid 是訓練差異；再固定相同工件、候選文字與測試條件，比實際排序品質與成本。本課介紹原始 SigLIP，不把 SigLIP 2 的新增能力混在一起。 可先用預訓練權重和文字探索，仍要準備相關性／類別驗證資料；換產品需補詞彙、難例及未知圖。各模型使用正確的 tokenizer／前處理，記錄快取、輸入解析度、完整耗時及人工覆核量，不能只比一個絕對分數。

工程第一步

以真實工作資料做小規模比較
選定原始 SigLIP 權重及配套前處理，沿用一組固定的工件影像與候選文字，同時保留 CLIP 基準和未知樣本。
可先用預訓練權重和文字探索，仍要準備相關性／類別驗證資料；換產品需補詞彙、難例及未知圖。各模型使用正確的 tokenizer／前處理，記錄快取、輸入解析度、完整耗時及人工覆核量，不能只比一個絕對分數。
同條件看候選命中、錯誤拒答與未知誤選，另量影像處理和文字快取後的完整時間；分數高低本身不是選型結果。
目前做法

既有流程或最簡單可行方法

要量什麼

同條件看候選命中、錯誤拒答與未知誤選，另量影像處理和文字快取後的完整時間；分數高低本身不是選型結果。

反例測試

SigLIP 也會受到候選內容限制：齒輪影像面對只有支架、軸承的集合，仍可能選錯。逐對訓練目標不會替你補齊遺漏詞彙，也不保證知道何時拒答。

誰確認

實作與影像核對負責人

先停下來的訊號：先補齊需求相關的候選與未知測試樣本，再用這組權重重新驗證分數與拒答方式；不要直接搬用 CLIP 的門檻。

讀完請自己講回來

它改變什麼？何時選另一種做法？
看本頁練習的核對說明

原始 SigLIP 同樣學影像和文字的共同表示，但在預訓練時，對每一個配對或不配對的圖文組合分別計算 sigmoid 損失，無需用全批次相似度做 softmax 正規化。這些學習訊號仍共同更新編碼器；部署時再比較新影像與候選文字的配合程度。 與 CLIP 比較時，先認清批次 softmax 和逐對 sigmoid 是訓練差異；再固定相同工件、候選文字與測試條件，比實際排序品質與成本。本課介紹原始 SigLIP，不把 SigLIP 2 的新增能力混在一起。

換一個現場再推理

分數較高能否直接勝出

情境：同一批圖上 SigLIP 的分數普遍高於 CLIP，但兩者最高候選大致相同。

請先回答：可以沿用 CLIP 門檻並宣稱 SigLIP 更準嗎？

核對這個遷移題

不行。兩者的分數尺度與轉換不同；應在固定留出集各自驗證拒答／門檻，再比實際排序錯誤及成本。如果結果相當，維護和延遲也可以決定選擇。

內容依據：展開這頁主張的 model.md 可追溯範圍

roadmap-model-selection/foundation-vlm-open-world/siglip/model.md ↗

WI-033：工程層四圖與手機比較修正；首讀有效內容保留。原生圖已審，整頁與使用者審閱另記。

beginner_path.visuals[same-ceramic-workpiece]：模型定位 / architecture_path
beginner_path.visuals[pairwise-sigmoid-training]：architecture_path
beginner_path.visuals[deployment-ranking]：representation_or_score
beginner_path.visuals[local-calibration-counterfactual]：failure_boundary
beginner_path.visuals[fair-clip-comparison]：selection_gate / evidence_bundle
promise：模型定位 / representation_or_score
mechanism_steps：architecture_path / representation_or_score
boundary_cases：failure_boundary / selection_gate
comparison + selection：selection_gate
poc + acceptance：cost_and_operating_point / evidence_bundle
transfer_check：failure_boundary / selection_gate
beginner_path.visuals[0] + scenario + mental_model：https://arxiv.org/abs/2303.15343
micro_example + transfer_check + selection：https://arxiv.org/abs/2303.15343
beginner_path + core + selection + transfer：https://arxiv.org/abs/2303.15343
工程參考：展開四張工程圖與完整節點
從輸入、機制、交付到限制，核對實作與驗證方式。

原始 SigLIP 同樣學影像和文字的共同表示，但在預訓練時，對每一個配對或不配對的圖文組合分別計算 sigmoid 損失，無需用全批次相似度做 softmax 正規化。這些學習訊號仍共同更新編碼器；部署時再比較新影像與候選文字的配合程度。

01
先看它交出什麼
02
再看它怎麼做
03
上線前先固定條件
04
選它前先看風險
01

先看它交出什麼

SigLIP：每個圖文配對都給學習訊號

正配對拉近、負配對分開，共同更新編碼器。

正式教學插圖 1/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

正配對拉近、負配對分開，共同更新編碼器。
先看哪裡

先看先知道哪個描述配哪張圖，再核對逐對比較配對與不配對，最後看損失共同更新兩編碼器。

框與框的關係

原始 SigLIP 的圖文編碼器分別產生表示，對正負圖文對計算 sigmoid 損失，梯度共同更新參數。每對有自己的二元目標，不需要 CLIP 式的整批 softmax 正規化；逐對計算不代表每對訓練一個獨立模型。

本圖只要帶走

正配對拉近、負配對分開，共同更新編碼器。

表示：影像／文字經編碼後的資料，不是原始照片。
示意結果：用來解釋流程，不代表本輪模型實測。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
原始 SigLIP 的圖文編碼器分別產生表示，對正負圖文對計算 sigmoid 損失，梯度共同更新參數。每對有自己的二元目標，不需要 CLIP 式的整批 softmax 正規化；逐對計算不代表每對訓練一個獨立模型。
先看先知道哪個描述配哪張圖，再核對逐對比較配對與不配對，最後看損失共同更新兩編碼器。
技術上不可誤讀
SigLIP 也會受到候選內容限制：齒輪影像面對只有支架、軸承的集合，仍可能選錯。逐對訓練目標不會替你補齊遺漏詞彙，也不保證知道何時拒答。

完整節點

原始工程節點
1
先知道哪個描述配哪張圖

先知道哪個描述配哪張圖；請對照圖內輸入、變化與結果。

2
逐對比較配對與不配對

逐對比較配對與不配對；請對照圖內輸入、變化與結果。

3
損失共同更新兩編碼器

損失共同更新兩編碼器；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看先知道哪個描述配哪張圖，再核對逐對比較配對與不配對，最後看損失共同更新兩編碼器。

怎麼理解

原始 SigLIP 的圖文編碼器分別產生表示，對正負圖文對計算 sigmoid 損失，梯度共同更新參數。每對有自己的二元目標，不需要 CLIP 式的整批 softmax 正規化；逐對計算不代表每對訓練一個獨立模型。

不要誤用

先補齊需求相關的候選與未知測試樣本，再用這組權重重新驗證分數與拒答方式；不要直接搬用 CLIP 的門檻。

繼續看第 2 張：再看它怎麼做 ↓

02

再看它怎麼做

SigLIP：雙路編碼，逐對計算損失

改變的是配對訓練目標，兩個編碼器仍並行。

正式教學插圖 2/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

改變的是配對訓練目標，兩個編碼器仍並行。
先看哪裡

先看影像與文字分別進模型，再核對每一對依正負標籤學習，最後看全部配對訊號一起更新。

框與框的關係

圖文表示的內積經可學習尺度與偏移後，以正／負標籤計算 sigmoid 損失。圖中高低分僅解釋學習方向，不是模型機率校正結果；部署時不需提供正負訓練標籤。

本圖只要帶走

改變的是配對訓練目標，兩個編碼器仍並行。

表示：影像／文字經編碼後的資料，不是原始照片。
示意結果：用來解釋流程，不代表本輪模型實測。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
圖文表示的內積經可學習尺度與偏移後，以正／負標籤計算 sigmoid 損失。圖中高低分僅解釋學習方向，不是模型機率校正結果；部署時不需提供正負訓練標籤。
先看影像與文字分別進模型，再核對每一對依正負標籤學習，最後看全部配對訊號一起更新。
技術上不可誤讀
SigLIP 也會受到候選內容限制：齒輪影像面對只有支架、軸承的集合，仍可能選錯。逐對訓練目標不會替你補齊遺漏詞彙，也不保證知道何時拒答。

完整節點

原始工程節點
1
影像與文字分別進模型

影像與文字分別進模型；請對照圖內輸入、變化與結果。

2
每一對依正負標籤學習

每一對依正負標籤學習；請對照圖內輸入、變化與結果。

3
全部配對訊號一起更新

全部配對訊號一起更新；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看影像與文字分別進模型，再核對每一對依正負標籤學習，最後看全部配對訊號一起更新。

怎麼理解

圖文表示的內積經可學習尺度與偏移後，以正／負標籤計算 sigmoid 損失。圖中高低分僅解釋學習方向，不是模型機率校正結果；部署時不需提供正負訓練標籤。

不要誤用

先補齊需求相關的候選與未知測試樣本，再用這組權重重新驗證分數與拒答方式；不要直接搬用 CLIP 的門檻。

繼續看第 3 張：上線前先固定條件 ↓

03

上線前先固定條件

SigLIP：部署交出分數，不是回答

以固定模型比較候選，再用域內資料設定覆核。

正式教學插圖 3/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

以固定模型比較候選，再用域內資料設定覆核。
先看哪裡

先看保存候選文字表示，再核對新影像與候選逐一比，最後看依工作風險核對結果。

框與框的關係

部署用預訓練好的圖文表示比較相似性；可依实现轉換配對分數，但不把 sigmoid 值直接當作域內正確率。結果是候選分數或排名，不是逐字生成回答，也不自帶可靠未知類別拒答。

本圖只要帶走

以固定模型比較候選，再用域內資料設定覆核。

表示：影像／文字經編碼後的資料，不是原始照片。
示意結果：用來解釋流程，不代表本輪模型實測。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
部署用預訓練好的圖文表示比較相似性；可依实现轉換配對分數，但不把 sigmoid 值直接當作域內正確率。結果是候選分數或排名，不是逐字生成回答，也不自帶可靠未知類別拒答。
先看保存候選文字表示，再核對新影像與候選逐一比，最後看依工作風險核對結果。
技術上不可誤讀
SigLIP 也會受到候選內容限制：齒輪影像面對只有支架、軸承的集合，仍可能選錯。逐對訓練目標不會替你補齊遺漏詞彙，也不保證知道何時拒答。

完整節點

原始工程節點
1
保存候選文字表示

保存候選文字表示；請對照圖內輸入、變化與結果。

2
新影像與候選逐一比

新影像與候選逐一比；請對照圖內輸入、變化與結果。

3
依工作風險核對結果

依工作風險核對結果；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看保存候選文字表示，再核對新影像與候選逐一比，最後看依工作風險核對結果。

怎麼理解

部署用預訓練好的圖文表示比較相似性；可依实现轉換配對分數，但不把 sigmoid 值直接當作域內正確率。結果是候選分數或排名，不是逐字生成回答，也不自帶可靠未知類別拒答。

不要誤用

先補齊需求相關的候選與未知測試樣本，再用這組權重重新驗證分數與拒答方式；不要直接搬用 CLIP 的門檻。

繼續看第 4 張：選它前先看風險 ↓

04

選它前先看風險

SigLIP：逐對學習不會補出缺少的類別

選型要看本地錯誤與成本，不能只看損失名稱。

正式教學插圖 4/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

選型要看本地錯誤與成本，不能只看損失名稱。
先看哪裡

先看兩種模型看同一齒輪，再核對兩者候選都缺齒輪，最後看固定條件後比較取捨。

框與框的關係

CLIP 與 SigLIP 使用相同工件照片、相同缺少正確答案的候選時，都不能因有最高分就判定正確。比較需固定資料及候選，測誤配、拒答和完整成本；原論文的訓練優點不保證每個域內任務較好。

本圖只要帶走

選型要看本地錯誤與成本，不能只看損失名稱。

表示：影像／文字經編碼後的資料，不是原始照片。
示意結果：用來解釋流程，不代表本輪模型實測。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
CLIP 與 SigLIP 使用相同工件照片、相同缺少正確答案的候選時，都不能因有最高分就判定正確。比較需固定資料及候選，測誤配、拒答和完整成本；原論文的訓練優點不保證每個域內任務較好。
先看兩種模型看同一齒輪，再核對兩者候選都缺齒輪，最後看固定條件後比較取捨。
技術上不可誤讀
SigLIP 也會受到候選內容限制：齒輪影像面對只有支架、軸承的集合，仍可能選錯。逐對訓練目標不會替你補齊遺漏詞彙，也不保證知道何時拒答。

完整節點

原始工程節點
1
兩種模型看同一齒輪

兩種模型看同一齒輪；請對照圖內輸入、變化與結果。

2
兩者候選都缺齒輪

兩者候選都缺齒輪；請對照圖內輸入、變化與結果。

3
固定條件後比較取捨

固定條件後比較取捨；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看兩種模型看同一齒輪，再核對兩者候選都缺齒輪，最後看固定條件後比較取捨。

怎麼理解

CLIP 與 SigLIP 使用相同工件照片、相同缺少正確答案的候選時，都不能因有最高分就判定正確。比較需固定資料及候選，測誤配、拒答和完整成本；原論文的訓練優點不保證每個域內任務較好。

不要誤用

先補齊需求相關的候選與未知測試樣本，再用這組權重重新驗證分數與拒答方式；不要直接搬用 CLIP 的門檻。

工程師操作卡

把 SigLIP 放進小規模試做（POC）前，先回答這五個問題。

先讀白話版就能開始溝通；需要實作、驗證或交接時，再展開下方技術契約。

1
它會交出什麼？

交出圖文表示及配合分數／排序，供語意檢索或分類；本課不把 sigmoid 分數當作缺陷機率或位置。

2
開始先做什麼？

選定原始 SigLIP 權重及配套前處理，沿用一組固定的工件影像與候選文字，同時保留 CLIP 基準和未知樣本。

3
開始前要寫下來的四件事

固定畫面範圍、資料來源、模型版本和驗收方式；詳細項目可在下方展開。

4
怎樣才算真的有用？

同條件看候選命中、錯誤拒答與未知誤選，另量影像處理和文字快取後的完整時間；分數高低本身不是選型結果。

5
碰到什麼先停下來？

先補齊需求相關的候選與未知測試樣本，再用這組權重重新驗證分數與拒答方式；不要直接搬用 CLIP 的門檻。

工程師需要時，再展開原始技術契約

原始建置項目

這些設定要一併保存

輸入與版本：選定原始 SigLIP 權重及配套前處理，沿用一組固定的工件影像與候選文字，同時保留 CLIP 基準和未知樣本。；關鍵機制：原始 SigLIP 同樣學影像和文字的共同表示，但在預訓練時，對每一個配對或不配對的圖文組合分別計算 sigmoid 損失，無需用全批次相似度做 softmax 正規化。這些學習訊號仍共同更新編碼器；部署時再比較新影像與候選文字的配合程度。；獨立驗證：同條件看候選命中、錯誤拒答與未知誤選，另量影像處理和文字快取後的完整時間；分數高低本身不是選型結果。；失敗處理：先補齊需求相關的候選與未知測試樣本，再用這組權重重新驗證分數與拒答方式；不要直接搬用 CLIP 的門檻。

原始選型條件：與 CLIP 比較時，先認清批次 softmax 和逐對 sigmoid 是訓練差異；再固定相同工件、候選文字與測試條件，比實際排序品質與成本。本課介紹原始 SigLIP，不把 SigLIP 2 的新增能力混在一起。 可先用預訓練權重和文字探索，仍要準備相關性／類別驗證資料；換產品需補詞彙、難例及未知圖。各模型使用正確的 tokenizer／前處理，記錄快取、輸入解析度、完整耗時及人工覆核量，不能只比一個絕對分數。

原始失效邊界

下列情況不可忽略

SigLIP 也會受到候選內容限制：齒輪影像面對只有支架、軸承的集合，仍可能選錯。逐對訓練目標不會替你補齊遺漏詞彙，也不保證知道何時拒答。

工程結論
交出圖文表示及配合分數／排序，供語意檢索或分類；本課不把 sigmoid 分數當作缺陷機率或位置。
把這張操作卡帶入 POC 畫布 →
查看 model.md ↗
查看圖檔 manifest ↗

相鄰比較

下一個可比較的模型

比較前，先把「看哪段畫面、用哪些資料、交出什麼、多久完成、誰覆核」固定；再到家族頁看共同條件與差異。

DINOv2 →
DINOv3 →
CLIP →
LLaVA →
查看家族比較 →
← CLIP
LLaVA →