首頁
/
Foundation Vision、語意檢索與 VLM
/
CLIP
✦ Foundation Vision、語意檢索與 VLM
先理解，再看工程圖
CLIP

你想用「金屬支架」「齒輪」等文字整理工件照片，希望增加候選類別時，不必每次都重新訓練一個固定分類器。

你會拿到：回傳影像與文字的相似度及候選排序，交給檢索或分類流程；整圖排序本身不提供可靠的缺陷位置。
先停下來：檢查候選是否涵蓋實際需求，加入域內易混淆及未知樣本；重測文字改寫的穩定性，必要時設可驗證的拒答或覆核流程。
完成本主題
☆ 收藏
從案例理解 CLIP
從必要原理、具體反例到工作取捨

你想用「金屬支架」「齒輪」等文字整理工件照片，希望增加候選類別時，不必每次都重新訓練一個固定分類器。 下列圖像、特徵及回答為教學示意，不是模型實測。

第 01 張 · 看案例，再做判斷

CLIP：讓影像和文字可以比相似

你想用「金屬支架」「齒輪」等文字整理工件照片，希望增加候選類別時，不必每次都重新訓練一個固定分類器。

CLIP：讓影像和文字可以比相似
放大完整圖
為什麼需要這個設計？

CLIP 用影像與其文字描述配對做預訓練，讓正確配對在共同表示空間裡更接近。部署時，影像和每條候選文字分別經編碼器，再比較兩邊表示的相似度。因此可用文字定義候選，做零樣本分類或檢索；它的這條路徑是在排序文字，不是逐字生成回答。

讀完要能說清楚的三個要點
圖與文字各走一條路

兩個編碼器把不同模態轉成可比較的表示；文字不需要先畫成圖片，也不是送入影像解碼器生成新圖。

候選文字就是問題的一部分

類別名稱、描述方式與候選集合都會影響比較。文字特徵可預先計算再重用，但改了文字或權重就要更新。

交付與接手

回傳影像與文字的相似度及候選排序，交給檢索或分類流程；整圖排序本身不提供可靠的缺陷位置。

想深入，再看這張圖的完整工程說明

第 02 張 · 看案例，再做判斷

候選沒有正確答案，第一名也可能錯

觀察哪個條件改變，讓原本的結果不夠可靠？

候選沒有正確答案，第一名也可能錯
放大完整圖
看哪裡

圖中明明是齒輪，但候選只有「金屬支架」與「軸承」時，仍可能選出一個第一名。第一名只是集合內的相對結果；加上「其他」也不會自動解決未知類別辨識。

接著怎麼做

檢查候選是否涵蓋實際需求，加入域內易混淆及未知樣本；重測文字改寫的穩定性，必要時設可驗證的拒答或覆核流程。

想深入，再看這張圖的完整工程說明

第 03 張 · 看案例，再做判斷

CLIP 與 SigLIP：訓練不同，選型要同題

相同工作需求下，資料、輸出與成本如何取捨？

CLIP 與 SigLIP：訓練不同，選型要同題
放大完整圖
怎麼選

CLIP 與原始 SigLIP 都可做圖文相似度，但 CLIP 用批次對比與 softmax 正規化，SigLIP 用逐對 sigmoid 目標。訓練目標的差異不保證某一個在你的工件上更準、更快。

資料與完整成本

只有正常照片也能探索語意检索，但仍需候選文字、實際類別／相關性標註與未知樣本來驗證。換產品先重查詞彙、拍攝條件及混淆類別；同圖同詞比較排序錯誤、拒答、文字特徵快取和完整耗時。

想深入，再看這張圖的完整工程說明

換情境試著判斷

新增一種外觀相似零件

你的照片庫新增聯軸器，現有候選只有軸承與齒輪。團隊想把最高分當作類別。

先增加文字候選、訓練分類器，還是加覆核？怎樣決定？

看解釋與可接受的取捨

可先補聯軸器與易混淆描述並做小規模檢索測試，配合未知樣本和覆核；若細微差異仍不穩定且有足夠標註，專用分類器也合理。不能只因已有最高分就把照片硬分進舊類別。

需要時查看基本名詞與模型背景
工程參考：展開文字因果鏈與設定核對點不重複主圖；只核對每步要鎖定的設定。
想深入：展開失效案例、模型比較與小規模試做（POC）
初學者可先跳過；準備實作或驗收時再逐項核對。
工程參考：展開四張工程圖與完整節點
從輸入、機制、交付到限制，核對實作與驗證方式。

工程師操作卡

把 CLIP 放進小規模試做（POC）前，先回答這五個問題。

先讀白話版就能開始溝通；需要實作、驗證或交接時，再展開下方技術契約。

1
它會交出什麼？

回傳影像與文字的相似度及候選排序，交給檢索或分類流程；整圖排序本身不提供可靠的缺陷位置。

2
開始先做什麼？

列出希望搜尋的工件名稱及描述，準備各類代表影像和容易混淆／不在候選內的測試圖；固定原始 CLIP 權重及前處理。

3
開始前要寫下來的四件事

固定畫面範圍、資料來源、模型版本和驗收方式；詳細項目可在下方展開。

4
怎樣才算真的有用？

核對前幾名是否包含正確／相關候選、改寫文字是否翻轉結果，以及未知圖是否被錯選；與既有搜尋或分類流程比較錯誤與覆核時間。

5
碰到什麼先停下來？

檢查候選是否涵蓋實際需求，加入域內易混淆及未知樣本；重測文字改寫的穩定性，必要時設可驗證的拒答或覆核流程。

工程師需要時，再展開原始技術契約
把這張操作卡帶入 POC 畫布 →
查看 model.md ↗
查看圖檔 manifest ↗

相鄰比較

下一個可比較的模型

比較前，先把「看哪段畫面、用哪些資料、交出什麼、多久完成、誰覆核」固定；再到家族頁看共同條件與差異。

DINOv2 →
DINOv3 →
SigLIP →
LLaVA →
查看家族比較 →
← DINOv3
SigLIP →