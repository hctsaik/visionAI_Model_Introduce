首頁
/
時間序列、運動與影片理解
/
ConvLSTM
▷ 時間序列、運動與影片理解
先理解，再看工程圖
ConvLSTM

夾爪的單張照片看不出動作經過；你想結合前幾張畫面，判斷目前較像接近、夾取還是移開的階段。

你會拿到：本例將時序狀態交給另行訓練的任務頭，產生夾取階段候選；其他接法可預測影像等，但不是單元自動具有的所有能力。
先停下來：固定片段切法與時間戳，依任務在邊界重置狀態；加入掉幀和長短片段測試，回看分類候選，不以記憶存在便宣稱理解動作。
完成本主題
☆ 收藏
從案例理解 ConvLSTM
從必要原理、具體反例到工作取捨

夾爪的單張照片看不出動作經過；你想結合前幾張畫面，判斷目前較像接近、夾取還是移開的階段。 下列圖像、特徵及回答為教學示意，不是模型實測。

第 01 張 · 看案例，再做判斷

ConvLSTM：把位置與時間一起留在記憶裡

夾爪的單張照片看不出動作經過；你想結合前幾張畫面，判斷目前較像接近、夾取還是移開的階段。

ConvLSTM：把位置與時間一起留在記憶裡
放大完整圖
為什麼需要這個設計？

ConvLSTM 在每個時間點把新輸入與舊狀態一起處理，以卷積更新保留空間結構的記憶，選擇保留、加入或忘掉哪些線索。相較把整張攤平處理，它保留鄰近位置的關係；相較只看單張，它可以利用前面看到的變化。

讀完要能說清楚的三個要點
記憶是學到的狀態

隱藏狀態與記憶不是把舊影格直接存成照片。它們由訓練目標決定保留哪些時間與空間線索。

單元不等於現成模型

原始論文以降雨預測為例；本課夾爪階段為工業改編，需要影像編碼、ConvLSTM與相應任務頭及訓練。

交付與接手

本例將時序狀態交給另行訓練的任務頭，產生夾取階段候選；其他接法可預測影像等，但不是單元自動具有的所有能力。

想深入，再看這張圖的完整工程說明
ConvLSTM：把位置與時間一起留在記憶裡

原始問題：夾爪的單張照片看不出動作經過；你想結合前幾張畫面，判斷目前較像接近、夾取還是移開的階段。

ConvLSTM 在每個時間點把新輸入與舊狀態一起處理，以卷積更新保留空間結構的記憶，選擇保留、加入或忘掉哪些線索。相較把整張攤平處理，它保留鄰近位置的關係；相較只看單張，它可以利用前面看到的變化。

記憶是學到的狀態

隱藏狀態與記憶不是把舊影格直接存成照片。它們由訓練目標決定保留哪些時間與空間線索。

單元不等於現成模型

原始論文以降雨預測為例；本課夾爪階段為工業改編，需要影像編碼、ConvLSTM與相應任務頭及訓練。

交付與接手

本例將時序狀態交給另行訓練的任務頭，產生夾取階段候選；其他接法可預測影像等，但不是單元自動具有的所有能力。

第 02 張 · 看案例，再做判斷

時間接錯，記憶就會帶著舊狀態

觀察哪個條件改變，讓原本的結果不夠可靠？

時間接錯，記憶就會帶著舊狀態
放大完整圖
看哪裡

一段影片結束後直接沿用記憶到另一段，舊的已夾住狀態可能干擾新片段；掉幀、順序錯誤或取樣間隔改變也會改變時間意義。 圖中的夾爪小圖用來表示狀態意義，不是記憶張量的實際外觀。

接著怎麼做

固定片段切法與時間戳，依任務在邊界重置狀態；加入掉幀和長短片段測試，回看分類候選，不以記憶存在便宣稱理解動作。

想深入，再看這張圖的完整工程說明
時間接錯，記憶就會帶著舊狀態

原始問題：觀察哪個條件改變，讓原本的結果不夠可靠？

一段影片結束後直接沿用記憶到另一段，舊的已夾住狀態可能干擾新片段；掉幀、順序錯誤或取樣間隔改變也會改變時間意義。 圖中的夾爪小圖用來表示狀態意義，不是記憶張量的實際外觀。

看哪裡

一段影片結束後直接沿用記憶到另一段，舊的已夾住狀態可能干擾新片段；掉幀、順序錯誤或取樣間隔改變也會改變時間意義。

接著怎麼做

固定片段切法與時間戳，依任務在邊界重置狀態；加入掉幀和長短片段測試，回看分類候選，不以記憶存在便宣稱理解動作。

第 03 張 · 看案例，再做判斷

記住序列、補像素、預測特徵：學習方式不同

相同工作需求下，資料、輸出與成本如何取捨？

記住序列、補像素、預測特徵：學習方式不同
放大完整圖
怎麼選

少量明確動作任務，可比較單張分類、簡單時序規則與ConvLSTM；較通用影片表示可比較VideoMAE或V-JEPA。後兩者提供預訓練表示，不是免標註的現成夾爪判定器。

資料與完整成本

需準備符合任務的連續片段與標註，按完整作業／實體分開訓練與測試。換動作、相機或取樣頻率要重新驗證，必要時重訓；成本含序列等待與狀態管理。

想深入，再看這張圖的完整工程說明
記住序列、補像素、預測特徵：學習方式不同

原始問題：相同工作需求下，資料、輸出與成本如何取捨？

少量明確動作任務，可比較單張分類、簡單時序規則與ConvLSTM；較通用影片表示可比較VideoMAE或V-JEPA。後兩者提供預訓練表示，不是免標註的現成夾爪判定器。

怎麼選

少量明確動作任務，可比較單張分類、簡單時序規則與ConvLSTM；較通用影片表示可比較VideoMAE或V-JEPA。後兩者提供預訓練表示，不是免標註的現成夾爪判定器。

資料與完整成本

需準備符合任務的連續片段與標註，按完整作業／實體分開訓練與測試。換動作、相機或取樣頻率要重新驗證，必要時重訓；成本含序列等待與狀態管理。

換情境試著判斷

換成兩台相機交替片段

服務交替收到機台A和B的片段，夾爪姿態不同。

可以共用上一段的記憶嗎？

看解釋與可接受的取捨

不能不加區分地共用；應按相機／連續片段隔離或重置狀態，按真正任務訓練並測邊界。若單張已足以判階段，先比較較簡單且無狀態維護的基準。

需要時查看基本名詞與模型背景
保留空間結構的循環單元
記憶是學到的狀態：隱藏狀態與記憶不是把舊影格直接存成照片。它們由訓練目標決定保留哪些時間與空間線索。
單元不等於現成模型：原始論文以降雨預測為例；本課夾爪階段為工業改編，需要影像編碼、ConvLSTM與相應任務頭及訓練。
保留空間結構的循環單元

ConvLSTM 在每個時間點把新輸入與舊狀態一起處理，以卷積更新保留空間結構的記憶，選擇保留、加入或忘掉哪些線索。相較把整張攤平處理，它保留鄰近位置的關係；相較只看單張，它可以利用前面看到的變化。

一段影片結束後直接沿用記憶到另一段，舊的已夾住狀態可能干擾新片段；掉幀、順序錯誤或取樣間隔改變也會改變時間意義。

工程參考：展開文字因果鏈與設定核對點不重複主圖；只核對每步要鎖定的設定。

模型專屬因果鏈

實作時逐步核對

主圖已在上方第一遍。這裡只核對每一步要鎖定的設定與為什麼，不再重貼同一張圖。

1
連續影格的空間線索

隱藏狀態與記憶不是把舊影格直接存成照片。它們由訓練目標決定保留哪些時間與空間線索。

因為：由本課核心機制連接工作用途

設定核對：WI028-1

2
卷積更新狀態

原始論文以降雨預測為例；本課夾爪階段為工業改編，需要影像編碼、ConvLSTM與相應任務頭及訓練。

因為：由本課核心機制連接工作用途

設定核對：WI028-2

3
任務頭判讀片段

本例將時序狀態交給另行訓練的任務頭，產生夾取階段候選；其他接法可預測影像等，但不是單元自動具有的所有能力。

因為：由本課核心機制連接工作用途

設定核對：WI028-3

4
工作核對與接手

比較漏掉的階段、錯誤切換、事件延遲和完整運算成本；用丟幀／片段邊界負例檢查模型是否依賴正確時間線索。

因為：由本課核心機制連接工作用途

設定核對：WI028-4

想深入：展開失效案例、模型比較與小規模試做（POC）
初學者可先跳過；準備實作或驗收時再逐項核對。

失效案例、選型與小規模試做

什麼情況不能相信它？

這一區保留完整工程條件，供準備實作或驗收的人逐項核對。

本課具體反例

可能看到：一段影片結束後直接沿用記憶到另一段，舊的已夾住狀態可能干擾新片段；掉幀、順序錯誤或取樣間隔改變也會改變時間意義。

正確下一步：固定片段切法與時間戳，依任務在邊界重置狀態；加入掉幀和長短片段測試，回看分類候選，不以記憶存在便宣稱理解動作。

更換產品、模型或輸入設定

可能看到：需準備符合任務的連續片段與標註，按完整作業／實體分開訓練與測試。換動作、相機或取樣頻率要重新驗證，必要時重訓；成本含序列等待與狀態管理。

正確下一步：先定義動作階段標註，保存完整循環與時間戳，依作業批次切分資料，建立單張或規則基準。

把示意當成實際推論

可能看到：本課特徵、排序、回答及圖像僅用於解釋，不支持現場準確率或耗時結論。

正確下一步：比較漏掉的階段、錯誤切換、事件延遲和完整運算成本；用丟幀／片段邊界負例檢查模型是否依賴正確時間線索。

相鄰方法比較

記住序列、補像素、預測特徵：學習方式不同
ConvLSTM應用
連續片段、任務標註；訓練空間記憶與任務頭
依任務定義的序列輸出；管理狀態／重置
VideoMAE原版
像素重建預訓練；可用現有權重，需下游標註
影片表示＋任務頭；重建不是現場判定
V-JEPA 2024原版
特徵預測預訓練；可用現有權重，需下游標註
影片表示＋任務頭；不是未來影片生成

少量明確動作任務，可比較單張分類、簡單時序規則與ConvLSTM；較通用影片表示可比較VideoMAE或V-JEPA。後兩者提供預訓練表示，不是免標註的現成夾爪判定器。 需準備符合任務的連續片段與標註，按完整作業／實體分開訓練與測試。換動作、相機或取樣頻率要重新驗證，必要時重訓；成本含序列等待與狀態管理。

工程第一步

以真實工作資料做小規模比較
先定義動作階段標註，保存完整循環與時間戳，依作業批次切分資料，建立單張或規則基準。
需準備符合任務的連續片段與標註，按完整作業／實體分開訓練與測試。換動作、相機或取樣頻率要重新驗證，必要時重訓；成本含序列等待與狀態管理。
比較漏掉的階段、錯誤切換、事件延遲和完整運算成本；用丟幀／片段邊界負例檢查模型是否依賴正確時間線索。
目前做法

既有流程或最簡單可行方法

要量什麼

比較漏掉的階段、錯誤切換、事件延遲和完整運算成本；用丟幀／片段邊界負例檢查模型是否依賴正確時間線索。

反例測試

一段影片結束後直接沿用記憶到另一段，舊的已夾住狀態可能干擾新片段；掉幀、順序錯誤或取樣間隔改變也會改變時間意義。

誰確認

實作與影像核對負責人

先停下來的訊號：固定片段切法與時間戳，依任務在邊界重置狀態；加入掉幀和長短片段測試，回看分類候選，不以記憶存在便宣稱理解動作。

讀完請自己講回來

它改變什麼？何時選另一種做法？
看本頁練習的核對說明

ConvLSTM 在每個時間點把新輸入與舊狀態一起處理，以卷積更新保留空間結構的記憶，選擇保留、加入或忘掉哪些線索。相較把整張攤平處理，它保留鄰近位置的關係；相較只看單張，它可以利用前面看到的變化。 少量明確動作任務，可比較單張分類、簡單時序規則與ConvLSTM；較通用影片表示可比較VideoMAE或V-JEPA。後兩者提供預訓練表示，不是免標註的現成夾爪判定器。

換一個現場再推理

換成兩台相機交替片段

情境：服務交替收到機台A和B的片段，夾爪姿態不同。

請先回答：可以共用上一段的記憶嗎？

核對這個遷移題

不能不加區分地共用；應按相機／連續片段隔離或重置狀態，按真正任務訓練並測邊界。若單張已足以判階段，先比較較簡單且無狀態維護的基準。

內容依據：展開這頁主張的 model.md 可追溯範圍

roadmap-model-selection/video-temporal/convlstm/model.md ↗

WI-033：工程層四圖與手機比較修正；首讀有效內容保留。原生圖已審，整頁與使用者審閱另記。

beginner_path.visuals[conveyor-window]：模型定位 / architecture_path
beginner_path.visuals[window-spatial-feature]：architecture_path
beginner_path.visuals[convolutional-gates-state]：representation_or_score
beginner_path.visuals[reset-warmup-counterfactual]：failure_boundary
beginner_path.visuals[downstream-event-gate]：selection_gate / evidence_bundle
promise：模型定位 / representation_or_score
mechanism_steps：architecture_path / representation_or_score
boundary_cases：failure_boundary / selection_gate
comparison + selection：selection_gate
poc + acceptance：cost_and_operating_point / evidence_bundle
transfer_check：failure_boundary / selection_gate
beginner_path.visuals[0] + scenario + mental_model：roadmap-model-selection/video-temporal/convlstm/model.md
micro_example + transfer_check + selection：roadmap-model-selection/video-temporal/convlstm/model.md
beginner_path + core + selection + transfer：https://arxiv.org/abs/1506.04214
工程參考：展開四張工程圖與完整節點
從輸入、機制、交付到限制，核對實作與驗證方式。

ConvLSTM 在每個時間點把新輸入與舊狀態一起處理，以卷積更新保留空間結構的記憶，選擇保留、加入或忘掉哪些線索。相較把整張攤平處理，它保留鄰近位置的關係；相較只看單張，它可以利用前面看到的變化。

01
先看它交出什麼
02
再看它怎麼做
03
上線前先固定條件
04
選它前先看風險
01

先看它交出什麼

ConvLSTM：影片狀態接上明確任務

時序表示要接任務頭，才有可驗的輸出。

正式教學插圖 1/4 · 已可直接閱讀；點圖僅用於放大細節開啟原圖 ↗

30 秒讀懂這張圖

時序表示要接任務頭，才有可驗的輸出。
先看哪裡

先看連續夾取影片與任務標註，再核對卷積記憶接任務頭，最後看完整影片驗失敗與延遲。

框與框的關係

同夾爪方塊，夾取成功/失敗是另定義標籤；ConvLSTM產生H/C，分類頭需有監督。以整段影片或實體工件切分，不讓相鄰幀跨訓練驗證。

本圖只要帶走

時序表示要接任務頭，才有可驗的輸出。

示意數值：作者給定局部算例，非模型實測或建議參數。
同位置：比較保持相同影像座標與時間身份，不能任意換工件。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
同夾爪方塊，夾取成功/失敗是另定義標籤；ConvLSTM產生H/C，分類頭需有監督。以整段影片或實體工件切分，不讓相鄰幀跨訓練驗證。
先看連續夾取影片與任務標註，再核對卷積記憶接任務頭，最後看完整影片驗失敗與延遲。
技術上不可誤讀
一段影片結束後直接沿用記憶到另一段，舊的已夾住狀態可能干擾新片段；掉幀、順序錯誤或取樣間隔改變也會改變時間意義。

完整節點

原始工程節點
1
連續夾取影片與任務標註

連續夾取影片與任務標註；請對照圖內輸入、變化與結果。

2
卷積記憶接任務頭

卷積記憶接任務頭；請對照圖內輸入、變化與結果。

3
完整影片驗失敗與延遲

完整影片驗失敗與延遲；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看連續夾取影片與任務標註，再核對卷積記憶接任務頭，最後看完整影片驗失敗與延遲。

怎麼理解

同夾爪方塊，夾取成功/失敗是另定義標籤；ConvLSTM產生H/C，分類頭需有監督。以整段影片或實體工件切分，不讓相鄰幀跨訓練驗證。

不要誤用

固定片段切法與時間戳，依任務在邊界重置狀態；加入掉幀和長短片段測試，回看分類候選，不以記憶存在便宣稱理解動作。

繼續看第 2 張：再看它怎麼做 ↓

02

再看它怎麼做

ConvLSTM：卷積門控更新空間記憶

新影格和舊狀態，逐位置決定記住什麼。

正式教學插圖 2/4 · 已可直接閱讀；點圖僅用於放大細節開啟原圖 ↗

30 秒讀懂這張圖

新影格和舊狀態，逐位置決定記住什麼。
先看哪裡

先看新影格與前一步空間狀態，再核對門控保留舊值、加入新值，最後看新狀態傳到下一時間點。

框與框的關係

同一夾爪與方塊依序張開、接近、夾住。X_t及H_(t-1)以卷積產生門控，C_t=f_t⊙C_(t-1)+i_t⊙候選；H_t=o_t⊙tanh(C_t)。原論文可有peephole。給定單位置Cprev=.4,f=.5,i=.8,candidate=.5得C=.6；其餘位置另有值。不是每張影格都重新歸零；任务頭另訓練。 r01實看修正：下一步明畫卷積門控與X(t+1)，避免誤讀直接預測圖像。

本圖只要帶走

新影格和舊狀態，逐位置決定記住什麼。

示意數值：作者給定局部算例，非模型實測或建議參數。
同位置：比較保持相同影像座標與時間身份，不能任意換工件。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
同一夾爪與方塊依序張開、接近、夾住。X_t及H_(t-1)以卷積產生門控，C_t=f_t⊙C_(t-1)+i_t⊙候選；H_t=o_t⊙tanh(C_t)。原論文可有peephole。給定單位置Cprev=.4,f=.5,i=.8,candidate=.5得C=.6；其餘位置另有值。不是每張影格都重新歸零；任务頭另訓練。 r01實看修正：下一步明畫卷積門控與X(t+1)，避免誤讀直接預測圖像。
先看新影格與前一步空間狀態，再核對門控保留舊值、加入新值，最後看新狀態傳到下一時間點。
技術上不可誤讀
一段影片結束後直接沿用記憶到另一段，舊的已夾住狀態可能干擾新片段；掉幀、順序錯誤或取樣間隔改變也會改變時間意義。

完整節點

原始工程節點
1
新影格與前一步空間狀態

新影格與前一步空間狀態；請對照圖內輸入、變化與結果。

2
門控保留舊值、加入新值

門控保留舊值、加入新值；請對照圖內輸入、變化與結果。

3
新狀態傳到下一時間點

新狀態傳到下一時間點；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看新影格與前一步空間狀態，再核對門控保留舊值、加入新值，最後看新狀態傳到下一時間點。

怎麼理解

同一夾爪與方塊依序張開、接近、夾住。X_t及H_(t-1)以卷積產生門控，C_t=f_t⊙C_(t-1)+i_t⊙候選；H_t=o_t⊙tanh(C_t)。原論文可有peephole。給定單位置Cprev=.4,f=.5,i=.8,candidate=.5得C=.6；其餘位置另有值。不是每張影格都重新歸零；任务頭另訓練。 r01實看修正：下一步明畫卷積門控與X(t+1)，避免誤讀直接預測圖像。

不要誤用

固定片段切法與時間戳，依任務在邊界重置狀態；加入掉幀和長短片段測試，回看分類候選，不以記憶存在便宣稱理解動作。

繼續看第 3 張：上線前先固定條件 ↓

03

上線前先固定條件

ConvLSTM：狀態生命週期要明定

換影片或相機時，依任務重設記憶。

正式教學插圖 3/4 · 已可直接閱讀；點圖僅用於放大細節開啟原圖 ↗

30 秒讀懂這張圖

換影片或相機時，依任務重設記憶。
先看哪裡

先看順序、幀距與縮放固定，再核對同片續接，換片重設，最後看保存狀態與任務頭版本。

框與框的關係

推論狀態初始化/持續/重設需符合訓練方式；不能讓相機A記憶污染B。保存幀距、解析度、網路及任務頭版本，量完整預處理/狀態更新/任務輸出延遲。 r01實看修正：时间刻度及標籤各自定位，避免SVG空白合併。

本圖只要帶走

換影片或相機時，依任務重設記憶。

示意數值：作者給定局部算例，非模型實測或建議參數。
同位置：比較保持相同影像座標與時間身份，不能任意換工件。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
推論狀態初始化/持續/重設需符合訓練方式；不能讓相機A記憶污染B。保存幀距、解析度、網路及任務頭版本，量完整預處理/狀態更新/任務輸出延遲。 r01實看修正：时间刻度及標籤各自定位，避免SVG空白合併。
先看順序、幀距與縮放固定，再核對同片續接，換片重設，最後看保存狀態與任務頭版本。
技術上不可誤讀
一段影片結束後直接沿用記憶到另一段，舊的已夾住狀態可能干擾新片段；掉幀、順序錯誤或取樣間隔改變也會改變時間意義。

完整節點

原始工程節點
1
順序、幀距與縮放固定

順序、幀距與縮放固定；請對照圖內輸入、變化與結果。

2
同片續接，換片重設

同片續接，換片重設；請對照圖內輸入、變化與結果。

3
保存狀態與任務頭版本

保存狀態與任務頭版本；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看順序、幀距與縮放固定，再核對同片續接，換片重設，最後看保存狀態與任務頭版本。

怎麼理解

推論狀態初始化/持續/重設需符合訓練方式；不能讓相機A記憶污染B。保存幀距、解析度、網路及任務頭版本，量完整預處理/狀態更新/任務輸出延遲。 r01實看修正：时间刻度及標籤各自定位，避免SVG空白合併。

不要誤用

固定片段切法與時間戳，依任務在邊界重置狀態；加入掉幀和長短片段測試，回看分類候選，不以記憶存在便宣稱理解動作。

繼續看第 4 張：選它前先看風險 ↓

04

選它前先看風險

ConvLSTM：有記憶也可能忘記關鍵

記憶是一種可學狀態，不是完整影格存檔。

正式教學插圖 4/4 · 已可直接閱讀；點圖僅用於放大細節開啟原圖 ↗

30 秒讀懂這張圖

記憶是一種可學狀態，不是完整影格存檔。
先看哪裡

先看短暫失敗發生在中間影格，再核對忘記門連乘使舊訊號衰減，最後看不同片長與幀距各自驗。

框與框的關係

作者單格例：後續不加入新候選，C初值1、每步f=.5，經三步變.125；不代表真模型門固定。關鍵事件若未取到/遭遺忘仍可能漏檢。

本圖只要帶走

記憶是一種可學狀態，不是完整影格存檔。

示意數值：作者給定局部算例，非模型實測或建議參數。
同位置：比較保持相同影像座標與時間身份，不能任意換工件。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
作者單格例：後續不加入新候選，C初值1、每步f=.5，經三步變.125；不代表真模型門固定。關鍵事件若未取到/遭遺忘仍可能漏檢。
先看短暫失敗發生在中間影格，再核對忘記門連乘使舊訊號衰減，最後看不同片長與幀距各自驗。
技術上不可誤讀
一段影片結束後直接沿用記憶到另一段，舊的已夾住狀態可能干擾新片段；掉幀、順序錯誤或取樣間隔改變也會改變時間意義。

完整節點

原始工程節點
1
短暫失敗發生在中間影格

短暫失敗發生在中間影格；請對照圖內輸入、變化與結果。

2
忘記門連乘使舊訊號衰減

忘記門連乘使舊訊號衰減；請對照圖內輸入、變化與結果。

3
不同片長與幀距各自驗

不同片長與幀距各自驗；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看短暫失敗發生在中間影格，再核對忘記門連乘使舊訊號衰減，最後看不同片長與幀距各自驗。

怎麼理解

作者單格例：後續不加入新候選，C初值1、每步f=.5，經三步變.125；不代表真模型門固定。關鍵事件若未取到/遭遺忘仍可能漏檢。

不要誤用

固定片段切法與時間戳，依任務在邊界重置狀態；加入掉幀和長短片段測試，回看分類候選，不以記憶存在便宣稱理解動作。

工程師操作卡

把 ConvLSTM 放進小規模試做（POC）前，先回答這五個問題。

先讀白話版就能開始溝通；需要實作、驗證或交接時，再展開下方技術契約。

1
它會交出什麼？

本例將時序狀態交給另行訓練的任務頭，產生夾取階段候選；其他接法可預測影像等，但不是單元自動具有的所有能力。

2
開始先做什麼？

先定義動作階段標註，保存完整循環與時間戳，依作業批次切分資料，建立單張或規則基準。

3
開始前要寫下來的四件事

固定畫面範圍、資料來源、模型版本和驗收方式；詳細項目可在下方展開。

4
怎樣才算真的有用？

比較漏掉的階段、錯誤切換、事件延遲和完整運算成本；用丟幀／片段邊界負例檢查模型是否依賴正確時間線索。

5
碰到什麼先停下來？

固定片段切法與時間戳，依任務在邊界重置狀態；加入掉幀和長短片段測試，回看分類候選，不以記憶存在便宣稱理解動作。

工程師需要時，再展開原始技術契約

原始建置項目

這些設定要一併保存

輸入與版本：先定義動作階段標註，保存完整循環與時間戳，依作業批次切分資料，建立單張或規則基準。；關鍵機制：ConvLSTM 在每個時間點把新輸入與舊狀態一起處理，以卷積更新保留空間結構的記憶，選擇保留、加入或忘掉哪些線索。相較把整張攤平處理，它保留鄰近位置的關係；相較只看單張，它可以利用前面看到的變化。；獨立驗證：比較漏掉的階段、錯誤切換、事件延遲和完整運算成本；用丟幀／片段邊界負例檢查模型是否依賴正確時間線索。；失敗處理：固定片段切法與時間戳，依任務在邊界重置狀態；加入掉幀和長短片段測試，回看分類候選，不以記憶存在便宣稱理解動作。

原始選型條件：少量明確動作任務，可比較單張分類、簡單時序規則與ConvLSTM；較通用影片表示可比較VideoMAE或V-JEPA。後兩者提供預訓練表示，不是免標註的現成夾爪判定器。 需準備符合任務的連續片段與標註，按完整作業／實體分開訓練與測試。換動作、相機或取樣頻率要重新驗證，必要時重訓；成本含序列等待與狀態管理。

原始失效邊界

下列情況不可忽略

一段影片結束後直接沿用記憶到另一段，舊的已夾住狀態可能干擾新片段；掉幀、順序錯誤或取樣間隔改變也會改變時間意義。

工程結論
本例將時序狀態交給另行訓練的任務頭，產生夾取階段候選；其他接法可預測影像等，但不是單元自動具有的所有能力。
把這張操作卡帶入 POC 畫布 →
查看 model.md ↗
查看圖檔 manifest ↗

相鄰比較

下一個可比較的模型

比較前，先把「看哪段畫面、用哪些資料、交出什麼、多久完成、誰覆核」固定；再到家族頁看共同條件與差異。

Frame Difference →
Background Subtraction →
Lucas–Kanade →
RAFT →
查看家族比較 →
← ByteTrack
VideoMAE →