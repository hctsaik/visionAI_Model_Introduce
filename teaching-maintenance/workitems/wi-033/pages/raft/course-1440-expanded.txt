首頁
/
時間序列、運動與影片理解
/
RAFT
▷ 時間序列、運動與影片理解
先理解，再看工程圖
RAFT

你想知道移動金屬板上不同區域如何移動，而不只追三個角點；需要一張可以往下游取樣的稠密位移圖。

你會拿到：每個像素的水平／垂直影像位移估計。本課箭頭只是示意；物件身分、速度、形變量測與事件判定要另有分群、校正與驗證。
先停下來：回原片查遮擋與反光，搭配前後向或任務檢查標出不可靠區；不要把平滑光流直接當毫米級量測。
完成本主題
☆ 收藏
從案例理解 RAFT
從必要原理、具體反例到工作取捨

你想知道移動金屬板上不同區域如何移動，而不只追三個角點；需要一張可以往下游取樣的稠密位移圖。 下列圖像、特徵及回答為教學示意，不是模型實測。

第 01 張 · 看案例，再做判斷

RAFT：反覆查找對應，估計整張位移

你想知道移動金屬板上不同區域如何移動，而不只追三個角點；需要一張可以往下游取樣的稠密位移圖。

RAFT：反覆查找對應，估計整張位移
放大完整圖
為什麼需要這個設計？

原始 RAFT 先對兩張影格各自抽特徵，建立所有位置配對的相似線索；再反覆查詢多尺度相關性，用更新單元修正目前的光流估計。全配對提供廣泛的候選位置，反覆更新把這些線索逐步轉成每個像素的二維位移。

讀完要能說清楚的三個要點
兩影格，許多候選

相關性描述哪些特徵可能相似，不代表每個位置都找到唯一真實對應；重複紋理仍可能混淆。

反覆修正估計

多次更新不是多拍幾張影格；推論反覆使用這一對影格的特徵。迭代與解析度會影響運算及記憶體。

交付與接手

每個像素的水平／垂直影像位移估計。本課箭頭只是示意；物件身分、速度、形變量測與事件判定要另有分群、校正與驗證。

想深入，再看這張圖的完整工程說明
RAFT：反覆查找對應，估計整張位移

原始問題：你想知道移動金屬板上不同區域如何移動，而不只追三個角點；需要一張可以往下游取樣的稠密位移圖。

原始 RAFT 先對兩張影格各自抽特徵，建立所有位置配對的相似線索；再反覆查詢多尺度相關性，用更新單元修正目前的光流估計。全配對提供廣泛的候選位置，反覆更新把這些線索逐步轉成每個像素的二維位移。

兩影格，許多候選

相關性描述哪些特徵可能相似，不代表每個位置都找到唯一真實對應；重複紋理仍可能混淆。

反覆修正估計

多次更新不是多拍幾張影格；推論反覆使用這一對影格的特徵。迭代與解析度會影響運算及記憶體。

交付與接手

每個像素的水平／垂直影像位移估計。本課箭頭只是示意；物件身分、速度、形變量測與事件判定要另有分群、校正與驗證。

第 02 張 · 看案例，再做判斷

沒有可靠對應，兩種光流都要核對

觀察哪個條件改變，讓原本的結果不夠可靠？

沒有可靠對應，兩種光流都要核對
放大完整圖
看哪裡

固定同一L形工件的影格對，清楚組與後影格反光組都交給LK和RAFT。兩者各自核對對應、錯誤與可用覆蓋，不能只把有利條件分給其中一種方法。

接著怎麼做

回原片查遮擋與反光，搭配前後向或任務檢查標出不可靠區；不要把平滑光流直接當毫米級量測。

想深入，再看這張圖的完整工程說明
沒有可靠對應，兩種光流都要核對

原始問題：觀察哪個條件改變，讓原本的結果不夠可靠？

固定同一L形工件的影格對，清楚組與後影格反光組都交給LK和RAFT。兩者各自核對對應、錯誤與可用覆蓋，不能只把有利條件分給其中一種方法。

看哪裡

固定同一L形工件的影格對，清楚組與後影格反光組都交給LK和RAFT。兩者各自核對對應、錯誤與可用覆蓋，不能只把有利條件分給其中一種方法。

接著怎麼做

回原片查遮擋與反光，搭配前後向或任務檢查標出不可靠區；不要把平滑光流直接當毫米級量測。

第 03 張 · 看案例，再做判斷

少量追點，還是整張位移？

相同工作需求下，資料、輸出與成本如何取捨？

少量追點，還是整張位移？
放大完整圖
怎麼選

需要全圖位移時比較RAFT與傳統稠密光流；只需少量角點可先試LK。RAFT要權重與較多運算預算，是否值得取決於域內位移品質和下游任務。

資料與完整成本

先固定具體權重、前處理、影像解析度與更新次數。換相機／材質需測域落差，必要時另準備配對監督資料微調；同看誤差、遮擋錯誤、記憶體與端到端耗時。

想深入，再看這張圖的完整工程說明
少量追點，還是整張位移？

原始問題：相同工作需求下，資料、輸出與成本如何取捨？

需要全圖位移時比較RAFT與傳統稠密光流；只需少量角點可先試LK。RAFT要權重與較多運算預算，是否值得取決於域內位移品質和下游任務。

怎麼選

需要全圖位移時比較RAFT與傳統稠密光流；只需少量角點可先試LK。RAFT要權重與較多運算預算，是否值得取決於域內位移品質和下游任務。

資料與完整成本

先固定具體權重、前處理、影像解析度與更新次數。換相機／材質需測域落差，必要時另準備配對監督資料微調；同看誤差、遮擋錯誤、記憶體與端到端耗時。

換情境試著判斷

換成只需幾個固定角點

現場CPU即可，工作只需三個角點是否偏移，位移很小、紋理清楚。

為了稠密輸出一定要選RAFT嗎？

看解釋與可接受的取捨

不必；先以LK或既有對位法作基準，測角點誤差與漂移。只有下游確實需要更廣位移覆蓋，且效果改善值得運算代價時，再採用RAFT。

需要時查看基本名詞與模型背景
學習式稠密光流
兩影格，許多候選：相關性描述哪些特徵可能相似，不代表每個位置都找到唯一真實對應；重複紋理仍可能混淆。
反覆修正估計：多次更新不是多拍幾張影格；推論反覆使用這一對影格的特徵。迭代與解析度會影響運算及記憶體。
學習式稠密光流

原始 RAFT 先對兩張影格各自抽特徵，建立所有位置配對的相似線索；再反覆查詢多尺度相關性，用更新單元修正目前的光流估計。全配對提供廣泛的候選位置，反覆更新把這些線索逐步轉成每個像素的二維位移。

金屬反光或遮擋讓某些像素在下一影格沒有可靠可見對應；RAFT仍可能輸出數值，但這是估計，不是觀測真值。

工程參考：展開文字因果鏈與設定核對點不重複主圖；只核對每步要鎖定的設定。

模型專屬因果鏈

實作時逐步核對

主圖已在上方第一遍。這裡只核對每一步要鎖定的設定與為什麼，不再重貼同一張圖。

1
兩張影格抽特徵

相關性描述哪些特徵可能相似，不代表每個位置都找到唯一真實對應；重複紋理仍可能混淆。

因為：由本課核心機制連接工作用途

設定核對：WI028-1

2
查詢全配對線索並更新

多次更新不是多拍幾張影格；推論反覆使用這一對影格的特徵。迭代與解析度會影響運算及記憶體。

因為：由本課核心機制連接工作用途

設定核對：WI028-2

3
交出稠密光流

每個像素的水平／垂直影像位移估計。本課箭頭只是示意；物件身分、速度、形變量測與事件判定要另有分群、校正與驗證。

因為：由本課核心機制連接工作用途

設定核對：WI028-3

4
工作核對與接手

在相同影片和校對位置比較位移誤差、有效覆蓋、遮擋表現、完整耗時和記憶體；圖中無現場模型成績。

因為：由本課核心機制連接工作用途

設定核對：WI028-4

想深入：展開失效案例、模型比較與小規模試做（POC）
初學者可先跳過；準備實作或驗收時再逐項核對。

失效案例、選型與小規模試做

什麼情況不能相信它？

這一區保留完整工程條件，供準備實作或驗收的人逐項核對。

本課具體反例

可能看到：金屬反光或遮擋讓某些像素在下一影格沒有可靠可見對應；RAFT仍可能輸出數值，但這是估計，不是觀測真值。

正確下一步：回原片查遮擋與反光，搭配前後向或任務檢查標出不可靠區；不要把平滑光流直接當毫米級量測。

更換產品、模型或輸入設定

可能看到：先固定具體權重、前處理、影像解析度與更新次數。換相機／材質需測域落差，必要時另準備配對監督資料微調；同看誤差、遮擋錯誤、記憶體與端到端耗時。

正確下一步：先收集成對且有時間戳的影片，選定原始RAFT權重與解析度，準備可信點對／已知移動作驗證。

把示意當成實際推論

可能看到：本課特徵、排序、回答及圖像僅用於解釋，不支持現場準確率或耗時結論。

正確下一步：在相同影片和校對位置比較位移誤差、有效覆蓋、遮擋表現、完整耗時和記憶體；圖中無現場模型成績。

相鄰方法比較

少量追點，還是整張位移？
方法	所需資料與方法差異	輸出與維護責任
Lucas–Kanade稀疏追點	不需權重；選角點、窗口、金字塔與幀間隔	選定點位移；需檢查追丟與錯配
原始RAFT	預訓練權重、相容前處理、迭代與運算預算	稠密像素位移；遮擋仍可能不可靠
傳統稠密光流基準	如Farnebäck；設定尺度和局部估計參數	稠密位移；同影片比較誤差、覆蓋及成本

需要全圖位移時比較RAFT與傳統稠密光流；只需少量角點可先試LK。RAFT要權重與較多運算預算，是否值得取決於域內位移品質和下游任務。 先固定具體權重、前處理、影像解析度與更新次數。換相機／材質需測域落差，必要時另準備配對監督資料微調；同看誤差、遮擋錯誤、記憶體與端到端耗時。

工程第一步

以真實工作資料做小規模比較
先收集成對且有時間戳的影片，選定原始RAFT權重與解析度，準備可信點對／已知移動作驗證。
先固定具體權重、前處理、影像解析度與更新次數。換相機／材質需測域落差，必要時另準備配對監督資料微調；同看誤差、遮擋錯誤、記憶體與端到端耗時。
在相同影片和校對位置比較位移誤差、有效覆蓋、遮擋表現、完整耗時和記憶體；圖中無現場模型成績。
目前做法

既有流程或最簡單可行方法

要量什麼

在相同影片和校對位置比較位移誤差、有效覆蓋、遮擋表現、完整耗時和記憶體；圖中無現場模型成績。

反例測試

金屬反光或遮擋讓某些像素在下一影格沒有可靠可見對應；RAFT仍可能輸出數值，但這是估計，不是觀測真值。

誰確認

實作與影像核對負責人

先停下來的訊號：回原片查遮擋與反光，搭配前後向或任務檢查標出不可靠區；不要把平滑光流直接當毫米級量測。

讀完請自己講回來

它改變什麼？何時選另一種做法？
看本頁練習的核對說明

原始 RAFT 先對兩張影格各自抽特徵，建立所有位置配對的相似線索；再反覆查詢多尺度相關性，用更新單元修正目前的光流估計。全配對提供廣泛的候選位置，反覆更新把這些線索逐步轉成每個像素的二維位移。 需要全圖位移時比較RAFT與傳統稠密光流；只需少量角點可先試LK。RAFT要權重與較多運算預算，是否值得取決於域內位移品質和下游任務。

換一個現場再推理

換成只需幾個固定角點

情境：現場CPU即可，工作只需三個角點是否偏移，位移很小、紋理清楚。

請先回答：為了稠密輸出一定要選RAFT嗎？

核對這個遷移題

不必；先以LK或既有對位法作基準，測角點誤差與漂移。只有下游確實需要更廣位移覆蓋，且效果改善值得運算代價時，再採用RAFT。

內容依據：展開這頁主張的 model.md 可追溯範圍

roadmap-model-selection/video-temporal/raft/model.md ↗

WI-033第二批：四工程圖重排，光流共用條件反例、AnomalyDINO旋轉深讀及EfficientAD同件反例／指定深讀修正；頁內與使用者審閱另記。

beginner_path.visuals[same-conveyor-frame-pair]：模型定位 / architecture_path
beginner_path.visuals[pair-and-encoders]：architecture_path
beginner_path.visuals[all-pairs-correlation]：representation_or_score
beginner_path.visuals[recurrent-dense-flow]：failure_boundary
beginner_path.visuals[declared-reject-and-choice]：selection_gate / evidence_bundle
promise：模型定位 / representation_or_score
mechanism_steps：architecture_path / representation_or_score
boundary_cases：failure_boundary / selection_gate
comparison + selection：selection_gate
poc + acceptance：cost_and_operating_point / evidence_bundle
transfer_check：failure_boundary / selection_gate
beginner_path.visuals[0] + scenario + mental_model：roadmap-model-selection/video-temporal/raft/model.md
micro_example + transfer_check + selection：roadmap-model-selection/video-temporal/raft/model.md
beginner_path + core + selection + transfer：https://www.ecva.net/papers/eccv_2020/papers_ECCV/papers/123470392.pdf
工程參考：展開四張工程圖與完整節點
從輸入、機制、交付到限制，核對實作與驗證方式。

原始 RAFT 先對兩張影格各自抽特徵，建立所有位置配對的相似線索；再反覆查詢多尺度相關性，用更新單元修正目前的光流估計。全配對提供廣泛的候選位置，反覆更新把這些線索逐步轉成每個像素的二維位移。

01
先看它交出什麼
02
再看它怎麼做
03
上線前先固定條件
04
選它前先看風險
01

先看它交出什麼

RAFT：先把兩張影格变成配對線索

兩張圖建立相關性，第一張圖另提供更新情境。

正式教學插圖 1/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

兩張圖建立相關性，第一張圖另提供更新情境。
先看哪裡

先看固定影格對與模型版本，再核對兩圖特徵建立全配對，最後看第一圖提供更新情境。

框與框的關係

固定原始RAFT權重、解析度、padding和迭代數。共享特徵網路抽兩圖表示後，建立全位置配對相關性及多尺度池化；第一圖經context encoder提供隱狀態與情境。相關性是候選配對線索，不是最終光流或每個像素的可靠度。

本圖只要帶走

兩張圖建立相關性，第一張圖另提供更新情境。

相關性：兩張影格特徵位置間的配對相似線索。
更新量：更新單元對目前光流提出的位移修正。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
固定原始RAFT權重、解析度、padding和迭代數。共享特徵網路抽兩圖表示後，建立全位置配對相關性及多尺度池化；第一圖經context encoder提供隱狀態與情境。相關性是候選配對線索，不是最終光流或每個像素的可靠度。
先看固定影格對與模型版本，再核對兩圖特徵建立全配對，最後看第一圖提供更新情境。
技術上不可誤讀
金屬反光或遮擋讓某些像素在下一影格沒有可靠可見對應；RAFT仍可能輸出數值，但這是估計，不是觀測真值。

完整節點

原始工程節點
1
固定影格對與模型版本

固定影格對與模型版本；請對照圖內輸入、變化與結果。

2
兩圖特徵建立全配對

兩圖特徵建立全配對；請對照圖內輸入、變化與結果。

3
第一圖提供更新情境

第一圖提供更新情境；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看固定影格對與模型版本，再核對兩圖特徵建立全配對，最後看第一圖提供更新情境。

怎麼理解

固定原始RAFT權重、解析度、padding和迭代數。共享特徵網路抽兩圖表示後，建立全位置配對相關性及多尺度池化；第一圖經context encoder提供隱狀態與情境。相關性是候選配對線索，不是最終光流或每個像素的可靠度。

不要誤用

回原片查遮擋與反光，搭配前後向或任務檢查標出不可靠區；不要把平滑光流直接當毫米級量測。

繼續看第 2 張：再看它怎麼做 ↓

02

再看它怎麼做

RAFT：依目前位置查詢，再修正光流

用目前光流查相關性，更新量加回後繼續修正。

正式教學插圖 2/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

用目前光流查相關性，更新量加回後繼續修正。
先看哪裡

先看在目前估計周圍查線索，再核對更新單元融合四種資訊，最後看加上更新量，再查下一輪。

框與框的關係

原始RAFT以目前座標查詢多尺度相關性；更新單元接相關性、目前光流、context與隱狀態，輸出位移增量並更新隱狀態。新光流=舊光流+增量，固定解析度反覆更新，最後上採樣。圖中(1,0)+(1,1)=(2,1)只解釋更新算術，不是實際模型收斂或逐輪保證改善。

本圖只要帶走

用目前光流查相關性，更新量加回後繼續修正。

相關性：兩張影格特徵位置間的配對相似線索。
更新量：更新單元對目前光流提出的位移修正。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
原始RAFT以目前座標查詢多尺度相關性；更新單元接相關性、目前光流、context與隱狀態，輸出位移增量並更新隱狀態。新光流=舊光流+增量，固定解析度反覆更新，最後上採樣。圖中(1,0)+(1,1)=(2,1)只解釋更新算術，不是實際模型收斂或逐輪保證改善。
先看在目前估計周圍查線索，再核對更新單元融合四種資訊，最後看加上更新量，再查下一輪。
技術上不可誤讀
金屬反光或遮擋讓某些像素在下一影格沒有可靠可見對應；RAFT仍可能輸出數值，但這是估計，不是觀測真值。

完整節點

原始工程節點
1
在目前估計周圍查線索

在目前估計周圍查線索；請對照圖內輸入、變化與結果。

2
更新單元融合四種資訊

更新單元融合四種資訊；請對照圖內輸入、變化與結果。

3
加上更新量，再查下一輪

加上更新量，再查下一輪；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看在目前估計周圍查線索，再核對更新單元融合四種資訊，最後看加上更新量，再查下一輪。

怎麼理解

原始RAFT以目前座標查詢多尺度相關性；更新單元接相關性、目前光流、context與隱狀態，輸出位移增量並更新隱狀態。新光流=舊光流+增量，固定解析度反覆更新，最後上採樣。圖中(1,0)+(1,1)=(2,1)只解釋更新算術，不是實際模型收斂或逐輪保證改善。

不要誤用

回原片查遮擋與反光，搭配前後向或任務檢查標出不可靠區；不要把平滑光流直接當毫米級量測。

繼續看第 3 張：上線前先固定條件 ↓

03

上線前先固定條件

RAFT：稠密數值要配合有效性核對

輸出向量不等於看見對應，遮擋區要另驗。

正式教學插圖 3/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

輸出向量不等於看見對應，遮擋區要另驗。
先看哪裡

先看同一工件輸出稠密場，再核對反光與遮擋另查一致性，最後看算進相關性與迭代成本。

框與框的關係

保存兩影格、原尺寸、前處理、權重、迭代設定與位移場；遮擋/反射可能沒有可見對應但仍輸出數值。可用往返一致性作應用檢查，仍不是遮擋真值或原始RAFT原生置信輸出。完整成本包含特徵、相關性、更新和上採樣，並量記憶體。

本圖只要帶走

輸出向量不等於看見對應，遮擋區要另驗。

相關性：兩張影格特徵位置間的配對相似線索。
更新量：更新單元對目前光流提出的位移修正。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
保存兩影格、原尺寸、前處理、權重、迭代設定與位移場；遮擋/反射可能沒有可見對應但仍輸出數值。可用往返一致性作應用檢查，仍不是遮擋真值或原始RAFT原生置信輸出。完整成本包含特徵、相關性、更新和上採樣，並量記憶體。
先看同一工件輸出稠密場，再核對反光與遮擋另查一致性，最後看算進相關性與迭代成本。
技術上不可誤讀
金屬反光或遮擋讓某些像素在下一影格沒有可靠可見對應；RAFT仍可能輸出數值，但這是估計，不是觀測真值。

完整節點

原始工程節點
1
同一工件輸出稠密場

同一工件輸出稠密場；請對照圖內輸入、變化與結果。

2
反光與遮擋另查一致性

反光與遮擋另查一致性；請對照圖內輸入、變化與結果。

3
算進相關性與迭代成本

算進相關性與迭代成本；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看同一工件輸出稠密場，再核對反光與遮擋另查一致性，最後看算進相關性與迭代成本。

怎麼理解

保存兩影格、原尺寸、前處理、權重、迭代設定與位移場；遮擋/反射可能沒有可見對應但仍輸出數值。可用往返一致性作應用檢查，仍不是遮擋真值或原始RAFT原生置信輸出。完整成本包含特徵、相關性、更新和上採樣，並量記憶體。

不要誤用

回原片查遮擋與反光，搭配前後向或任務檢查標出不可靠區；不要把平滑光流直接當毫米級量測。

繼續看第 4 張：選它前先看風險 ↓

04

選它前先看風險

RAFT：增加迭代是否值得，要同題量

迭代設定改變成本，效果仍用同一資料驗證。

正式教學插圖 4/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

迭代設定改變成本，效果仍用同一資料驗證。
先看哪裡

先看同資料與同一初始設定，再核對較少迭代的候選，最後看較多迭代的候選。

框與框的關係

固定同一權重、影格對、解析度、precision和硬體，只改更新次數；在同一有效性標註上比较錯誤、覆蓋與耗時，不能保證更多迭代一定改善，也不能把彩色光流當精度證據。保留較少迭代也可能合理。

本圖只要帶走

迭代設定改變成本，效果仍用同一資料驗證。

相關性：兩張影格特徵位置間的配對相似線索。
更新量：更新單元對目前光流提出的位移修正。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
固定同一權重、影格對、解析度、precision和硬體，只改更新次數；在同一有效性標註上比较錯誤、覆蓋與耗時，不能保證更多迭代一定改善，也不能把彩色光流當精度證據。保留較少迭代也可能合理。
先看同資料與同一初始設定，再核對較少迭代的候選，最後看較多迭代的候選。
技術上不可誤讀
金屬反光或遮擋讓某些像素在下一影格沒有可靠可見對應；RAFT仍可能輸出數值，但這是估計，不是觀測真值。

完整節點

原始工程節點
1
同資料與同一初始設定

同資料與同一初始設定；請對照圖內輸入、變化與結果。

2
較少迭代的候選

較少迭代的候選；請對照圖內輸入、變化與結果。

3
較多迭代的候選

較多迭代的候選；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看同資料與同一初始設定，再核對較少迭代的候選，最後看較多迭代的候選。

怎麼理解

固定同一權重、影格對、解析度、precision和硬體，只改更新次數；在同一有效性標註上比较錯誤、覆蓋與耗時，不能保證更多迭代一定改善，也不能把彩色光流當精度證據。保留較少迭代也可能合理。

不要誤用

回原片查遮擋與反光，搭配前後向或任務檢查標出不可靠區；不要把平滑光流直接當毫米級量測。

工程師操作卡

把 RAFT 放進小規模試做（POC）前，先回答這五個問題。

先讀白話版就能開始溝通；需要實作、驗證或交接時，再展開下方技術契約。

1
它會交出什麼？

每個像素的水平／垂直影像位移估計。本課箭頭只是示意；物件身分、速度、形變量測與事件判定要另有分群、校正與驗證。

2
開始先做什麼？

先收集成對且有時間戳的影片，選定原始RAFT權重與解析度，準備可信點對／已知移動作驗證。

3
開始前要寫下來的四件事

固定畫面範圍、資料來源、模型版本和驗收方式；詳細項目可在下方展開。

4
怎樣才算真的有用？

在相同影片和校對位置比較位移誤差、有效覆蓋、遮擋表現、完整耗時和記憶體；圖中無現場模型成績。

5
碰到什麼先停下來？

回原片查遮擋與反光，搭配前後向或任務檢查標出不可靠區；不要把平滑光流直接當毫米級量測。

工程師需要時，再展開原始技術契約

原始建置項目

這些設定要一併保存

輸入與版本：先收集成對且有時間戳的影片，選定原始RAFT權重與解析度，準備可信點對／已知移動作驗證。；關鍵機制：原始 RAFT 先對兩張影格各自抽特徵，建立所有位置配對的相似線索；再反覆查詢多尺度相關性，用更新單元修正目前的光流估計。全配對提供廣泛的候選位置，反覆更新把這些線索逐步轉成每個像素的二維位移。；獨立驗證：在相同影片和校對位置比較位移誤差、有效覆蓋、遮擋表現、完整耗時和記憶體；圖中無現場模型成績。；失敗處理：回原片查遮擋與反光，搭配前後向或任務檢查標出不可靠區；不要把平滑光流直接當毫米級量測。

原始選型條件：需要全圖位移時比較RAFT與傳統稠密光流；只需少量角點可先試LK。RAFT要權重與較多運算預算，是否值得取決於域內位移品質和下游任務。 先固定具體權重、前處理、影像解析度與更新次數。換相機／材質需測域落差，必要時另準備配對監督資料微調；同看誤差、遮擋錯誤、記憶體與端到端耗時。

原始失效邊界

下列情況不可忽略

金屬反光或遮擋讓某些像素在下一影格沒有可靠可見對應；RAFT仍可能輸出數值，但這是估計，不是觀測真值。

工程結論
每個像素的水平／垂直影像位移估計。本課箭頭只是示意；物件身分、速度、形變量測與事件判定要另有分群、校正與驗證。
把這張操作卡帶入 POC 畫布 →
查看 model.md ↗
查看圖檔 manifest ↗

相鄰比較

下一個可比較的模型

比較前，先把「看哪段畫面、用哪些資料、交出什麼、多久完成、誰覆核」固定；再到家族頁看共同條件與差異。

Frame Difference →
Background Subtraction →
Lucas–Kanade →
ByteTrack →
查看家族比較 →
← Lucas–Kanade
ByteTrack →