首頁
/
已知目標：分類、分割與姿態
/
YOLO-Seg
◫ 已知目標：分類、分割與姿態
先理解，再看工程圖
YOLO-Seg

輸送帶有兩個同類墊圈，需要分開每一件再計數，不能只知道哪些像素是墊圈。

你會拿到：每件框、類別與獨立遮罩，供逐件核對和計數；A/B是實例ID，不代表不同產品類別或跨影格身分。
先停下來：逐件核對原圖和標註，補足重疊樣本，必要時調整取像視角或分料，再用未見資料重測漏件和錯誤分件。
完成本主題
☆ 收藏
從案例理解 YOLO-Seg
從必要原理、具體反例到工作取捨

輸送帶有兩個同類墊圈，需要分開每一件再計數，不能只知道哪些像素是墊圈。 下列圖像、特徵及回答為教學示意，不是模型實測。

第 01 張 · 看案例，再做判斷

YOLO-Seg：每個物件，都有自己的遮罩

輸送帶有兩個同類墊圈，需要分開每一件再計數，不能只知道哪些像素是墊圈。

YOLO-Seg：每個物件，都有自己的遮罩
放大完整圖
為什麼需要這個設計？

以YOLOv8-seg機制為例，共享特徵產生一組原型遮罩，偵測分支為每件預測框、類別及遮罩係數。每件係數組合共享原型，再依框處理成該件遮罩。共享原型不是一張完整墊圈答案；兩件同類也要有分開的實例輸出。

讀完要能說清楚的三個要點
共享原型、各件係數

原型提供共同基底，A和B各自一組係數形成各自遮罩；兩路不是先算A再把剩下區域當B。

先偵測到才能分件

輸出依賴偵測與遮罩品質，重疊、遮擋及小物件可造成漏檢；計數另需定義可見與允收規則。

交付與接手

每件框、類別與獨立遮罩，供逐件核對和計數；A/B是實例ID，不代表不同產品類別或跨影格身分。

想深入，再看這張圖的完整工程說明
YOLO-Seg：每個物件，都有自己的遮罩

原始問題：輸送帶有兩個同類墊圈，需要分開每一件再計數，不能只知道哪些像素是墊圈。

以YOLOv8-seg機制為例，共享特徵產生一組原型遮罩，偵測分支為每件預測框、類別及遮罩係數。每件係數組合共享原型，再依框處理成該件遮罩。共享原型不是一張完整墊圈答案；兩件同類也要有分開的實例輸出。

共享原型、各件係數

原型提供共同基底，A和B各自一組係數形成各自遮罩；兩路不是先算A再把剩下區域當B。

先偵測到才能分件

輸出依賴偵測與遮罩品質，重疊、遮擋及小物件可造成漏檢；計數另需定義可見與允收規則。

交付與接手

每件框、類別與獨立遮罩，供逐件核對和計數；A/B是實例ID，不代表不同產品類別或跨影格身分。

第 02 張 · 看案例，再做判斷

兩件靠在一起，遮罩可能合錯或漏掉

觀察哪個條件改變，讓原本的結果不夠可靠？

兩件靠在一起，遮罩可能合錯或漏掉
放大完整圖
看哪裡

兩個墊圈稍微重疊，模型可能只交出一件遮罩。原圖仍有兩件；人工圈出的漏件區不是模型的第二個偵測。

接著怎麼做

逐件核對原圖和標註，補足重疊樣本，必要時調整取像視角或分料，再用未見資料重測漏件和錯誤分件。

想深入，再看這張圖的完整工程說明
兩件靠在一起，遮罩可能合錯或漏掉

原始問題：觀察哪個條件改變，讓原本的結果不夠可靠？

兩個墊圈稍微重疊，模型可能只交出一件遮罩。原圖仍有兩件；人工圈出的漏件區不是模型的第二個偵測。

看哪裡

兩個墊圈稍微重疊，模型可能只交出一件遮罩。原圖仍有兩件；人工圈出的漏件區不是模型的第二個偵測。

接著怎麼做

逐件核對原圖和標註，補足重疊樣本，必要時調整取像視角或分料，再用未見資料重測漏件和錯誤分件。

第 03 張 · 看案例，再做判斷

要同類區域，還是每件各一張遮罩？

相同工作需求下，資料、輸出與成本如何取捨？

要同類區域，還是每件各一張遮罩？
放大完整圖
怎麼選

U-Net和SegFormer提供語意分割：同類像素用同類標籤，未必區分每一件。YOLO-Seg提供實例分割：每件有自己的遮罩。先決定要量區域還是逐件計數，再比較模型與標註成本。

資料與完整成本

需每件實例標註和相應版本權重；本課以YOLOv8-seg為例，不泛化聲稱所有YOLO版本都相同。固定解析度、門檻及後處理，測每件漏檢、重疊和推論成本。

想深入，再看這張圖的完整工程說明
要同類區域，還是每件各一張遮罩？

原始問題：相同工作需求下，資料、輸出與成本如何取捨？

U-Net和SegFormer提供語意分割：同類像素用同類標籤，未必區分每一件。YOLO-Seg提供實例分割：每件有自己的遮罩。先決定要量區域還是逐件計數，再比較模型與標註成本。

怎麼選

U-Net和SegFormer提供語意分割：同類像素用同類標籤，未必區分每一件。YOLO-Seg提供實例分割：每件有自己的遮罩。先決定要量區域還是逐件計數，再比較模型與標註成本。

資料與完整成本

需每件實例標註和相應版本權重；本課以YOLOv8-seg為例，不泛化聲稱所有YOLO版本都相同。固定解析度、門檻及後處理，測每件漏檢、重疊和推論成本。

換情境試著判斷

補模型資料，還是改分料？

兩個相接墊圈常只被算成一件。可增加重疊標註訓練，也可用分料或調整視角讓兩件更容易看清。

你會先做哪一項小試驗？何時保留兩者一起做？

看解釋與可接受的取捨

先檢查人是否能從原圖穩定分辨兩件。可見線索足夠而資料不足時先補重疊樣本；遮擋讓身份難辨時改善分料／視角更直接。兩者都有問題可合用，但各自量成本與效益。驗證逐件召回、錯誤合併／切分、計數誤差和節拍，面積估數只能作另行驗證的受限方案。

需要時查看基本名詞與模型背景
每件獨立的實例分割
共享原型、各件係數：原型提供共同基底，A和B各自一組係數形成各自遮罩；兩路不是先算A再把剩下區域當B。
先偵測到才能分件：輸出依賴偵測與遮罩品質，重疊、遮擋及小物件可造成漏檢；計數另需定義可見與允收規則。
每件獨立的實例分割

以YOLOv8-seg機制為例，共享特徵產生一組原型遮罩，偵測分支為每件預測框、類別及遮罩係數。每件係數組合共享原型，再依框處理成該件遮罩。共享原型不是一張完整墊圈答案；兩件同類也要有分開的實例輸出。

兩個墊圈稍微重疊，模型可能只交出一件遮罩。原圖仍有兩件；人工圈出的漏件區不是模型的第二個偵測。

工程參考：展開文字因果鏈與設定核對點不重複主圖；只核對每步要鎖定的設定。

模型專屬因果鏈

實作時逐步核對

主圖已在上方第一遍。這裡只核對每一步要鎖定的設定與為什麼，不再重貼同一張圖。

1
先有實例標註

原型提供共同基底，A和B各自一組係數形成各自遮罩；兩路不是先算A再把剩下區域當B。

因為：由本課核心機制連接工作用途

設定核對：WI030-1

2
共享底圖，各自組合

輸出依賴偵測與遮罩品質，重疊、遮擋及小物件可造成漏檢；計數另需定義可見與允收規則。

因為：由本課核心機制連接工作用途

設定核對：WI030-2

3
分開每一件

每件框、類別與獨立遮罩，供逐件核對和計數；A/B是實例ID，不代表不同產品類別或跨影格身分。

因為：由本課核心機制連接工作用途

設定核對：WI030-3

4
工作核對與接手

保存真實原圖與逐件標註，量物件召回、錯誤合併／切分、遮罩品質和計數誤差；孔洞保留需求另驗，示意圖不是模型結果。

因為：由本課核心機制連接工作用途

設定核對：WI030-4

想深入：展開失效案例、模型比較與小規模試做（POC）
初學者可先跳過；準備實作或驗收時再逐項核對。

失效案例、選型與小規模試做

什麼情況不能相信它？

這一區保留完整工程條件，供準備實作或驗收的人逐項核對。

本課具體反例

可能看到：兩個墊圈稍微重疊，模型可能只交出一件遮罩。原圖仍有兩件；人工圈出的漏件區不是模型的第二個偵測。

正確下一步：逐件核對原圖和標註，補足重疊樣本，必要時調整取像視角或分料，再用未見資料重測漏件和錯誤分件。

更換產品、模型或輸入設定

可能看到：需每件實例標註和相應版本權重；本課以YOLOv8-seg為例，不泛化聲稱所有YOLO版本都相同。固定解析度、門檻及後處理，測每件漏檢、重疊和推論成本。

正確下一步：先決定遮擋物件如何標註與計數，收單件、相鄰、重疊和空畫面，分開批次驗證。

把示意當成實際推論

可能看到：本課特徵、排序、回答及圖像僅用於解釋，不支持現場準確率或耗時結論。

正確下一步：保存真實原圖與逐件標註，量物件召回、錯誤合併／切分、遮罩品質和計數誤差；孔洞保留需求另驗，示意圖不是模型結果。

相鄰方法比較

要同類區域，還是每件各一張遮罩？
方法	所需資料與方法差異	輸出與維護責任
U-Net	像素標註；編碼解碼同尺度跳接	語意區域；邊界與尺度需另驗
SegFormer	像素標註；階層特徵多尺度融合	語意區域；不自動提供每件ID
YOLOv8-seg例	每件標註；原型＋每件係數	每件框與遮罩；漏檢重疊逐件驗

U-Net和SegFormer提供語意分割：同類像素用同類標籤，未必區分每一件。YOLO-Seg提供實例分割：每件有自己的遮罩。先決定要量區域還是逐件計數，再比較模型與標註成本。 需每件實例標註和相應版本權重；本課以YOLOv8-seg為例，不泛化聲稱所有YOLO版本都相同。固定解析度、門檻及後處理，測每件漏檢、重疊和推論成本。

工程第一步

以真實工作資料做小規模比較
先決定遮擋物件如何標註與計數，收單件、相鄰、重疊和空畫面，分開批次驗證。
需每件實例標註和相應版本權重；本課以YOLOv8-seg為例，不泛化聲稱所有YOLO版本都相同。固定解析度、門檻及後處理，測每件漏檢、重疊和推論成本。
保存真實原圖與逐件標註，量物件召回、錯誤合併／切分、遮罩品質和計數誤差；孔洞保留需求另驗，示意圖不是模型結果。
目前做法

既有流程或最簡單可行方法

要量什麼

保存真實原圖與逐件標註，量物件召回、錯誤合併／切分、遮罩品質和計數誤差；孔洞保留需求另驗，示意圖不是模型結果。

反例測試

兩個墊圈稍微重疊，模型可能只交出一件遮罩。原圖仍有兩件；人工圈出的漏件區不是模型的第二個偵測。

誰確認

實作與影像核對負責人

先停下來的訊號：逐件核對原圖和標註，補足重疊樣本，必要時調整取像視角或分料，再用未見資料重測漏件和錯誤分件。

讀完請自己講回來

它改變什麼？何時選另一種做法？
看本頁練習的核對說明

以YOLOv8-seg機制為例，共享特徵產生一組原型遮罩，偵測分支為每件預測框、類別及遮罩係數。每件係數組合共享原型，再依框處理成該件遮罩。共享原型不是一張完整墊圈答案；兩件同類也要有分開的實例輸出。 U-Net和SegFormer提供語意分割：同類像素用同類標籤，未必區分每一件。YOLO-Seg提供實例分割：每件有自己的遮罩。先決定要量區域還是逐件計數，再比較模型與標註成本。

換一個現場再推理

補模型資料，還是改分料？

情境：兩個相接墊圈常只被算成一件。可增加重疊標註訓練，也可用分料或調整視角讓兩件更容易看清。

請先回答：你會先做哪一項小試驗？何時保留兩者一起做？

核對這個遷移題

先檢查人是否能從原圖穩定分辨兩件。可見線索足夠而資料不足時先補重疊樣本；遮擋讓身份難辨時改善分料／視角更直接。兩者都有問題可合用，但各自量成本與效益。驗證逐件召回、錯誤合併／切分、計數誤差和節拍，面積估數只能作另行驗證的受限方案。

內容依據：展開這頁主張的 model.md 可追溯範圍

roadmap-model-selection/known-target-classification/yolo-seg/model.md ↗

WI-033：工程層四圖與手機比較修正；首讀有效內容保留。原生圖已審，整頁與使用者審閱另記。

beginner_path.visuals[pcb-instance-anchor]：模型定位 / architecture_path
beginner_path.visuals[instance-label-contract]：architecture_path
beginner_path.visuals[dense-fpn-prototypes]：representation_or_score
beginner_path.visuals[decode-nms-compose-review]：failure_boundary
beginner_path.visuals[operating-boundary-selection]：selection_gate / evidence_bundle
promise：模型定位 / representation_or_score
mechanism_steps：architecture_path / representation_or_score
boundary_cases：failure_boundary / selection_gate
comparison + selection：selection_gate
poc + acceptance：cost_and_operating_point / evidence_bundle
transfer_check：failure_boundary / selection_gate
beginner_path + core + selection + transfer：https://github.com/ultralytics/ultralytics/blob/v8.3.0/ultralytics/nn/modules/head.py
工程參考：展開四張工程圖與完整節點
從輸入、機制、交付到限制，核對實作與驗證方式。

以YOLOv8-seg機制為例，共享特徵產生一組原型遮罩，偵測分支為每件預測框、類別及遮罩係數。每件係數組合共享原型，再依框處理成該件遮罩。共享原型不是一張完整墊圈答案；兩件同類也要有分開的實例輸出。

01
先看它交出什麼
02
再看它怎麼做
03
上線前先固定條件
04
選它前先看風險
01

先看它交出什麼

YOLO-Seg：同類零件也要各有一張遮罩

每件有框、類別與遮罩，身份只屬當前影像。

正式教學插圖 1/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

每件有框、類別與遮罩，身份只屬當前影像。
先看哪裡

先看两件同類支架各自標註，再核對共享影像特徵產生兩路，最後看每件係數組合出自己的遮罩。

框與框的關係

以YOLOv8式prototype和mask coefficients路徑說明。訓練需各實例的類別與輪廓，推論保留各物件框、分數和實例遮罩；圖中甲乙只是這張影像的實例，不是跨影格追蹤ID。

本圖只要帶走

每件有框、類別與遮罩，身份只屬當前影像。

特徵：由影像學到的中間表示，並非標註真值。
本圖示意：解釋模型責任，不代表新模型實測。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
以YOLOv8式prototype和mask coefficients路徑說明。訓練需各實例的類別與輪廓，推論保留各物件框、分數和實例遮罩；圖中甲乙只是這張影像的實例，不是跨影格追蹤ID。
先看两件同類支架各自標註，再核對共享影像特徵產生兩路，最後看每件係數組合出自己的遮罩。
技術上不可誤讀
兩個墊圈稍微重疊，模型可能只交出一件遮罩。原圖仍有兩件；人工圈出的漏件區不是模型的第二個偵測。

完整節點

原始工程節點
1
两件同類支架各自標註

两件同類支架各自標註；請對照圖內輸入、變化與結果。

2
共享影像特徵產生兩路

共享影像特徵產生兩路；請對照圖內輸入、變化與結果。

3
每件係數組合出自己的遮罩

每件係數組合出自己的遮罩；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看两件同類支架各自標註，再核對共享影像特徵產生兩路，最後看每件係數組合出自己的遮罩。

怎麼理解

以YOLOv8式prototype和mask coefficients路徑說明。訓練需各實例的類別與輪廓，推論保留各物件框、分數和實例遮罩；圖中甲乙只是這張影像的實例，不是跨影格追蹤ID。

不要誤用

逐件核對原圖和標註，補足重疊樣本，必要時調整取像視角或分料，再用未見資料重測漏件和錯誤分件。

繼續看第 2 張：再看它怎麼做 ↓

02

再看它怎麼做

YOLO-Seg：共用原型，每件用不同係數

原型乘各件係數，再依物件框取回遮罩。

正式教學插圖 2/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

原型乘各件係數，再依物件框取回遮罩。
先看哪裡

先看共享特徵產生原型圖，再核對每個候選各有係數，最後看組合、裁框、映回影像。

框與框的關係

檢測頭預測框/類別和每個候選的mask coefficients；Proto支路產生共享basis maps。保留候選的係數線性組合原型，经相應後處理與裁框映回。兩原型是簡化代數例，實際通道數與後處理依所用版本；不能把每張原型直接叫某件物體。

本圖只要帶走

原型乘各件係數，再依物件框取回遮罩。

特徵：由影像學到的中間表示，並非標註真值。
本圖示意：解釋模型責任，不代表新模型實測。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
檢測頭預測框/類別和每個候選的mask coefficients；Proto支路產生共享basis maps。保留候選的係數線性組合原型，经相應後處理與裁框映回。兩原型是簡化代數例，實際通道數與後處理依所用版本；不能把每張原型直接叫某件物體。
先看共享特徵產生原型圖，再核對每個候選各有係數，最後看組合、裁框、映回影像。
技術上不可誤讀
兩個墊圈稍微重疊，模型可能只交出一件遮罩。原圖仍有兩件；人工圈出的漏件區不是模型的第二個偵測。

完整節點

原始工程節點
1
共享特徵產生原型圖

共享特徵產生原型圖；請對照圖內輸入、變化與結果。

2
每個候選各有係數

每個候選各有係數；請對照圖內輸入、變化與結果。

3
組合、裁框、映回影像

組合、裁框、映回影像；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看共享特徵產生原型圖，再核對每個候選各有係數，最後看組合、裁框、映回影像。

怎麼理解

檢測頭預測框/類別和每個候選的mask coefficients；Proto支路產生共享basis maps。保留候選的係數線性組合原型，经相應後處理與裁框映回。兩原型是簡化代數例，實際通道數與後處理依所用版本；不能把每張原型直接叫某件物體。

不要誤用

逐件核對原圖和標註，補足重疊樣本，必要時調整取像視角或分料，再用未見資料重測漏件和錯誤分件。

繼續看第 3 張：上線前先固定條件 ↓

03

上線前先固定條件

YOLO-Seg：遮罩要和同一個框一起交付

把框、類別與遮罩綁定，映回原圖再核對。

正式教學插圖 3/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

把框、類別與遮罩綁定，映回原圖再核對。
先看哪裡

先看候選框與係數保持配對，再核對去重保留後產生對應遮罩，最後看原圖檢查孔洞與相鄰件。

框與框的關係

遵循實作的候選過濾與去重，索引必須同步框、類別、係數，不能將甲框配乙mask。映回需還原letterbox，輪廓不能填掉空孔；定位與量測仍須另外驗證，遮罩不是亞像素尺寸真值。

本圖只要帶走

把框、類別與遮罩綁定，映回原圖再核對。

特徵：由影像學到的中間表示，並非標註真值。
本圖示意：解釋模型責任，不代表新模型實測。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
遵循實作的候選過濾與去重，索引必須同步框、類別、係數，不能將甲框配乙mask。映回需還原letterbox，輪廓不能填掉空孔；定位與量測仍須另外驗證，遮罩不是亞像素尺寸真值。
先看候選框與係數保持配對，再核對去重保留後產生對應遮罩，最後看原圖檢查孔洞與相鄰件。
技術上不可誤讀
兩個墊圈稍微重疊，模型可能只交出一件遮罩。原圖仍有兩件；人工圈出的漏件區不是模型的第二個偵測。

完整節點

原始工程節點
1
候選框與係數保持配對

候選框與係數保持配對；請對照圖內輸入、變化與結果。

2
去重保留後產生對應遮罩

去重保留後產生對應遮罩；請對照圖內輸入、變化與結果。

3
原圖檢查孔洞與相鄰件

原圖檢查孔洞與相鄰件；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看候選框與係數保持配對，再核對去重保留後產生對應遮罩，最後看原圖檢查孔洞與相鄰件。

怎麼理解

遵循實作的候選過濾與去重，索引必須同步框、類別、係數，不能將甲框配乙mask。映回需還原letterbox，輪廓不能填掉空孔；定位與量測仍須另外驗證，遮罩不是亞像素尺寸真值。

不要誤用

逐件核對原圖和標註，補足重疊樣本，必要時調整取像視角或分料，再用未見資料重測漏件和錯誤分件。

繼續看第 4 張：選它前先看風險 ↓

04

選它前先看風險

語意與實例分割：同類是否需要分成兩件

需要逐件交付時，比較實例標註與分離品質。

正式教學插圖 4/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

需要逐件交付時，比較實例標註與分離品質。
先看哪裡

先看同一影像有兩件支架，再核對語意：兩件都屬支架類，最後看實例：甲乙各有遮罩。

框與框的關係

共同输入与物件外观不变。语意遮罩同色表示同類，不一定提供逐件身份；實例分割分出甲乙，可支援後續逐件統計。重疊與遮擋仍可能分錯，需要標註與域內驗證；不聲稱必然比語意分割更適合所有工作。

本圖只要帶走

需要逐件交付時，比較實例標註與分離品質。

特徵：由影像學到的中間表示，並非標註真值。
本圖示意：解釋模型責任，不代表新模型實測。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
共同输入与物件外观不变。语意遮罩同色表示同類，不一定提供逐件身份；實例分割分出甲乙，可支援後續逐件統計。重疊與遮擋仍可能分錯，需要標註與域內驗證；不聲稱必然比語意分割更適合所有工作。
先看同一影像有兩件支架，再核對語意：兩件都屬支架類，最後看實例：甲乙各有遮罩。
技術上不可誤讀
兩個墊圈稍微重疊，模型可能只交出一件遮罩。原圖仍有兩件；人工圈出的漏件區不是模型的第二個偵測。

完整節點

原始工程節點
1
同一影像有兩件支架

同一影像有兩件支架；請對照圖內輸入、變化與結果。

2
語意：兩件都屬支架類

語意：兩件都屬支架類；請對照圖內輸入、變化與結果。

3
實例：甲乙各有遮罩

實例：甲乙各有遮罩；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看同一影像有兩件支架，再核對語意：兩件都屬支架類，最後看實例：甲乙各有遮罩。

怎麼理解

共同输入与物件外观不变。语意遮罩同色表示同類，不一定提供逐件身份；實例分割分出甲乙，可支援後續逐件統計。重疊與遮擋仍可能分錯，需要標註與域內驗證；不聲稱必然比語意分割更適合所有工作。

不要誤用

逐件核對原圖和標註，補足重疊樣本，必要時調整取像視角或分料，再用未見資料重測漏件和錯誤分件。

工程師操作卡

把 YOLO-Seg 放進小規模試做（POC）前，先回答這五個問題。

先讀白話版就能開始溝通；需要實作、驗證或交接時，再展開下方技術契約。

1
它會交出什麼？

每件框、類別與獨立遮罩，供逐件核對和計數；A/B是實例ID，不代表不同產品類別或跨影格身分。

2
開始先做什麼？

先決定遮擋物件如何標註與計數，收單件、相鄰、重疊和空畫面，分開批次驗證。

3
開始前要寫下來的四件事

固定畫面範圍、資料來源、模型版本和驗收方式；詳細項目可在下方展開。

4
怎樣才算真的有用？

保存真實原圖與逐件標註，量物件召回、錯誤合併／切分、遮罩品質和計數誤差；孔洞保留需求另驗，示意圖不是模型結果。

5
碰到什麼先停下來？

逐件核對原圖和標註，補足重疊樣本，必要時調整取像視角或分料，再用未見資料重測漏件和錯誤分件。

工程師需要時，再展開原始技術契約

原始建置項目

這些設定要一併保存

輸入與版本：先決定遮擋物件如何標註與計數，收單件、相鄰、重疊和空畫面，分開批次驗證。；關鍵機制：以YOLOv8-seg機制為例，共享特徵產生一組原型遮罩，偵測分支為每件預測框、類別及遮罩係數。每件係數組合共享原型，再依框處理成該件遮罩。共享原型不是一張完整墊圈答案；兩件同類也要有分開的實例輸出。；獨立驗證：保存真實原圖與逐件標註，量物件召回、錯誤合併／切分、遮罩品質和計數誤差；孔洞保留需求另驗，示意圖不是模型結果。；失敗處理：逐件核對原圖和標註，補足重疊樣本，必要時調整取像視角或分料，再用未見資料重測漏件和錯誤分件。

原始選型條件：U-Net和SegFormer提供語意分割：同類像素用同類標籤，未必區分每一件。YOLO-Seg提供實例分割：每件有自己的遮罩。先決定要量區域還是逐件計數，再比較模型與標註成本。 需每件實例標註和相應版本權重；本課以YOLOv8-seg為例，不泛化聲稱所有YOLO版本都相同。固定解析度、門檻及後處理，測每件漏檢、重疊和推論成本。

原始失效邊界

下列情況不可忽略

兩個墊圈稍微重疊，模型可能只交出一件遮罩。原圖仍有兩件；人工圈出的漏件區不是模型的第二個偵測。

工程結論
每件框、類別與獨立遮罩，供逐件核對和計數；A/B是實例ID，不代表不同產品類別或跨影格身分。
把這張操作卡帶入 POC 畫布 →
查看 model.md ↗
查看圖檔 manifest ↗

相鄰比較

下一個可比較的模型

比較前，先把「看哪段畫面、用哪些資料、交出什麼、多久完成、誰覆核」固定；再到家族頁看共同條件與差異。

ResNet →
ConvNeXt →
ViT →
U-Net →
查看家族比較 →
← SegFormer
Keypoint R-CNN →