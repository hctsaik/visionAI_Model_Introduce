首頁
/
已知目標：分類、分割與姿態
/
ViT
◫ 已知目標：分類、分割與姿態
先理解，再看工程圖
ViT

固定相機拍螺絲頭，要把十字、一字與內六角分流到不同料盒。

你會拿到：已知類別與模型分數，交分流規則或人工覆核；未知產品、輪廓與3D姿態另行處理。
先停下來：讓各類工件都跨背景、批次與允收光照取樣；以未參與訓練的批次驗證，分析各類混淆與低信心案例，再決定是否重新訓練。
完成本主題
☆ 收藏
從案例理解 ViT
從必要原理、具體反例到工作取捨

固定相機拍螺絲頭，要把十字、一字與內六角分流到不同料盒。 下列圖像、特徵及回答為教學示意，不是模型實測。

第 01 張 · 看案例，再做判斷

ViT：把影像變成可互相參照的區塊

固定相機拍螺絲頭，要把十字、一字與內六角分流到不同料盒。

ViT：把影像變成可互相參照的區塊
放大完整圖
為什麼需要這個設計？

ViT把影像分成區塊，編碼成帶位置資訊的tokens。自注意力讓每個區塊依其他區塊的線索更新表示，再匯整成分類結果。圖上粗細連線示意資訊關係；注意力權重不是經驗證的缺陷位置或因果解釋。

讀完要能說清楚的三個要點
區塊需保留位置

區塊編碼與位置資訊共同進模型；示意九格不代表所有ViT固定用九個token。

互看後再分類

注意力聚合其他tokens的訊息，分類表示再送任務頭。全圖關係不等於免去工作標註或對小細節一定有利。

交付與接手

已知類別與模型分數，交分流規則或人工覆核；未知產品、輪廓與3D姿態另行處理。

想深入，再看這張圖的完整工程說明
ViT：把影像變成可互相參照的區塊

原始問題：固定相機拍螺絲頭，要把十字、一字與內六角分流到不同料盒。

ViT把影像分成區塊，編碼成帶位置資訊的tokens。自注意力讓每個區塊依其他區塊的線索更新表示，再匯整成分類結果。圖上粗細連線示意資訊關係；注意力權重不是經驗證的缺陷位置或因果解釋。

區塊需保留位置

區塊編碼與位置資訊共同進模型；示意九格不代表所有ViT固定用九個token。

互看後再分類

注意力聚合其他tokens的訊息，分類表示再送任務頭。全圖關係不等於免去工作標註或對小細節一定有利。

交付與接手

已知類別與模型分數，交分流規則或人工覆核；未知產品、輪廓與3D姿態另行處理。

第 02 張 · 看案例，再做判斷

模型認的是工件，還是拍攝背景？

觀察哪個條件改變，讓原本的結果不夠可靠？

模型認的是工件，還是拍攝背景？
放大完整圖
看哪裡

訓練照片中十字螺絲總在藍墊、內六角總在橘墊，模型可能學到背景捷徑。把同一十字螺絲換到橘墊後，可能誤判內六角；工件本身沒有改變。

接著怎麼做

讓各類工件都跨背景、批次與允收光照取樣；以未參與訓練的批次驗證，分析各類混淆與低信心案例，再決定是否重新訓練。

想深入，再看這張圖的完整工程說明
模型認的是工件，還是拍攝背景？

原始問題：觀察哪個條件改變，讓原本的結果不夠可靠？

訓練照片中十字螺絲總在藍墊、內六角總在橘墊，模型可能學到背景捷徑。把同一十字螺絲換到橘墊後，可能誤判內六角；工件本身沒有改變。

看哪裡

訓練照片中十字螺絲總在藍墊、內六角總在橘墊，模型可能學到背景捷徑。把同一十字螺絲換到橘墊後，可能誤判內六角；工件本身沒有改變。

接著怎麼做

讓各類工件都跨背景、批次與允收光照取樣；以未參與訓練的批次驗證，分析各類混淆與低信心案例，再決定是否重新訓練。

第 03 張 · 看案例，再做判斷

同一分類工作，三種特徵整理方式

相同工作需求下，資料、輸出與成本如何取捨？

同一分類工作，三種特徵整理方式
放大完整圖
怎麼選

ResNet用殘差相加傳遞特徵；ConvNeXt仍以卷積作空間與通道處理；ViT讓區塊特徵互相參照。三者都可接分類頭；用相同標註、資料切分、前處理與硬體比較錯誤、耗時和記憶體，不能只看模型新舊。

資料與完整成本

需各類工作標註和未見批次測試；選預訓練權重後調整任務頭。換類別、背景或拍攝條件重新核對，不能只沿用原測試準確率。

想深入，再看這張圖的完整工程說明
同一分類工作，三種特徵整理方式

原始問題：相同工作需求下，資料、輸出與成本如何取捨？

ResNet用殘差相加傳遞特徵；ConvNeXt仍以卷積作空間與通道處理；ViT讓區塊特徵互相參照。三者都可接分類頭；用相同標註、資料切分、前處理與硬體比較錯誤、耗時和記憶體，不能只看模型新舊。

怎麼選

ResNet用殘差相加傳遞特徵；ConvNeXt仍以卷積作空間與通道處理；ViT讓區塊特徵互相參照。三者都可接分類頭；用相同標註、資料切分、前處理與硬體比較錯誤、耗時和記憶體，不能只看模型新舊。

資料與完整成本

需各類工作標註和未見批次測試；選預訓練權重後調整任務頭。換類別、背景或拍攝條件重新核對，不能只沿用原測試準確率。

換情境試著判斷

分流還是輪廓？

ViT分類可以把可疑工件送人覆核。另一部門希望直接取得缺陷輪廓，但像素標註成本高。

先交付分類覆核流程，或增加分割任務？哪些需求會改變你的決定？

看解釋與可接受的取捨

若工作目的是先分流且人工覆核量可接受，可先交付經驗證的分類流程；若後段確實需要輪廓或定位，則要定義相應輸出與標註並驗證。注意力圖不能替代輪廓真值。比較時一起計入標註、覆核、漏分和後段量測校正成本。

需要時查看基本名詞與模型背景
區塊注意力影像分類
區塊需保留位置：區塊編碼與位置資訊共同進模型；示意九格不代表所有ViT固定用九個token。
互看後再分類：注意力聚合其他tokens的訊息，分類表示再送任務頭。全圖關係不等於免去工作標註或對小細節一定有利。
區塊注意力影像分類

ViT把影像分成區塊，編碼成帶位置資訊的tokens。自注意力讓每個區塊依其他區塊的線索更新表示，再匯整成分類結果。圖上粗細連線示意資訊關係；注意力權重不是經驗證的缺陷位置或因果解釋。

訓練照片中十字螺絲總在藍墊、內六角總在橘墊，模型可能學到背景捷徑。把同一十字螺絲換到橘墊後，可能誤判內六角；工件本身沒有改變。

工程參考：展開文字因果鏈與設定核對點不重複主圖；只核對每步要鎖定的設定。

模型專屬因果鏈

實作時逐步核對

主圖已在上方第一遍。這裡只核對每一步要鎖定的設定與為什麼，不再重貼同一張圖。

1
影像切塊

區塊編碼與位置資訊共同進模型；示意九格不代表所有ViT固定用九個token。

因為：由本課核心機制連接工作用途

設定核對：WI030-1

2
帶位置的特徵互看

注意力聚合其他tokens的訊息，分類表示再送任務頭。全圖關係不等於免去工作標註或對小細節一定有利。

因為：由本課核心機制連接工作用途

設定核對：WI030-2

3
匯整成類別

已知類別與模型分數，交分流規則或人工覆核；未知產品、輪廓與3D姿態另行處理。

因為：由本課核心機制連接工作用途

設定核對：WI030-3

4
工作核對與接手

用未見批次逐類核對真實輸入、標籤、預測及信心，列混淆矩陣與背景／反光失敗例；記錄整段耗時、記憶體與人工覆核量。教學圖的分類結果不是實測。

因為：由本課核心機制連接工作用途

設定核對：WI030-4

想深入：展開失效案例、模型比較與小規模試做（POC）
初學者可先跳過；準備實作或驗收時再逐項核對。

失效案例、選型與小規模試做

什麼情況不能相信它？

這一區保留完整工程條件，供準備實作或驗收的人逐項核對。

本課具體反例

可能看到：訓練照片中十字螺絲總在藍墊、內六角總在橘墊，模型可能學到背景捷徑。把同一十字螺絲換到橘墊後，可能誤判內六角；工件本身沒有改變。

正確下一步：讓各類工件都跨背景、批次與允收光照取樣；以未參與訓練的批次驗證，分析各類混淆與低信心案例，再決定是否重新訓練。

更換產品、模型或輸入設定

可能看到：需各類工作標註和未見批次測試；選預訓練權重後調整任務頭。換類別、背景或拍攝條件重新核對，不能只沿用原測試準確率。

正確下一步：先定義各類與未知品接手規則，讓每類跨背景拍攝，按批次切開訓練和測試。

把示意當成實際推論

可能看到：本課特徵、排序、回答及圖像僅用於解釋，不支持現場準確率或耗時結論。

正確下一步：用未見批次逐類核對真實輸入、標籤、預測及信心，列混淆矩陣與背景／反光失敗例；記錄整段耗時、記憶體與人工覆核量。教學圖的分類結果不是實測。

相鄰方法比較

同一分類工作，三種特徵整理方式
ResNet
類別標註；殘差特徵相加
已知類別；同資料測錯誤與成本
ConvNeXt
類別標註；現代空間／通道卷積
已知類別；不能以模型新舊排名
ViT
類別標註；區塊表示與注意力
已知類別；注意力不是輪廓真值

ResNet用殘差相加傳遞特徵；ConvNeXt仍以卷積作空間與通道處理；ViT讓區塊特徵互相參照。三者都可接分類頭；用相同標註、資料切分、前處理與硬體比較錯誤、耗時和記憶體，不能只看模型新舊。 需各類工作標註和未見批次測試；選預訓練權重後調整任務頭。換類別、背景或拍攝條件重新核對，不能只沿用原測試準確率。

工程第一步

以真實工作資料做小規模比較
先定義各類與未知品接手規則，讓每類跨背景拍攝，按批次切開訓練和測試。
需各類工作標註和未見批次測試；選預訓練權重後調整任務頭。換類別、背景或拍攝條件重新核對，不能只沿用原測試準確率。
用未見批次逐類核對真實輸入、標籤、預測及信心，列混淆矩陣與背景／反光失敗例；記錄整段耗時、記憶體與人工覆核量。教學圖的分類結果不是實測。
目前做法

既有流程或最簡單可行方法

要量什麼

用未見批次逐類核對真實輸入、標籤、預測及信心，列混淆矩陣與背景／反光失敗例；記錄整段耗時、記憶體與人工覆核量。教學圖的分類結果不是實測。

反例測試

訓練照片中十字螺絲總在藍墊、內六角總在橘墊，模型可能學到背景捷徑。把同一十字螺絲換到橘墊後，可能誤判內六角；工件本身沒有改變。

誰確認

實作與影像核對負責人

先停下來的訊號：讓各類工件都跨背景、批次與允收光照取樣；以未參與訓練的批次驗證，分析各類混淆與低信心案例，再決定是否重新訓練。

讀完請自己講回來

它改變什麼？何時選另一種做法？
看本頁練習的核對說明

ViT把影像分成區塊，編碼成帶位置資訊的tokens。自注意力讓每個區塊依其他區塊的線索更新表示，再匯整成分類結果。圖上粗細連線示意資訊關係；注意力權重不是經驗證的缺陷位置或因果解釋。 ResNet用殘差相加傳遞特徵；ConvNeXt仍以卷積作空間與通道處理；ViT讓區塊特徵互相參照。三者都可接分類頭；用相同標註、資料切分、前處理與硬體比較錯誤、耗時和記憶體，不能只看模型新舊。

換一個現場再推理

分流還是輪廓？

情境：ViT分類可以把可疑工件送人覆核。另一部門希望直接取得缺陷輪廓，但像素標註成本高。

請先回答：先交付分類覆核流程，或增加分割任務？哪些需求會改變你的決定？

核對這個遷移題

若工作目的是先分流且人工覆核量可接受，可先交付經驗證的分類流程；若後段確實需要輪廓或定位，則要定義相應輸出與標註並驗證。注意力圖不能替代輪廓真值。比較時一起計入標註、覆核、漏分和後段量測校正成本。

內容依據：展開這頁主張的 model.md 可追溯範圍

roadmap-model-selection/known-target-classification/vit-classifier/model.md ↗

WI-033：工程層四圖與手機比較修正；首讀有效內容保留。原生圖已審，整頁與使用者審閱另記。

beginner_path.visuals[same-workpiece]：模型定位 / architecture_path
beginner_path.visuals[patch-contract]：architecture_path
beginner_path.visuals[token-position-attention]：representation_or_score
beginner_path.visuals[closed-set-output]：failure_boundary
beginner_path.visuals[observability-boundary]：selection_gate / evidence_bundle
promise：模型定位 / representation_or_score
mechanism_steps：architecture_path / representation_or_score
boundary_cases：failure_boundary / selection_gate
comparison + selection：selection_gate
poc + acceptance：cost_and_operating_point / evidence_bundle
transfer_check：failure_boundary / selection_gate
beginner_path + core + selection + transfer：https://arxiv.org/abs/2010.11929
工程參考：展開四張工程圖與完整節點
從輸入、機制、交付到限制，核對實作與驗證方式。

ViT把影像分成區塊，編碼成帶位置資訊的tokens。自注意力讓每個區塊依其他區塊的線索更新表示，再匯整成分類結果。圖上粗細連線示意資訊關係；注意力權重不是經驗證的缺陷位置或因果解釋。

01
先看它交出什麼
02
再看它怎麼做
03
上線前先固定條件
04
選它前先看風險
01

先看它交出什麼

ViT：把影像區塊變成可交流的表示

切片保留位置，分類仍需資料與任務頭。

正式教學插圖 1/4 · 已可直接閱讀；點圖僅用於放大細節開啟原圖 ↗

30 秒讀懂這張圖

切片保留位置，分類仍需資料與任務頭。
先看哪裡

先看同件影像切成區塊，再核對各片加入位置表示，最後看分類token讀出整件分數。

框與框的關係

原始ViT把不重疊patch線性投影成token，加入位置嵌入及分類token；Transformer交換資訊，最後CLS表示經分類頭讀出類別。本課是分類器，不把patch熱圖當分割。

本圖只要帶走

切片保留位置，分類仍需資料與任務頭。

特徵：由影像學到的中間表示，並非標註真值。
本圖示意：解釋模型責任，不代表新模型實測。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
原始ViT把不重疊patch線性投影成token，加入位置嵌入及分類token；Transformer交換資訊，最後CLS表示經分類頭讀出類別。本課是分類器，不把patch熱圖當分割。
先看同件影像切成區塊，再核對各片加入位置表示，最後看分類token讀出整件分數。
技術上不可誤讀
訓練照片中十字螺絲總在藍墊、內六角總在橘墊，模型可能學到背景捷徑。把同一十字螺絲換到橘墊後，可能誤判內六角；工件本身沒有改變。

完整節點

原始工程節點
1
同件影像切成區塊

同件影像切成區塊；請對照圖內輸入、變化與結果。

2
各片加入位置表示

各片加入位置表示；請對照圖內輸入、變化與結果。

3
分類token讀出整件分數

分類token讀出整件分數；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看同件影像切成區塊，再核對各片加入位置表示，最後看分類token讀出整件分數。

怎麼理解

原始ViT把不重疊patch線性投影成token，加入位置嵌入及分類token；Transformer交換資訊，最後CLS表示經分類頭讀出類別。本課是分類器，不把patch熱圖當分割。

不要誤用

讓各類工件都跨背景、批次與允收光照取樣；以未參與訓練的批次驗證，分析各類混淆與低信心案例，再決定是否重新訓練。

繼續看第 2 張：再看它怎麼做 ↓

02

再看它怎麼做

ViT：一個位置也會參考其他區塊

注意力加權其他表示，位置身份仍要保留。

正式教學插圖 2/4 · 已可直接閱讀；點圖僅用於放大細節開啟原圖 ↗

30 秒讀懂這張圖

注意力加權其他表示，位置身份仍要保留。
先看哪裡

先看刮傷片與孔邊片各有表示，再核對依內容形成參考權重，最後看加權更新，再由CLS分類。

框與框的關係

示意某query对兩個value加權0.75和0.25；value [2,0]與[0,4]加權成[1.5,1]。這是單頭簡化算例，實際還含多頭投影、殘差、正規化和MLP。注意力不是物理缺陷機率，也不能用線寬宣稱模型實测解釋。

本圖只要帶走

注意力加權其他表示，位置身份仍要保留。

特徵：由影像學到的中間表示，並非標註真值。
本圖示意：解釋模型責任，不代表新模型實測。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
示意某query对兩個value加權0.75和0.25；value [2,0]與[0,4]加權成[1.5,1]。這是單頭簡化算例，實際還含多頭投影、殘差、正規化和MLP。注意力不是物理缺陷機率，也不能用線寬宣稱模型實测解釋。
先看刮傷片與孔邊片各有表示，再核對依內容形成參考權重，最後看加權更新，再由CLS分類。
技術上不可誤讀
訓練照片中十字螺絲總在藍墊、內六角總在橘墊，模型可能學到背景捷徑。把同一十字螺絲換到橘墊後，可能誤判內六角；工件本身沒有改變。

完整節點

原始工程節點
1
刮傷片與孔邊片各有表示

刮傷片與孔邊片各有表示；請對照圖內輸入、變化與結果。

2
依內容形成參考權重

依內容形成參考權重；請對照圖內輸入、變化與結果。

3
加權更新，再由CLS分類

加權更新，再由CLS分類；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看刮傷片與孔邊片各有表示，再核對依內容形成參考權重，最後看加權更新，再由CLS分類。

怎麼理解

示意某query对兩個value加權0.75和0.25；value [2,0]與[0,4]加權成[1.5,1]。這是單頭簡化算例，實際還含多頭投影、殘差、正規化和MLP。注意力不是物理缺陷機率，也不能用線寬宣稱模型實测解釋。

不要誤用

讓各類工件都跨背景、批次與允收光照取樣；以未參與訓練的批次驗證，分析各類混淆與低信心案例，再決定是否重新訓練。

繼續看第 3 張：上線前先固定條件 ↓

03

上線前先固定條件

ViT：改切片設定，細節與成本都會變

先確認細傷還可見，再測分類與記憶體。

正式教學插圖 3/4 · 已可直接閱讀；點圖僅用於放大細節開啟原圖 ↗

30 秒讀懂這張圖

先確認細傷還可見，再測分類與記憶體。
先看哪裡

先看同一細痕與輸入取樣，再核對切片尺度改變token數，最後看保留同件測試與錯分類型。

框與框的關係

改解析度會改token數及注意力成本；改patch尺寸涉及模型配置與權重相容性，不能隨意把現有權重改為另一尺寸。先查實際輸入可見線索，再比較分類、記憶體及延遲；圖中的格子是分片概念而非真實注意力圖。

本圖只要帶走

先確認細傷還可見，再測分類與記憶體。

特徵：由影像學到的中間表示，並非標註真值。
本圖示意：解釋模型責任，不代表新模型實測。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
改解析度會改token數及注意力成本；改patch尺寸涉及模型配置與權重相容性，不能隨意把現有權重改為另一尺寸。先查實際輸入可見線索，再比較分類、記憶體及延遲；圖中的格子是分片概念而非真實注意力圖。
先看同一細痕與輸入取樣，再核對切片尺度改變token數，最後看保留同件測試與錯分類型。
技術上不可誤讀
訓練照片中十字螺絲總在藍墊、內六角總在橘墊，模型可能學到背景捷徑。把同一十字螺絲換到橘墊後，可能誤判內六角；工件本身沒有改變。

完整節點

原始工程節點
1
同一細痕與輸入取樣

同一細痕與輸入取樣；請對照圖內輸入、變化與結果。

2
切片尺度改變token數

切片尺度改變token數；請對照圖內輸入、變化與結果。

3
保留同件測試與錯分類型

保留同件測試與錯分類型；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看同一細痕與輸入取樣，再核對切片尺度改變token數，最後看保留同件測試與錯分類型。

怎麼理解

改解析度會改token數及注意力成本；改patch尺寸涉及模型配置與權重相容性，不能隨意把現有權重改為另一尺寸。先查實際輸入可見線索，再比較分類、記憶體及延遲；圖中的格子是分片概念而非真實注意力圖。

不要誤用

讓各類工件都跨背景、批次與允收光照取樣；以未參與訓練的批次驗證，分析各類混淆與低信心案例，再決定是否重新訓練。

繼續看第 4 張：選它前先看風險 ↓

04

選它前先看風險

ViT與ConvNeXt：同題比較代價

全局交流是能力來源，不保證小資料就更準。

正式教學插圖 4/4 · 已可直接閱讀；點圖僅用於放大細節開啟原圖 ↗

30 秒讀懂這張圖

全局交流是能力來源，不保證小資料就更準。
先看哪裡

先看相同分類照片與標籤，再核對ConvNeXt先做逐通道空間卷積，最後看注意力跨片交換資訊。

框與框的關係

在相同產品和標籤下比較預訓練、可調整資料量、輸入解析度及硬體成本；卷積局部先驗可能適合部分小資料任務，ViT利用大規模預訓練仍需域內驗證。候選可並行，不畫成必須串接。

本圖只要帶走

全局交流是能力來源，不保證小資料就更準。

特徵：由影像學到的中間表示，並非標註真值。
本圖示意：解釋模型責任，不代表新模型實測。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
在相同產品和標籤下比較預訓練、可調整資料量、輸入解析度及硬體成本；卷積局部先驗可能適合部分小資料任務，ViT利用大規模預訓練仍需域內驗證。候選可並行，不畫成必須串接。
先看相同分類照片與標籤，再核對ConvNeXt先做逐通道空間卷積，最後看注意力跨片交換資訊。
技術上不可誤讀
訓練照片中十字螺絲總在藍墊、內六角總在橘墊，模型可能學到背景捷徑。把同一十字螺絲換到橘墊後，可能誤判內六角；工件本身沒有改變。

完整節點

原始工程節點
1
相同分類照片與標籤

相同分類照片與標籤；請對照圖內輸入、變化與結果。

2
ConvNeXt先做逐通道空間卷積

ConvNeXt先做逐通道空間卷積；請對照圖內輸入、變化與結果。

3
注意力跨片交換資訊

注意力跨片交換資訊；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看相同分類照片與標籤，再核對ConvNeXt先做逐通道空間卷積，最後看注意力跨片交換資訊。

怎麼理解

在相同產品和標籤下比較預訓練、可調整資料量、輸入解析度及硬體成本；卷積局部先驗可能適合部分小資料任務，ViT利用大規模預訓練仍需域內驗證。候選可並行，不畫成必須串接。

不要誤用

讓各類工件都跨背景、批次與允收光照取樣；以未參與訓練的批次驗證，分析各類混淆與低信心案例，再決定是否重新訓練。

工程師操作卡

把 ViT 放進小規模試做（POC）前，先回答這五個問題。

先讀白話版就能開始溝通；需要實作、驗證或交接時，再展開下方技術契約。

1
它會交出什麼？

已知類別與模型分數，交分流規則或人工覆核；未知產品、輪廓與3D姿態另行處理。

2
開始先做什麼？

先定義各類與未知品接手規則，讓每類跨背景拍攝，按批次切開訓練和測試。

3
開始前要寫下來的四件事

固定畫面範圍、資料來源、模型版本和驗收方式；詳細項目可在下方展開。

4
怎樣才算真的有用？

用未見批次逐類核對真實輸入、標籤、預測及信心，列混淆矩陣與背景／反光失敗例；記錄整段耗時、記憶體與人工覆核量。教學圖的分類結果不是實測。

5
碰到什麼先停下來？

讓各類工件都跨背景、批次與允收光照取樣；以未參與訓練的批次驗證，分析各類混淆與低信心案例，再決定是否重新訓練。

工程師需要時，再展開原始技術契約

原始建置項目

這些設定要一併保存

輸入與版本：先定義各類與未知品接手規則，讓每類跨背景拍攝，按批次切開訓練和測試。；關鍵機制：ViT把影像分成區塊，編碼成帶位置資訊的tokens。自注意力讓每個區塊依其他區塊的線索更新表示，再匯整成分類結果。圖上粗細連線示意資訊關係；注意力權重不是經驗證的缺陷位置或因果解釋。；獨立驗證：用未見批次逐類核對真實輸入、標籤、預測及信心，列混淆矩陣與背景／反光失敗例；記錄整段耗時、記憶體與人工覆核量。教學圖的分類結果不是實測。；失敗處理：讓各類工件都跨背景、批次與允收光照取樣；以未參與訓練的批次驗證，分析各類混淆與低信心案例，再決定是否重新訓練。

原始選型條件：ResNet用殘差相加傳遞特徵；ConvNeXt仍以卷積作空間與通道處理；ViT讓區塊特徵互相參照。三者都可接分類頭；用相同標註、資料切分、前處理與硬體比較錯誤、耗時和記憶體，不能只看模型新舊。 需各類工作標註和未見批次測試；選預訓練權重後調整任務頭。換類別、背景或拍攝條件重新核對，不能只沿用原測試準確率。

原始失效邊界

下列情況不可忽略

訓練照片中十字螺絲總在藍墊、內六角總在橘墊，模型可能學到背景捷徑。把同一十字螺絲換到橘墊後，可能誤判內六角；工件本身沒有改變。

工程結論
已知類別與模型分數，交分流規則或人工覆核；未知產品、輪廓與3D姿態另行處理。
把這張操作卡帶入 POC 畫布 →
查看 model.md ↗
查看圖檔 manifest ↗

相鄰比較

下一個可比較的模型

比較前，先把「看哪段畫面、用哪些資料、交出什麼、多久完成、誰覆核」固定；再到家族頁看共同條件與差異。

ResNet →
ConvNeXt →
U-Net →
SegFormer →
查看家族比較 →
← ConvNeXt
U-Net →