首頁
/
Foundation Vision、語意檢索與 VLM
/
DINOv3
✦ Foundation Vision、語意檢索與 VLM
先理解，再看工程圖
DINOv3

你要比較接頭上不同位置的線索，既希望整件可辨識，也希望局部特徵在長時間預訓練後仍保留一致的關係。

你會拿到：提供可供檢索、局部配對或任務頭使用的整圖與局部特徵；本課不把特徵色圖當成精確輪廓。
先停下來：先從原圖確認細節，固定合適的輸入設定；再把局部特徵交給可驗證的配對或異常方法，回查其失敗樣本。
完成本主題
☆ 收藏
從案例理解 DINOv3
從必要原理、具體反例到工作取捨

你要比較接頭上不同位置的線索，既希望整件可辨識，也希望局部特徵在長時間預訓練後仍保留一致的關係。 下列圖像、特徵及回答為教學示意，不是模型實測。

第 01 張 · 看案例，再做判斷

DINOv3：把局部之間的關係學穩

你要比較接頭上不同位置的線索，既希望整件可辨識，也希望局部特徵在長時間預訓練後仍保留一致的關係。

DINOv3：把局部之間的關係學穩
放大完整圖
為什麼需要這個設計？

DINOv3 延續大規模自監督特徵學習。本課以 ViT 版本的 Gram anchoring 說明關鍵設計：訓練時，不只關心整圖表現，也讓目前模型的局部特徵兩兩關係，接近較早模型提供的關係目標，以緩解長訓練中局部一致性退化。部署得到的是特徵，配對、分類或異常判斷仍由下游完成。

讀完要能說清楚的三個要點
比較關係，而非複製像素

Gram 矩陣記錄影像內局部特徵兩兩內積。圖中 A／B 插針、C 外殼只用來示意關係，藍色不是人工分割真值，也不保證金屬位置一定得到相同特徵。

訓練約束與部署用途分開

較早模型提供訓練中的關係目標；正式使用時提取選定骨幹的整圖／局部表示，再接自己的用途。Gram anchoring 不是每次推論時運行的缺陷判定頭。

交付與接手

提供可供檢索、局部配對或任務頭使用的整圖與局部特徵；本課不把特徵色圖當成精確輪廓。

想深入，再看這張圖的完整工程說明
DINOv3：把局部之間的關係學穩

原始問題：你要比較接頭上不同位置的線索，既希望整件可辨識，也希望局部特徵在長時間預訓練後仍保留一致的關係。

DINOv3 延續大規模自監督特徵學習。本課以 ViT 版本的 Gram anchoring 說明關鍵設計：訓練時，不只關心整圖表現，也讓目前模型的局部特徵兩兩關係，接近較早模型提供的關係目標，以緩解長訓練中局部一致性退化。部署得到的是特徵，配對、分類或異常判斷仍由下游完成。

比較關係，而非複製像素

Gram 矩陣記錄影像內局部特徵兩兩內積。圖中 A／B 插針、C 外殼只用來示意關係，藍色不是人工分割真值，也不保證金屬位置一定得到相同特徵。

訓練約束與部署用途分開

較早模型提供訓練中的關係目標；正式使用時提取選定骨幹的整圖／局部表示，再接自己的用途。Gram anchoring 不是每次推論時運行的缺陷判定頭。

交付與接手

提供可供檢索、局部配對或任務頭使用的整圖與局部特徵；本課不把特徵色圖當成精確輪廓。

第 02 張 · 看案例，再做判斷

特徵很相近，仍可能漏掉小缺口

觀察哪個條件改變，讓原本的結果不夠可靠？

特徵很相近，仍可能漏掉小缺口
放大完整圖
看哪裡

更好的局部表示仍受到影像細節限制。墊圈右側小缺口若在縮圖後變弱，DINOv3 的名稱或漂亮特徵色圖，都不能代替輸入可見性與下游漏檢測試。

接著怎麼做

先從原圖確認細節，固定合適的輸入設定；再把局部特徵交給可驗證的配對或異常方法，回查其失敗樣本。

想深入，再看這張圖的完整工程說明
特徵很相近，仍可能漏掉小缺口

原始問題：觀察哪個條件改變，讓原本的結果不夠可靠？

更好的局部表示仍受到影像細節限制。墊圈右側小缺口若在縮圖後變弱，DINOv3 的名稱或漂亮特徵色圖，都不能代替輸入可見性與下游漏檢測試。

看哪裡

更好的局部表示仍受到影像細節限制。墊圈右側小缺口若在縮圖後變弱，DINOv3 的名稱或漂亮特徵色圖，都不能代替輸入可見性與下游漏檢測試。

接著怎麼做

先從原圖確認細節，固定合適的輸入設定；再把局部特徵交給可驗證的配對或異常方法，回查其失敗樣本。

第 03 張 · 看案例，再做判斷

DINOv2 與 DINOv3：比較的是整套用途

相同工作需求下，資料、輸出與成本如何取捨？

DINOv2 與 DINOv3：比較的是整套用途
放大完整圖
怎麼選

與 DINOv2 比較時，固定相同影像、工作任務、留出測試集與調整預算；允許各自使用相容的前處理與下游設定，並如實記錄。不要要求不同模型硬接同一維度的參考庫。

資料與完整成本

更換骨幹後，重新抽取參考特徵、調整下游和驗證新門檻；換產品也需補參考及失敗案例。比較候選命中或定位誤差、正常誤報／缺陷漏檢，以及建庫、記憶體和完整耗時；局部圖好看不等於工作改善。

想深入，再看這張圖的完整工程說明
DINOv2 與 DINOv3：比較的是整套用途

原始問題：相同工作需求下，資料、輸出與成本如何取捨？

與 DINOv2 比較時，固定相同影像、工作任務、留出測試集與調整預算；允許各自使用相容的前處理與下游設定，並如實記錄。不要要求不同模型硬接同一維度的參考庫。

怎麼選

與 DINOv2 比較時，固定相同影像、工作任務、留出測試集與調整預算；允許各自使用相容的前處理與下游設定，並如實記錄。不要要求不同模型硬接同一維度的參考庫。

資料與完整成本

更換骨幹後，重新抽取參考特徵、調整下游和驗證新門檻；換產品也需補參考及失敗案例。比較候選命中或定位誤差、正常誤報／缺陷漏檢，以及建庫、記憶體和完整耗時；局部圖好看不等於工作改善。

換情境試著判斷

新骨幹的特徵圖更漂亮

DINOv3 的局部色圖更整齊，但在相同資料上重新建庫需要更久，候選命中尚未測量。

現在應直接更換，還是先比較什麼？

看解釋與可接受的取捨

先保留基準，以同一留出集比較實際命中或定位錯誤，再把重新建庫、記憶體、查詢時間算進去。只有當改善符合工作需求，且維護代價可接受時才換；保留 DINOv2 也可能合理。

需要時查看基本名詞與模型背景
自監督視覺特徵骨幹
比較關係，而非複製像素：Gram 矩陣記錄影像內局部特徵兩兩內積。圖中 A／B 插針、C 外殼只用來示意關係，藍色不是人工分割真值，也不保證金屬位置一定得到相同特徵。
訓練約束與部署用途分開：較早模型提供訓練中的關係目標；正式使用時提取選定骨幹的整圖／局部表示，再接自己的用途。Gram anchoring 不是每次推論時運行的缺陷判定頭。
自監督視覺特徵骨幹

DINOv3 延續大規模自監督特徵學習。本課以 ViT 版本的 Gram anchoring 說明關鍵設計：訓練時，不只關心整圖表現，也讓目前模型的局部特徵兩兩關係，接近較早模型提供的關係目標，以緩解長訓練中局部一致性退化。部署得到的是特徵，配對、分類或異常判斷仍由下游完成。

更好的局部表示仍受到影像細節限制。墊圈右側小缺口若在縮圖後變弱，DINOv3 的名稱或漂亮特徵色圖，都不能代替輸入可見性與下游漏檢測試。

工程參考：展開文字因果鏈與設定核對點不重複主圖；只核對每步要鎖定的設定。

模型專屬因果鏈

實作時逐步核對

主圖已在上方第一遍。這裡只核對每一步要鎖定的設定與為什麼，不再重貼同一張圖。

1
選定影像局部

你要比較接頭上不同位置的線索，既希望整件可辨識，也希望局部特徵在長時間預訓練後仍保留一致的關係。

因為：由本課核心機制連接工作用途

設定核對：WI027-1

2
保留早期特徵關係

Gram 矩陣記錄影像內局部特徵兩兩內積。圖中 A／B 插針、C 外殼只用來示意關係，藍色不是人工分割真值，也不保證金屬位置一定得到相同特徵。

因為：由本課核心機制連接工作用途

設定核對：WI027-2

3
訓練中對齊關係

較早模型提供訓練中的關係目標；正式使用時提取選定骨幹的整圖／局部表示，再接自己的用途。Gram anchoring 不是每次推論時運行的缺陷判定頭。

因為：由本課核心機制連接工作用途

設定核對：WI027-3

4
部署後交給下游

提供可供檢索、局部配對或任務頭使用的整圖與局部特徵；本課不把特徵色圖當成精確輪廓。

因為：由本課核心機制連接工作用途

設定核對：WI027-4

想深入：展開失效案例、模型比較與小規模試做（POC）
初學者可先跳過；準備實作或驗收時再逐項核對。

失效案例、選型與小規模試做

什麼情況不能相信它？

這一區保留完整工程條件，供準備實作或驗收的人逐項核對。

本課具體反例

可能看到：更好的局部表示仍受到影像細節限制。墊圈右側小缺口若在縮圖後變弱，DINOv3 的名稱或漂亮特徵色圖，都不能代替輸入可見性與下游漏檢測試。

正確下一步：先從原圖確認細節，固定合適的輸入設定；再把局部特徵交給可驗證的配對或異常方法，回查其失敗樣本。

更換產品、模型或輸入設定

可能看到：更換骨幹後，重新抽取參考特徵、調整下游和驗證新門檻；換產品也需補參考及失敗案例。比較候選命中或定位誤差、正常誤報／缺陷漏檢，以及建庫、記憶體和完整耗時；局部圖好看不等於工作改善。

正確下一步：保留既有 DINOv2 基準，選定一個 DINOv3 ViT 權重；用相同實體隔離的參考／測試資料，為兩者各自建立相容的下游。

把示意當成實際推論

可能看到：本課特徵、排序、回答及圖像僅用於解釋，不支持現場準確率或耗時結論。

正確下一步：在同一留出集比較任務錯誤與完整成本；若要局部配對，用可核對的點位或對應標註驗證，不以彩色特徵圖評輸贏。

相鄰方法比較

DINOv2 與 DINOv3：比較的是整套用途
方法	所需資料與方法差異	輸出與維護責任
DINOv2	預訓練視覺骨幹；整理參考及任務資料	整圖／局部特徵，由下游完成檢索或判斷
DINOv3	預訓練局部關係設計；換骨幹需重抽參考與調整下游	相容的整圖／局部特徵；在同測試集評任務與成本
既有檢索基準	保留目前參考庫與查詢設定，使用同一留出測試集	原有候選與完整耗時，作為換骨幹是否有用的比較點

與 DINOv2 比較時，固定相同影像、工作任務、留出測試集與調整預算；允許各自使用相容的前處理與下游設定，並如實記錄。不要要求不同模型硬接同一維度的參考庫。 更換骨幹後，重新抽取參考特徵、調整下游和驗證新門檻；換產品也需補參考及失敗案例。比較候選命中或定位誤差、正常誤報／缺陷漏檢，以及建庫、記憶體和完整耗時；局部圖好看不等於工作改善。

工程第一步

以真實工作資料做小規模比較
保留既有 DINOv2 基準，選定一個 DINOv3 ViT 權重；用相同實體隔離的參考／測試資料，為兩者各自建立相容的下游。
更換骨幹後，重新抽取參考特徵、調整下游和驗證新門檻；換產品也需補參考及失敗案例。比較候選命中或定位誤差、正常誤報／缺陷漏檢，以及建庫、記憶體和完整耗時；局部圖好看不等於工作改善。
在同一留出集比較任務錯誤與完整成本；若要局部配對，用可核對的點位或對應標註驗證，不以彩色特徵圖評輸贏。
目前做法

既有流程或最簡單可行方法

要量什麼

在同一留出集比較任務錯誤與完整成本；若要局部配對，用可核對的點位或對應標註驗證，不以彩色特徵圖評輸贏。

反例測試

更好的局部表示仍受到影像細節限制。墊圈右側小缺口若在縮圖後變弱，DINOv3 的名稱或漂亮特徵色圖，都不能代替輸入可見性與下游漏檢測試。

誰確認

實作與影像核對負責人

先停下來的訊號：先從原圖確認細節，固定合適的輸入設定；再把局部特徵交給可驗證的配對或異常方法，回查其失敗樣本。

讀完請自己講回來

它改變什麼？何時選另一種做法？
看本頁練習的核對說明

DINOv3 延續大規模自監督特徵學習。本課以 ViT 版本的 Gram anchoring 說明關鍵設計：訓練時，不只關心整圖表現，也讓目前模型的局部特徵兩兩關係，接近較早模型提供的關係目標，以緩解長訓練中局部一致性退化。部署得到的是特徵，配對、分類或異常判斷仍由下游完成。 與 DINOv2 比較時，固定相同影像、工作任務、留出測試集與調整預算；允許各自使用相容的前處理與下游設定，並如實記錄。不要要求不同模型硬接同一維度的參考庫。

換一個現場再推理

新骨幹的特徵圖更漂亮

情境：DINOv3 的局部色圖更整齊，但在相同資料上重新建庫需要更久，候選命中尚未測量。

請先回答：現在應直接更換，還是先比較什麼？

核對這個遷移題

先保留基準，以同一留出集比較實際命中或定位錯誤，再把重新建庫、記憶體、查詢時間算進去。只有當改善符合工作需求，且維護代價可接受時才換；保留 DINOv2 也可能合理。

內容依據：展開這頁主張的 model.md 可追溯範圍

roadmap-model-selection/foundation-vlm-open-world/dinov3/model.md ↗

WI-033：工程層四圖與手機比較修正；首讀有效內容保留。原生圖已審，整頁與使用者審閱另記。

beginner_path.visuals[same-ceramic-baseline]：模型定位 / architecture_path
beginner_path.visuals[gram-training-boundary]：architecture_path
beginner_path.visuals[versioned-feature-route]：representation_or_score
beginner_path.visuals[same-baseline-evidence]：failure_boundary
beginner_path.visuals[observability-release-gate]：selection_gate / evidence_bundle
promise：模型定位 / representation_or_score
mechanism_steps：architecture_path / representation_or_score
boundary_cases：failure_boundary / selection_gate
comparison + selection：selection_gate
poc + acceptance：cost_and_operating_point / evidence_bundle
transfer_check：failure_boundary / selection_gate
beginner_path.visuals[0] + scenario + mental_model：https://arxiv.org/abs/2508.10104
micro_example + transfer_check + selection：https://arxiv.org/abs/2508.10104
beginner_path + core + selection + transfer：https://arxiv.org/html/2508.10104v1
工程參考：展開四張工程圖與完整節點
從輸入、機制、交付到限制，核對實作與驗證方式。

DINOv3 延續大規模自監督特徵學習。本課以 ViT 版本的 Gram anchoring 說明關鍵設計：訓練時，不只關心整圖表現，也讓目前模型的局部特徵兩兩關係，接近較早模型提供的關係目標，以緩解長訓練中局部一致性退化。部署得到的是特徵，配對、分類或異常判斷仍由下游完成。

01
先看它交出什麼
02
再看它怎麼做
03
上線前先固定條件
04
選它前先看風險
01

先看它交出什麼

DINOv3：一張圖保留多個局部表示

骨幹交出特徵，局部位置與下游工作仍要對齊。

正式教學插圖 1/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

骨幹交出特徵，局部位置與下游工作仍要對齊。
先看哪裡

先看輸入一個墊圈影像，再核對每個區塊各有特徵，最後看下游才解讀相似或不同。

框與框的關係

以ViT骨幹為例，影像轉成patch token並與其他位置交換資訊，輸出局部及全局表示。局部表示保留對應位置，但每個向量包含上下文，不等於該格像素值。異常判斷、分類或匹配要接下游方法；特徵色圖不是現成缺陷分數。

本圖只要帶走

骨幹交出特徵，局部位置與下游工作仍要對齊。

patch：影像的局部區塊，特徵可包含其他位置的資訊。
Gram：同一影像內，各patch特徵兩兩相似關係的矩陣。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
以ViT骨幹為例，影像轉成patch token並與其他位置交換資訊，輸出局部及全局表示。局部表示保留對應位置，但每個向量包含上下文，不等於該格像素值。異常判斷、分類或匹配要接下游方法；特徵色圖不是現成缺陷分數。
先看輸入一個墊圈影像，再核對每個區塊各有特徵，最後看下游才解讀相似或不同。
技術上不可誤讀
更好的局部表示仍受到影像細節限制。墊圈右側小缺口若在縮圖後變弱，DINOv3 的名稱或漂亮特徵色圖，都不能代替輸入可見性與下游漏檢測試。

完整節點

原始工程節點
1
輸入一個墊圈影像

輸入一個墊圈影像；請對照圖內輸入、變化與結果。

2
每個區塊各有特徵

每個區塊各有特徵；請對照圖內輸入、變化與結果。

3
下游才解讀相似或不同

下游才解讀相似或不同；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看輸入一個墊圈影像，再核對每個區塊各有特徵，最後看下游才解讀相似或不同。

怎麼理解

以ViT骨幹為例，影像轉成patch token並與其他位置交換資訊，輸出局部及全局表示。局部表示保留對應位置，但每個向量包含上下文，不等於該格像素值。異常判斷、分類或匹配要接下游方法；特徵色圖不是現成缺陷分數。

不要誤用

先從原圖確認細節，固定合適的輸入設定；再把局部特徵交給可驗證的配對或異常方法，回查其失敗樣本。

繼續看第 2 張：再看它怎麼做 ↓

02

再看它怎麼做

DINOv3：訓練時守住局部關係

Gram 只約束訓練，部署仍輸出特徵。

正式教學插圖 2/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

Gram 只約束訓練，部署仍輸出特徵。
先看哪裡

先看同一影像，對照兩個模型，再核對比較 patch 之間的關係，最後看用關係差異更新學生。

框與框的關係

Gram矩陣記錄同圖patch特徵的兩兩內積；以較早教師的關係當目標，約束目前學生，並保留原有自監督訓練目標。這不是把特徵逐點鎖成相同數值，也不是推論時要額外跑的模組。本圖數字為二維單位向量算例。

本圖只要帶走

Gram 只約束訓練，部署仍輸出特徵。

patch：影像的局部區塊，特徵可包含其他位置的資訊。
Gram：同一影像內，各patch特徵兩兩相似關係的矩陣。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
Gram矩陣記錄同圖patch特徵的兩兩內積；以較早教師的關係當目標，約束目前學生，並保留原有自監督訓練目標。這不是把特徵逐點鎖成相同數值，也不是推論時要額外跑的模組。本圖數字為二維單位向量算例。
先看同一影像，對照兩個模型，再核對比較 patch 之間的關係，最後看用關係差異更新學生。
技術上不可誤讀
更好的局部表示仍受到影像細節限制。墊圈右側小缺口若在縮圖後變弱，DINOv3 的名稱或漂亮特徵色圖，都不能代替輸入可見性與下游漏檢測試。

完整節點

原始工程節點
1
同一影像，對照兩個模型

同一影像，對照兩個模型；請對照圖內輸入、變化與結果。

2
比較 patch 之間的關係

比較 patch 之間的關係；請對照圖內輸入、變化與結果。

3
用關係差異更新學生

用關係差異更新學生；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看同一影像，對照兩個模型，再核對比較 patch 之間的關係，最後看用關係差異更新學生。

怎麼理解

Gram矩陣記錄同圖patch特徵的兩兩內積；以較早教師的關係當目標，約束目前學生，並保留原有自監督訓練目標。這不是把特徵逐點鎖成相同數值，也不是推論時要額外跑的模組。本圖數字為二維單位向量算例。

不要誤用

先從原圖確認細節，固定合適的輸入設定；再把局部特徵交給可驗證的配對或異常方法，回查其失敗樣本。

繼續看第 3 張：上線前先固定條件 ↓

03

上線前先固定條件

DINOv3：換骨幹也要重建相容參考

保存權重與前處理，同條件重建庫並量完整成本。

正式教學插圖 3/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

保存權重與前處理，同條件重建庫並量完整成本。
先看哪裡

先看固定取像與骨幹版本，再核對建立相容的參考表示，最後看核對下游結果與成本。

框與框的關係

部署不跑Gram教師；用固定權重與前處理抽取表示，再建相容特徵庫或訓練下游頭。不能把DINOv2庫直接當DINOv3庫查詢。以同一留出集核對命中／漏檢、抽特徵和查庫時間、記憶體與重建成本，不能只比較色圖漂亮程度。

本圖只要帶走

保存權重與前處理，同條件重建庫並量完整成本。

patch：影像的局部區塊，特徵可包含其他位置的資訊。
Gram：同一影像內，各patch特徵兩兩相似關係的矩陣。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
部署不跑Gram教師；用固定權重與前處理抽取表示，再建相容特徵庫或訓練下游頭。不能把DINOv2庫直接當DINOv3庫查詢。以同一留出集核對命中／漏檢、抽特徵和查庫時間、記憶體與重建成本，不能只比較色圖漂亮程度。
先看固定取像與骨幹版本，再核對建立相容的參考表示，最後看核對下游結果與成本。
技術上不可誤讀
更好的局部表示仍受到影像細節限制。墊圈右側小缺口若在縮圖後變弱，DINOv3 的名稱或漂亮特徵色圖，都不能代替輸入可見性與下游漏檢測試。

完整節點

原始工程節點
1
固定取像與骨幹版本

固定取像與骨幹版本；請對照圖內輸入、變化與結果。

2
建立相容的參考表示

建立相容的參考表示；請對照圖內輸入、變化與結果。

3
核對下游結果與成本

核對下游結果與成本；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看固定取像與骨幹版本，再核對建立相容的參考表示，最後看核對下游結果與成本。

怎麼理解

部署不跑Gram教師；用固定權重與前處理抽取表示，再建相容特徵庫或訓練下游頭。不能把DINOv2庫直接當DINOv3庫查詢。以同一留出集核對命中／漏檢、抽特徵和查庫時間、記憶體與重建成本，不能只比較色圖漂亮程度。

不要誤用

先從原圖確認細節，固定合適的輸入設定；再把局部特徵交給可驗證的配對或異常方法，回查其失敗樣本。

繼續看第 4 張：選它前先看風險 ↓

04

選它前先看風險

DINOv3：以工作結果決定是否換骨幹

同題比較效果與重建代價，保留基準也合理。

正式教學插圖 4/4 · 已可直接閱讀；點圖僅用於放大細節
開啟原圖 ↗

30 秒讀懂這張圖

同題比較效果與重建代價，保留基準也合理。
先看哪裡

先看同一批正常與缺口件，再核對既有骨幹：保留相容基準，最後看新骨幹：重建後再比較。

框與框的關係

工程4不重複主反例。固定同一批正常與缺口墊圈及取像，各自使用相容骨幹與參考表示；記錄下游漏檢/誤報、抽特徵/查庫時間與重建負擔。新骨幹訓練機制不同，不代表每項域內指標一定進步。

本圖只要帶走

同題比較效果與重建代價，保留基準也合理。

patch：影像的局部區塊，特徵可包含其他位置的資訊。
Gram：同一影像內，各patch特徵兩兩相似關係的矩陣。
想深入，再看工程細節與完整名詞

技術版讀圖

沿著資料流確認
工程4不重複主反例。固定同一批正常與缺口墊圈及取像，各自使用相容骨幹與參考表示；記錄下游漏檢/誤報、抽特徵/查庫時間與重建負擔。新骨幹訓練機制不同，不代表每項域內指標一定進步。
先看同一批正常與缺口件，再核對既有骨幹：保留相容基準，最後看新骨幹：重建後再比較。
技術上不可誤讀
更好的局部表示仍受到影像細節限制。墊圈右側小缺口若在縮圖後變弱，DINOv3 的名稱或漂亮特徵色圖，都不能代替輸入可見性與下游漏檢測試。

完整節點

原始工程節點
1
同一批正常與缺口件

同一批正常與缺口件；請對照圖內輸入、變化與結果。

2
既有骨幹：保留相容基準

既有骨幹：保留相容基準；請對照圖內輸入、變化與結果。

3
新骨幹：重建後再比較

新骨幹：重建後再比較；請對照圖內輸入、變化與結果。

工程師先帶走：不用作答，先把這三句話記住。

先看哪裡

先看同一批正常與缺口件，再核對既有骨幹：保留相容基準，最後看新骨幹：重建後再比較。

怎麼理解

工程4不重複主反例。固定同一批正常與缺口墊圈及取像，各自使用相容骨幹與參考表示；記錄下游漏檢/誤報、抽特徵/查庫時間與重建負擔。新骨幹訓練機制不同，不代表每項域內指標一定進步。

不要誤用

先從原圖確認細節，固定合適的輸入設定；再把局部特徵交給可驗證的配對或異常方法，回查其失敗樣本。

工程師操作卡

把 DINOv3 放進小規模試做（POC）前，先回答這五個問題。

先讀白話版就能開始溝通；需要實作、驗證或交接時，再展開下方技術契約。

1
它會交出什麼？

提供可供檢索、局部配對或任務頭使用的整圖與局部特徵；本課不把特徵色圖當成精確輪廓。

2
開始先做什麼？

保留既有 DINOv2 基準，選定一個 DINOv3 ViT 權重；用相同實體隔離的參考／測試資料，為兩者各自建立相容的下游。

3
開始前要寫下來的四件事

固定畫面範圍、資料來源、模型版本和驗收方式；詳細項目可在下方展開。

4
怎樣才算真的有用？

在同一留出集比較任務錯誤與完整成本；若要局部配對，用可核對的點位或對應標註驗證，不以彩色特徵圖評輸贏。

5
碰到什麼先停下來？

先從原圖確認細節，固定合適的輸入設定；再把局部特徵交給可驗證的配對或異常方法，回查其失敗樣本。

工程師需要時，再展開原始技術契約

原始建置項目

這些設定要一併保存

輸入與版本：保留既有 DINOv2 基準，選定一個 DINOv3 ViT 權重；用相同實體隔離的參考／測試資料，為兩者各自建立相容的下游。；關鍵機制：DINOv3 延續大規模自監督特徵學習。本課以 ViT 版本的 Gram anchoring 說明關鍵設計：訓練時，不只關心整圖表現，也讓目前模型的局部特徵兩兩關係，接近較早模型提供的關係目標，以緩解長訓練中局部一致性退化。部署得到的是特徵，配對、分類或異常判斷仍由下游完成。；獨立驗證：在同一留出集比較任務錯誤與完整成本；若要局部配對，用可核對的點位或對應標註驗證，不以彩色特徵圖評輸贏。；失敗處理：先從原圖確認細節，固定合適的輸入設定；再把局部特徵交給可驗證的配對或異常方法，回查其失敗樣本。

原始選型條件：與 DINOv2 比較時，固定相同影像、工作任務、留出測試集與調整預算；允許各自使用相容的前處理與下游設定，並如實記錄。不要要求不同模型硬接同一維度的參考庫。 更換骨幹後，重新抽取參考特徵、調整下游和驗證新門檻；換產品也需補參考及失敗案例。比較候選命中或定位誤差、正常誤報／缺陷漏檢，以及建庫、記憶體和完整耗時；局部圖好看不等於工作改善。

原始失效邊界

下列情況不可忽略

更好的局部表示仍受到影像細節限制。墊圈右側小缺口若在縮圖後變弱，DINOv3 的名稱或漂亮特徵色圖，都不能代替輸入可見性與下游漏檢測試。

工程結論
提供可供檢索、局部配對或任務頭使用的整圖與局部特徵；本課不把特徵色圖當成精確輪廓。
把這張操作卡帶入 POC 畫布 →
查看 model.md ↗
查看圖檔 manifest ↗

相鄰比較

下一個可比較的模型

比較前，先把「看哪段畫面、用哪些資料、交出什麼、多久完成、誰覆核」固定；再到家族頁看共同條件與差異。

DINOv2 →
CLIP →
SigLIP →
LLaVA →
查看家族比較 →
← DINOv2
CLIP →