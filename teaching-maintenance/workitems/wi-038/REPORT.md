# WI-038：需求導向 PoC 實作與發布

2026-09-13。依使用者「請更新到網站」授權，將 WI-037 三位 Agent 的設計實作。過去要求使用者先選模型，現在先選工作、回答會影響方法的現場條件，再取得方法、理由、缺口與小規模實驗。

## 變更

- 每次一個下拉問題，明確按繼續；每題提供「還不確定」，不需輸入工程名詞。
- 九類任務的條件式規則，包含簡單格位規則、OCR、先改善取像或收集資料。模型不是輸入或完成度；相關課程是結果後的選讀參考。
- 改上游答案會撤銷下游答案與確認狀態。結果可回看答案與修改，並整理成 Markdown、列印計畫。
- 舊草稿與補充保留在歷史區／JSON，不混入新的推導；課程帶入改存獨立 `_courseNotes`，避免覆寫原輸出。清除／範例仍可撤銷，學習進度獨立保留。

## 驗證

15 項 Node 規則測試通過，含正常品異常篩查、固定格位漏裝、逐件輪廓、無校正毫米量測、逐字抄錄、不可辨識取像六案例，以及全可達答案路徑、前提、未知及分支變化。所有課程 ID 有實際對應。

兩入口各 13 項瀏覽器測試通過（六情境為子測試），包含舊稿、reload、JSON、Markdown、列印、reset/undo、課程入口及手機鍵盤／無橫向溢出。source exit 0；docs 的 unittest 結論 OK，但 PowerShell stderr 包裝層 exit 1，保留實際紀錄，不冒稱該 wrapper 成功。課程筆記最後修正另以 capture_output 執行專項補驗。

3 項導覽、4 項 mobile page、2 項部署資產測試通過；1370 個資產建置。四節點 preflight 通過。舊全課程 verifier 的 `charuco: incomplete model-specific teaching story` 為 WI-035 已核實的既有 schema 差異，本輪不宣稱該舊門檻通過。

12 張截圖已產生並實看兩種寬度的問題、結果與計畫；字體可讀、選項無橫向溢出、主流程無模型輸入。兩入口 handoff 補驗皆 exit 0，重整後原稿不變。source/docs 逐位元一致，58 課資料與 193f70d 相同。見 verification.json、handoff-verification.json。

## 學習與接續

下拉選項降低填寫難度；答案會改變方法、前提與準備工作，才降低決策難度。未知必須變成確認工作，而不是隱藏預設。方法可以是先補資料，不應為了模型目錄強推工具。新流程與舊稿分開保存，也必須檢查課程入口等旁路，避免主畫面保留、旁路仍覆寫。

根 TEACHING_WEBPAGE_GUIDE.md、TEACHING_REVIEW_LOG.md 已記錄設計學習；本報告記錄實作反證。sync.py 保存規範、工作入口、WI-037 設計、實作來源及測試，並核對 manifest SHA-256。首次同步曾因新規則／測試未建立而 exit 1，屬施工中狀態；最終須重新同步。唯一 checklist：PLAN.md。

## 發布前 checkpoint

功能、28 項瀏覽器檢查（26 全套＋2 舊稿補驗）、15 項規則測試及9項回歸測試完成。12 張實際頁面截圖已檢查，source/docs一致且課程資料未改。即將 Git 提交推送，公開驗證尚未完成；使用者成品審閱 pending。
