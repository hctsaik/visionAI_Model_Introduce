from pathlib import Path
import json,subprocess,sys
W=Path(__file__).resolve().parent;C=W.parents[1]
lessons=json.loads((W/'lesson-content.json').read_text(encoding='utf-8'))
rows=[]
def add(owner,n,title,items,takeaway,detail):
 rows.append(dict(id=f'{owner}-engineering-{n}',owner=owner,index=n,title=title,kind='D' if n==4 else 'C',nodes=[i[0] for i in items],panels=[dict(title=a,graphic=b,lines=c) for a,b,c in items],takeaway=takeaway,detail=detail,source=lessons[owner]['sources'][0]))
add('charuco',1,'ChArUco：把板上點連到影像點',[
 ('已知板上座標','board',['角點ID固定','實體尺寸與單位已知']),
 ('多視角觀測','boardviews',['同一角點跨影像對應','涵蓋位置與傾角']),
 ('估相機模型','projection',['內參K＋畸變參數','保存解析度與鏡頭條件'])],
 '建立的是相機幾何，不是每天工件的定位結果。','以標靶已知3D平面座標與各視角2D點估計相機模型；板上Z=0不等於影像是正視投影。校正需要合適視角、模型及品質核對，不能把固定工件對位當成校正。')
add('charuco',2,'辨認標記與精細棋盤角點，各有任務',[
 ('標記辨認位置','marker',['字典與標記ID對上標靶','先知道看見板上哪一區']),
 ('棋盤交點供定位','corner',['角點在黑白格交界','不是ArUco圖樣的中心']),
 ('同ID連回板座標','pointtable',['保存ID、影像座標','與已知板上點逐一配對'])],
 'ID解決身份，棋盤角點提供精細影像位置。','偵測到ArUco標記之後，ChArUco流程估計及細化棋盤交點位置；實際可用角點取決於清晰度、視角、遮擋與設定。不能把marker中心替換成棋盤角點。')
add('charuco',3,'已知內參後，才另外解單張姿態',[
 ('板上3D與影像2D點','boardpose',['同名點、足夠幾何約束','K／畸變已知且適用']),
 ('求板到相機的R/t','pose',['X相機 = R × X板 + t','方向與長度單位都要寫清楚']),
 ('重投影與多解核對','residual',['檢查重投影與合理姿態','工站座標還需另一個轉換'])],
 '姿態、相機校正與工站轉換，是不同的交付。','solvePnP把板上物體座標轉到相機座標。平面與特定求解器可能涉及多解；需結合可見性、重投影和工作約束核對，單一低誤差不自動保證工站量測。')
add('charuco',4,'相機設定變了，舊參數還能用嗎？',[
 ('光學與影像條件','camera',['鏡頭／對焦／解析度','裁切也會改變像素座標']),
 ('改條件後重查投影','gridpair',['不能只沿用舊K','參數換算仍需獨立驗證']),
 ('量測另有尺寸基準','ruler',['校正誤差不是毫米保證','重拍、邊角與尺度另核對'])],
 '把參數與取像條件綁定，改條件就重新核對。','更換鏡頭、對焦或解析度後應重新確認參數適用性；已知縮放／裁切可換算像素內參但仍需驗證。毫米準確還涉及外參、工作平面、標靶真實尺寸和誤差預算。')
add('ecc',1,'ECC交付的是warp與相關值',[
 ('模板與待對圖','imagepair',['模板固定、待對圖可重採樣','需要合理初始位置']),
 ('依外觀做局部更新','curves',['比較共同座標的強度','依目標更新幾何參數']),
 ('輸出與核對分開','warp',['函式：相關值＋warp','另產生對齊圖及獨立殘差'])],
 '相關值不是缺陷標記，也不是獨立定位誤差。','OpenCV findTransformECC回傳相關值並更新warpMatrix。對齊影像需另行warp，獨立地標殘差與下游檢查也需另外計算；不把概念曲線當真實迭代紀錄。')
add('ecc',2,'一輪ECC：取樣、比較、更新',[
 ('目前warp重採樣','sample',['待對圖映到可比較座標','插值與mask影響可用像素']),
 ('對比模板強度','curves',['對齊前曲線位置不合','目標是外觀相關']),
 ('更新後重新取樣','iterate',['重複直到停止或失敗','不是每一步都保證改善'])],
 '靠密集外觀更新幾何，不靠逐點描述子。','同一物件的強度曲線僅用來顯示錯位如何影響比較。ECC局部最優化依照當前幾何和強度計算參數更新；過遠初始位置、遮擋或模型不適用仍可能失敗。')
add('ecc',3,'warp方向錯了，影像就會往反方向移',[
 ('定義座標與方向','axes',['模板座標 xT','待對圖座標 xI']),
 ('明確定義W','map',['xI = W(xT)','用待對圖取樣填模板格點']),
 ('取樣旗標須相符','sample',['常見OpenCV用法：','warp＋WARP_INVERSE_MAP'])],
 '先寫清楚映射方向，再決定反矩陣與取樣設定。','OpenCV findTransformECC的warp常配warpAffine/warpPerspective及WARP_INVERSE_MAP，把input重採樣至template座標。若交換角色或自行取逆矩陣，旗標也必須一致；用已知平移點做驗證，不憑圖看起來像就認為方向正確。')
add('ecc',4,'局部相關高，整件仍可能對錯',[
 ('同身份地標核對','landmarks',['孔位與外框都要查','不能只挑相似局部']),
 ('遠起點或遮擋','occlusion',['先找粗定位／改善取像','多迭代未必救得回']),
 ('保留失敗案例','evidence',['保存起點、warp與設定','既有實測另附原圖與來源'])],
 '用獨立幾何與完整失敗成本決定是否採用。','既有32/12平移及遠起點失敗案例保留在舊正式圖及來源紀錄，並非本輪重新執行。這張圖補說如何核對孔位身份、外框與失敗處理，不用單一相關值放行。')
add('sift',1,'SIFT先交付局部特徵，再由matcher配對',[
 ('跨尺度找局部','scale',['圖像金字塔尋找穩定局部','排除不穩定候選']),
 ('尺度與主方向','orientation',['每點附自己的局部座標','不是把整張照片轉正']),
 ('關鍵點與描述子','descriptor',['點：位置、尺度、方向','描述：局部梯度分布'])],
 'SIFT特徵、matcher點對與幾何變換分開。','SIFT透過尺度空間極值與局部定位找點，分配主方向後計算局部梯度描述子。匹配器以描述子尋找點對；單應、姿態或對齊並不是SIFT本身的輸出。')
add('sift',2,'描述子比較的是局部方向分布',[
 ('把梯度放進局部格','histgrid',['相對主方向的視窗','標準例：4×4個空間格']),
 ('組成描述向量','descriptor128',['每格8個方向分量','4 × 4 × 8 = 128']),
 ('比較第一與第二候選','ratio',['最近候選要夠獨特','相近兩名代表含糊'])],
 'ratio test篩含糊候選，不是幾何真值證明。','標準SIFT描述子為4×4空間格、每格8方向，共128維；實作含加權、正規化及截斷等步驟。最近與次近描述子距離比可用來篩掉含糊候選，但閾值須用目標資料驗證。')
add('sift',3,'點對夠多，仍要看幾何分布',[
 ('正確且分散的點對','spread',['平面單應最少4組','還要非退化且對應正確']),
 ('幾何估計與內點','homography',['選適用幾何模型','RANSAC也不是免驗證']),
 ('獨立位置再核對','residual',['看內點分布及誤差','保存原圖與錯配'])],
 '數量、正確性與分布要一起看。','四組合適的非退化平面對應可求單應；大量共線或錯配點仍可能不可用。需要按工作選模型，RANSAC篩選內點后仍看空間覆蓋與獨立地標誤差。')
add('sift',4,'有視差時，一張平面warp未必夠',[
 ('同場景不同深度','parallax',['前景與背景不在同平面','視點改變產生不同位移']),
 ('點對可能仍正確','pairs',['找對點不等於模型適用','不要直接怪matcher']),
 ('改選幾何假設','planes',['分平面或改3D方法','以工作誤差重新核對'])],
 '匹配失敗與幾何模型不適用，要分開診斷。','SIFT可提供局部對應，但場景有非平面視差時，單一單應未必能同時對齊全部深度。不能把此時的殘差全部解釋成特徵錯配，需檢查模型適用范圍。')
add('lightglue',1,'配對器前後各有負責的步驟',[
 ('相容extractor','features',['輸入：兩圖的點與描述','核對extractor／matcher權重']),
 ('LightGlue更新配對','attention',['有預訓練權重','圖內與跨圖上下文']),
 ('點對交幾何估計','pairs',['交付匹配與信心','另接幾何求解、取樣'])],
 'LightGlue不取代找點，也不直接給工站姿態。','官方提供多種extractor相容設定，部署應記錄權重、前處理與影像尺寸。點對與匹配信心不等於幾何準確率，後端求解與工作驗證仍需另行負責。')
add('lightglue',2,'圖內關係與跨圖關係，一起更新表示',[
 ('圖內注意力','selfattention',['同圖點之間交換資訊','局部描述加入上下文']),
 ('跨圖注意力','crossattention',['另一張圖提供候選關係','更新點的表示']),
 ('可匹配性與分配','assignment',['部分點可不匹配','輸出點對仍需幾何核對'])],
 '不是每個點都必須硬配到另一張圖。','LightGlue交替使用self-/cross-attention更新局部表示，再結合匹配分配與matchability；一些點沒有可靠對應時允許不匹配。圖為機制示意，無真實注意力或信心數值。')
add('lightglue',3,'自適應計算：少算什麼，要分清楚',[
 ('深度：可能提早停止','depth',['容易的配對可少跑層','困難影像可能仍需更多層']),
 ('寬度：修剪部分點','prune',['依判斷移除部分計算候選','不是刪掉原圖的像素']),
 ('量完整處理鏈','runtime',['extractor＋matcher＋幾何','同硬體、同影像才可比'])],
 '自適應不是每張圖都更快的保證。','自適應深度依信心提前停止，寬度修剪部分低可匹配點以減少後續計算；實際門檻與后端實現有關。完整時間包含extractor、資料搬移、matcher及幾何估計，不能只用名稱宣稱更快。')
add('lightglue',4,'少重疊或對稱物，先確認是否有線索',[
 ('少重疊','overlap',['兩圖共有可見區域有限','可用點對可能不足']),
 ('對稱與重複','repeated',['不同孔位可能很像','信心不是實體身份真值']),
 ('補資料再比較','camera',['補視角或已知定位點','固定資料比較完整成本'])],
 '看不見或分不出的部分，不能要求模型猜對。','用相同影像與獨立幾何證據比較傳統matcher及LightGlue；無法觀測的獨特細節不是換配對器一定能補回。優先核對重疊、紋理與遮擋，再決定是否換算法。')
add('det-dino-detector',1,'固定類別偵測：先把學習任務定清楚',[
 ('逐件框與類別','objects',['訓練資料需要物件標註','依工件／批次隔離資料']),
 ('查詢更新框與類別','queries',['影像特徵參與decoder','訓練後推論不使用真值']),
 ('交付候選物件','boxes',['邊界框＋類別＋分數','計數與品質判定另核對'])],
 'DINO detector是偵測器，不是DINOv2特徵骨幹。','本課討論DINO DETR系檢測器。模型交付物件框、類別與分數，而像素分割、毫米尺寸及業務計數規則需其他流程；訓練/驗證分組應避免同工件或連續影像泄漏。')
add('det-dino-detector',2,'Mixed query selection：位置與內容分開來',[
 ('影像提供候選位置','proposal',['由影像特徵選候選框','當作查詢位置的初始化']),
 ('內容查詢可學習','querycontent',['位置來自影像候選','內容使用可學表示']),
 ('decoder逐層更新','boxrefine',['位置與內容共同參與','逐步估類別與框'])],
 '推論起點來自影像，不是人工真值框。','DINO mixed query selection以encoder候選提供位置初始化，同時使用可學習內容queries。decoder進一步精修框與類別；與訓練專用的GT noisy queries區分，不能把二者畫在同一推論路徑。')
add('det-dino-detector',3,'對比去噪：正負查詢的學習目標不同',[
 ('從真值產生帶噪查詢','noisy',['只在訓練建立此分支','此圖展開一個標註物件']),
 ('近正樣本還原目標','positive',['學框與類別的還原','保留穩定的監督訊號']),
 ('負樣本學無物件','negative',['偏離的查詢學背景目標','不是把影像物件刪除'])],
 '帶噪查詢是訓練輔助，正式推論不用真值。','正負帶噪查詢以不同噪聲范圍構建，正樣本學習還原對應GT框/類，負樣本學習無物件。噪聲示意不是實際采樣統計；不能將no-object目標解釋為原圖里真的沒有物件。')
add('det-dino-detector',4,'新增類別，不能只改顯示名稱',[
 ('原本固定類別','classes',['類別頭與訓練標註綁定','更名不會學會新外觀']),
 ('新類別與新場景','newclass',['準備標註與獨立核對集','評估微調與其他方法']),
 ('用錯誤與成本比較','runtime',['查漏檢、錯類、重複框','包含訓練與覆核成本'])],
 '按類別變更與資料成本選方法，不按模型新舊。','與YOLO-World開放詞匯路線相比，DINO固定類別通常需相應模型頭與監督訓練/微調。二者都需現場獨立數據驗證；改變標注或閾值也應重新量測錯誤與完整成本。')
add('yolo-world',1,'開放詞彙偵測：詞彙是輸入的一部分',[
 ('文字詞彙與預訓練','vocabulary',['螺栓／墊圈等類別文字','使用視覺語言預訓練能力']),
 ('區域與文字表示','regiontext',['區域特徵與詞彙相互作用','以相似關係產生候選']),
 ('交付詞彙對應框','boxes',['候選框、類別與分數','不是自由問答或精密mask'])],
 '詞彙可變，但現場辨識能力仍需驗證。','YOLO-World利用預訓練視覺語言表示進行開放詞匯檢測，文字不是直接搜索像素的規則。提示類別的粒度、視覺可見性和訓練分布都會影響現場結果。')
add('yolo-world',2,'文字參與特徵整理，也參與區域比對',[
 ('視覺多尺度特徵','pyramid',['影像由骨幹編成特徵','不是把原圖當成語意真值']),
 ('視覺語言路徑','fusion',['詞彙與影像表示互動','整理適合找物件的特徵']),
 ('區域與詞彙打分','similarity',['比較區域／詞彙表示','分數配合框形成候選'])],
 '保留影像與文字兩條來源，才看得懂比對。','RepVL-PAN相關實現涉及圖像與文字交互及多尺度特征融合；此圖只表達職責，不偽稱完整張量架構。region-text相似關系用于類別評分，邊框回歸另承擔位置。')
add('yolo-world',3,'快取與重參數化，是兩種不同準備',[
 ('固定詞彙先編碼','cache',['保存文字表示','省每張圖重復編碼']),
 ('實作可做重參數化','reparameterize',['把固定詞彙整合進部署','依版本與支援路徑處理']),
 ('換詞彙就更新產物','refresh',['更新快取／部署模型','重新驗證相容設定與輸出'])],
 '快取向量不等於已完成模型重參數化。','官方reparameterize文檔區分快取文本特征和進一步轉換部署結構。采用時應記錄版本、詞匯、編碼器及部署產物；換詞匯需要對應更新，不只改前端顯示文字。')
add('yolo-world',4,'文字提示不是完整的檢驗規則',[
 ('詞彙描述目標','vocabulary',['類別名稱產生候選','不是布林NOT或因果規則']),
 ('輸出仍有觀測限制','smallobject',['小目標或反光細節不足','換詞未必改善可見資訊']),
 ('接專用檢查與覆核','inspection',['依目標用分類／分割等','用獨立真值驗證缺陷'])],
 '先定義要框物件還是判缺陷，再選後續流程。','物件框不證明細裂紋或缺陷存在，也不直接給尺寸或根因。若任務要求細缺陷，應先檢查取像像素和光照，再準備適合缺陷任務的標注與驗證流程。')
(W/'engineering-plan.json').write_text(json.dumps(rows,ensure_ascii=False,indent=2),encoding='utf-8')
REF='teaching-images/vision-ai-model-selection/course-delivery/section-pages/01-geometry-alignment-measurement/images/final/GEO-21-alignment-to-measurement_v01.png'
for r in rows:
 brief=f"# WI-032 {r['id']} r01\n- lesson objective: {r['takeaway']}\n- page type: {r['kind']}\n- primary reading path: {' → '.join(r['nodes'])}\n- major visual nodes:\n"+''.join(f'  {i+1}. {n}\n' for i,n in enumerate(r['nodes']))+f"- named guide-conformant reference page: `{REF}`\n- pale-yellow takeaway: #FFF4CC：{r['takeaway']}\n- generation: 新建精確SVG→PNG，桌機1672×941、手機直向；actual PNG review pending，user approval pending。與首讀PNG分開。\n- source: {r['source']}\n- evidence: {r['detail']}\n"
 brief=brief.replace(' → '.join(r['nodes'])+'\n',' → '.join(r['nodes'])+' → 工作核對與接手\n')
 p=W/f"{r['id']}-r01.md";p.write_text(brief,encoding='utf-8')
 v=subprocess.run([sys.executable,'-X','utf8',str(C/'tools/validate_teaching_preflight.py'),str(p)],capture_output=True,text=True,encoding='utf-8');assert v.returncode==0,v.stdout+v.stderr
print('24 engineering stories and preflights saved; not rendered or reviewed')
