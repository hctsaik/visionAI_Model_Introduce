# WI-040：定版收尾

依使用者授權完成 WI-039 三項收尾：先驗備份格式、首頁直達需求助手、三課展開機制一致性。

## 行為變更

備份匯入在確認取代之前驗證識別、版本與必要結構。非課程檔、陣列、損壞欄位與不支援版本均拒絕，原進度不變；取消及儲存失敗也保留原狀。新匯出帶 backupType；無識別碼但符合既有結構的 v1/v2 備份仍接受，v1 遷移至 v2，缺省 PoC 為空草稿。

首頁第一個主要操作為「從現場問題找方法」，直接開啟第一題；保留繼續課程、共同前提與家族探索。

實看後縮短首頁標題並以首頁專用字級避免桌面零碎換行；最後截圖與公開入口操作驗證涵蓋此調整。

ConvNeXt 回到輸入／運算分工／分類／驗證；V-JEPA 分開預訓練特徵與下游任務；AnomalyGPT 分開內建定位與選用應用整合。三課的 title/body/why/anchor 與 diagram 鏡像同步，設定改為可核對條件。詳細來源見 content-changes.md。

## 實際驗證

- 新增 hardening 測試兩入口各 6 項，exit 0。每入口包含 9 類錯檔、兩種舊版本、新備份往返、取消、儲存失敗及兩種寬度首頁路徑。
- 既有 PoC 13 項測試通過，50.831 秒、exit 0，包含六情境子測試、草稿／JSON／Markdown、手機與課程筆記。
- 導覽模板 3 項回歸斷言通過；這是模板驗證，不冒稱三項真人導航試驗。
- source/docs 逐位元一致，58 課編譯資料只變更三課 mechanism_steps；其他文字與全部圖片引用不變。
- 10 張桌面／手機截圖：首頁完整及首屏、三課展開文字。因果鏈獨立截圖只在拍攝時隱藏固定導覽，避免長元素截圖把頂欄重疊在文字上；未修改網站導覽。實看無新橫向溢出與 JS 例外。
- build 產出 1370 資產；不對未重評的319圖給分。不重跑已知舊全站 verifier 的 schema 差異，也不宣稱全站模型實測。

首次 hardening 儲存 mock 因 Playwright 將回傳函式當 expression 執行而失敗，修正測試寫法後兩入口完整通過；保留 attempt1 紀錄。最終腳本與真實收據分開保存。

目前狀態：實作、本機驗證完成，Git／公開部署待完成。使用者成品審閱 pending；可選的學習檢核位置、全站通用文字清理仍屬後續改善。

## 學習

確認覆蓋不是格式驗證。匯入需要先驗證來源結構與版本，保持合法舊資料相容；測試同時涵蓋拒絕、取消及保存失敗。新手流程需要從首頁就能找到，不能只驗直接打開功能網址。教學同步要包含預設主線與展開參考，避免原模型和應用管線混在同一因果鏈。


三項修正與本機驗證完成：12項新行為測試、13項既有PoC、3項導覽斷言通過；10張截圖實看，僅三課mechanism_steps變更。即將提交推送與驗證公開站，公開發布未完成。
