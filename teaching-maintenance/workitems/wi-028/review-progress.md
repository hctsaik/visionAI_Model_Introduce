# WI028 逐圖候選審查 checkpoint

- bg-c1 desktop r01：已看原生，固定場景、背景累積與目前前景對應成立；需頁內尺寸與手機再查。網格僅常見外觀示意，不作精確分布圖。
- raft-c1 desktop r01：已看原生，两幀、抽象特徵、全配對查詢／迭代及稠密位移責任成立；零背景與遮擋估計標示清楚。待尺寸與手機。
- lk-c1 desktop r01：需修正。同一剛性板純右移卻有不同方向的點箭頭，會誤教局部位移；下一版所有有效箭頭同長水平右向，被遮點只標追丟。此圖未採用。
- 八課文字草稿已保存 lesson-content.json；尚未整合到正式topic。
- 下一步：LK r02，再做其餘核心桌面原型與已看核心的手機圖；每張仍須獨立評分。
- conv-c1 r01 desktop：已實看，三時刻夾爪、抽象空間狀態更新及任務頭分工成立，待手機／縮放審查。
- byte-c1 r01 desktop：高分→未匹配軌跡用低分、未配對噪聲丟棄及ID與計數分工成立；右側遮擋示意較弱，需手机仍保留可見遮擋。
- mae-c1 r01 desktop：需修正。左側只是透明網格，不夠明確遮掉內容；灰遮蔽token可能誤讀成進編碼器。r02左側改大多數不透明灰塊，灰token獨立連到解碼器，只有可見片段進編碼器。
- jepa-c1 r01 desktop：需修正。左側三影格各自編碼器像逐幀影像編碼，不清楚影片共同編碼；r02改成三影格合為一組，只進一個上下文編碼器，保持原有兩路特徵比較。
- lk-c1 r02 desktop：箭頭已全部同向右移，全圖實看通過此修正；待手机及頁內尺寸。
- mae-c1 r02 desktop：不透明遮蔽與灰token直入解碼器已修正；輸出重建圖與原始像素監督分開。待手機／縮放。
- jepa-c1 r02 desktop：左側已合併一個影片編碼器；上下文和完整目標路徑分開，無像素生成。待手機／縮放。
- bg-d2、frame-d2 r01 desktop：已實看停留吸收和照明變化兩反例，物件維持、方法輸出與動作成立；待手机／縮放。
- change-d3 r01 desktop：FAIL，前景結果被放在Frame Difference區，背景方法只剩字。r02改三大欄：共同輸入、差分參考＋黑結果、背景參考＋白前景；两方法獨立，不串接，標題與結果就近。
- byte-d2 desktop r01：遮擋中只有虛線預測、無可見工件，前後身分候選與中斷／回查成立。待手機。
- conv-d2 desktop r01：片段切換和舊狀態混入可見；狀態圖以動作圖示解釋語意，不是原始記憶張量，正文會明示。待手機。
- track-d3 desktop r01：改用銀色L形支架作新情境比較，三幀位置和ID一致；從偵測到追蹤為本來依賴，不是將互斥模型串接。待手機。
- video-d3 desktop r01：需修正。V-JEPA短註誤寫「未來特徵」；VideoMAE重建輸出箭頭下接任務頭會暗示部署輸入是重建影像。r02改「被遮區特徵」，底部部署獨立標「編碼器＋任務頭」，刪除重建／目標比較到任務頭的資料箭頭。
- change-d3 desktop r02：參考與結果已分別放入差分／背景區，黑差分與白前景和同工件对应成立，可做手機。
- bg-c1 mobile r01、RAFT mobile r01已看原生和328px：上下主線與結果可讀；背景累積小格為補充，核心比較可見。待正式頁。
- lk-c1 mobile r02：FAIL，被遮點提示移到右側但有效箭頭仍在原點；r03將紙片覆蓋左下角點並刪該點箭頭，保留上方兩個同向右箭頭。
- mae-c1 mobile r02：三主區內仍放多欄小圖，原生1024×1536在328px必要標籤偏密。r03改高直式768×2048，刪重複tube立方圖與副段落，保留可見token→編碼器、遮蔽token→解碼器及下游分工。
- jepa-c1 mobile r02、bg-d2 mobile r01已看原生，責任與因果成立；待328px與正式頁。
- flow-d2 desktop r01：需重製。誤把「三區固定相機」設計指令印成標題並畫三台相機，L刻痕對應位置也不一致。r02以單一金屬板局部對照：清楚角點、後幀高光蓋角、回查原片，刪相機陣列／多區L來源不一致。
- lk-c1 mobile r03：已修正為兩個有效右箭頭、一個被遮左下角點，其他局部對應未變，原生實看通過此缺口。
- change-d3 mobile r02：FAIL，背景參考寫空卻仍有銀片。r03只移除右方法參考照片中的銀片，保留同位置灰帶和下方白色前景結果。
- mae-c1 mobile r03：可讀性改善，但新增數字圓章且遮蔽又變透明；r04移除1／2／3圓章，三幀同位置用不透明灰塊遮蔽，只保留相同兩處可見窗口。仍未採用r03。
- ByteTrack核心重查：預測框與偵測框僅窄帶重疊，不足以清楚表達合理位置關聯；桌面／手機r02把兩個綠虛線預測框移至與對應框大幅重疊，保留不同線型。
